# CLAUDE.md

Canonical context for AI assistants working in this repo. If anything elsewhere disagrees, trust this file.

## What this is
**ORA Navigator** — an AI assistant + proposal toolkit for Morgan State University's **Office of
Research Administration** (ORA): grants, compliance, pre/post-award, forms, ORA contacts, and a
guided workspace that helps inexperienced PIs actually write and submit fundable proposals.

## Three services
| Service | Stack | Local port | Container port |
|---|---|---|---|
| **frontend** | Vite + React 19 (PWA) | 3001 | 8080 |
| **backend** | FastAPI, **single-worker uvicorn** | 5002 | 5000 |
| **adk_agent** | Google ADK + Gemini 2.5 Flash (Vertex AI), `VertexAiSearchTool` over one unified KB datastore | 8081 | 8080 |

The chat path: frontend → backend (`vertex_agent.py`) → ADK agent (`adk_agent/ora_navigator_unified/`) → Gemini on Vertex AI. The backend also serves the proposal toolkit (below) and talks to Cloud SQL (MySQL) + Redis.

## Build / test / deploy
- **Backend tests (keep green before any push/deploy):**
  ```bash
  cd backend && JWT_SECRET=test-secret TRUSTED_HOSTS=testserver,localhost,127.0.0.1 \
    python3 -m pytest -q --ignore=tests/test_agent_instruction.py
  ```
  Env vars are required because importing `main` constructs the app. **`main.py` imports several services INSIDE the endpoint function, not at module scope** (`compliance_sentinel`, `solicitation_extractor`, …) — so a long-running local uvicorn with **no `--reload`** still picks up your edits on the *first* call to an endpoint whose service it has not imported yet. This cost a wrong conclusion on 2026-08-12: a server started the previous day was used as the "before" half of a before/after comparison and returned the *new* behaviour, making a real fix look like a no-op. **To compare old vs new, load the committed module explicitly** (`git show HEAD:backend/services/x.py` into a temp path + `importlib.util.spec_from_file_location`) rather than trusting a server's age. `test_agent_instruction.py` needs `google-adk` (not always installed locally). App-level e2e tests use FastAPI `TestClient` with `dependency_overrides` for `get_db` / `get_current_user` (see `tests/test_proposals_api_e2e.py`).
- **Frontend:** `cd frontend && npm run build` (no `node_modules`? `npm install` first). It's a **PWA** — verify changes in a fresh/incognito window.
- **Deploy: there IS a Cloud Build trigger — `deploy-to-main` — and a push to `main` SHIPS TO PRODUCTION.** This corrects what this file asserted until 2026-08-07 ("there is NO trigger; a merge to `main` ships NOTHING; every deploy is by hand"), which was wrong and cost a production incident. **`git push origin main` is a deploy.** Treat it with the care golden rule 7 already demands.
  **Why the old claim survived so long — both checks that would disprove it come back EMPTY:**
  ```bash
  gcloud builds triggers list          # -> empty
  gcloud builds list --limit=10        # -> shows ONLY hand-submitted builds
  ```
  **THE REGION IS `us-east4`** (confirmed 2026-08-11 by pushing to main and hunting for the build). Both commands above default to `global`, which is why they come back empty and why this file said for months that no trigger existed. The commands that actually work:
  ```bash
  gcloud builds list --region=us-east4 --limit=5
  gcloud builds describe <BUILD_ID> --region=us-east4 --format='value(status,logUrl)'
  gcloud builds triggers list --region=us-east4
  ```
  Observed 2026-08-11: a push to `main` at 20:30:48 UTC produced build `7c35d4dc` in `us-east4` at 20:30:59 — eleven seconds later — and finished SUCCESS in **6m25s**, all ten steps green (build/push/deploy for adk, backend and frontend, then a summary). Backend `00222-jrz` -> `00223-prd`, frontend `00172-lp6` -> `00173-qsw`. So the trigger is real, it is fast, and **a push to `main` is a production deploy** — the golden rule 7 warning is now confirmed twice over. **Confirmed a THIRD time 2026-08-12:** a push at 20:43:57 UTC produced build `2f972183` in `us-east4` at **20:44:08 — eleven seconds later**, again, and finished SUCCESS; backend `00223-prd` -> `00224-b4d`, frontend `00173-qsw` -> `00174-ctb`, ADK untouched and still `min=max=1`. Eleven seconds and one revision bump per service is now the known-good signature — if a push does not produce a queued build in `us-east4` within ~15s, something is wrong with the trigger, not with your query.
  **Eleven seconds is the TYPICAL signature, not a guarantee — do not treat a slower queue as a broken trigger.** Observed 2026-08-13 (commit `a59735d`): the build was **not** in `gcloud builds list --region=us-east4` twenty seconds after the push and took roughly **90 seconds to appear as QUEUED**, then ran SUCCESS in the usual few minutes (backend `00226-gx2` -> `00227-8dj`, frontend `00176-lkh` -> `00177-2qn`, both 100% traffic, `draft_review_json` migration logged clean on prod MySQL). This file previously implied that nothing queued within ~15s meant the trigger was broken; that would have been a wrong conclusion here. **Poll for the SHORT_SHA rather than checking once**, and confirm from the serving revision, never from a single empty list.
  **Note the repo has branch protection requiring a PR, which an owner push BYPASSES with a warning** (`remote: Bypassed rule violations for refs/heads/main: Changes must be made through a pull request`). The push still succeeds and still deploys — so that warning is the only thing standing between a direct push and production, and it is easy to read past.
  Trigger-fired builds and the trigger itself live in a **region** that neither default-scoped command reads, so the evidence for "no trigger" is an artifact of the query, not a fact about the project. Observed 2026-08-07: a build appeared in the Console (`Trigger: deploy-to-main`, branch `main`, commit `001eb85`) that was absent from `gcloud builds list`, and a Cloud Run revision appeared with an image nobody could account for — it was the push firing the trigger. **Confirm from the Cloud Console, or from the serving revision, never from an empty `gcloud builds list`.**
  A manual submit is still possible and is what you want when deploying **uncommitted** work (it uploads the working directory, not the commit):
  ```bash
  gcloud builds submit --config=cloudbuild.yaml . --substitutions=SHORT_SHA=$(git rev-parse --short HEAD)
  ```
  **Do NOT push and then also submit by hand.** Both builds tag the same `SHORT_SHA`, Docker builds are not reproducible, so they produce different digests and **race on the tag**. The `deploy-frontend` verify step then resolves `frontend:<sha>` to the *other* build's digest and fails with "the serving revision does NOT run this build's image" — a true statement about a harmless situation, and a red build that means nothing. Measured 2026-08-07: two builds of `001eb85` started 17 seconds apart and did exactly this.
  **The `--substitutions=SHORT_SHA=…` is REQUIRED on this config too, not just the scraper's.** `cloudbuild.yaml` interpolates `${SHORT_SHA}` in **9 places**, and it is populated only for repo-triggered builds — a local directory submit leaves it empty and the build dies instantly with `invalid image name ".../adk-agent:": could not parse reference`, naming a service rather than the missing substitution. This file previously gave the command without the flag, so the documented instruction failed on first contact. There's also a manual `deploy-cloudrun.sh` (needs local `gcloud`). **Always confirm a deploy actually ran** — `gcloud run services describe <svc> --region=us-central1 --format='value(status.latestReadyRevisionName)'` — rather than inferring it from a merge or a green build list. The **backend** runs `--min-instances=1 --cpu-boost` so the chat path never pays a 30–60s cold start (the dominant cause of slow first answers); the frontend stays `min-instances=0` (static, off the per-question path). **The ADK service MUST stay a SINGLE instance (`--min-instances=1 --max-instances=1`).** It stores agent sessions **in memory** (no `ADK_SESSION_DB_URI`), so with >1 instance a session created on one instance **404s on `/run_sse`** when the next call is routed to another — the user sees *"An error occurred while processing your question"*, intermittently (random per-instance routing). A brief bump to `--min-instances=2` for concurrency caused exactly this outage. **Never scale ADK horizontally without wiring a shared session store (`ADK_SESSION_DB_URI`) first.** `concurrency=80` lets one warm ADK instance handle normal + demo load fine. (You can apply a scaling change live without a full rebuild via `gcloud run services update <svc> --min-instances=N --max-instances=M`.) **A red build does NOT mean nothing shipped — and `deploy-frontend` is now the only step that tells the truth.** Cloud Run versions each service optimistically, `gcloud run deploy` asserts the version it read, and this service carries the `ora.inavigator.ai` domain mapping whose controller reconciles independently and bumps that version mid-deploy. Observed 2026-08-06: the deploy created the revision, routed 100% of traffic to it, **then exited 1** with `ABORTED: version '<16:49:50>' was specified but current version is '<18:04:07>'`. A red build that had shipped everything — worse than a failure, because the signal then lies in both directions (a green build on this repo already ships nothing, since there is no trigger). The step now retries the race three times and decides its exit code on the only evidence that matters: whether the serving revision runs the digest this build pushed. It **fails closed** — an unreadable digest or revision exits 1 rather than letting an empty string glob-match anything. **`cloudbuild.yaml` is a single point of failure** — the backend deploy step is an inline bash heredoc, so a one-character typo there (e.g. a stray `=` swallowing `--min-instances`) breaks the *whole* deploy and silently blocks every merge from shipping. Validate it (`python3 -c "import yaml; yaml.safe_load(open('cloudbuild.yaml'))"`) after any edit.
- **THERE IS NO `gh` ON THIS MACHINE, and the GitHub MCP server cannot authenticate — the working path to open a PR is the keychain credential (2026-08-20).** `gh` is not installed and not on `PATH` (same shape as `gcloud`, which lives at `~/google-cloud-sdk/bin/gcloud` — except `gh` is genuinely absent, not merely unlisted). `mcp__github__authenticate` fails outright with *"Incompatible auth server: does not support dynamic client registration"*, so the OAuth flow cannot be started programmatically; a human running `/mcp` is the only way through it. There is **no `GITHUB_TOKEN` in the environment and no `~/.git-credentials`** — git uses the **osxkeychain** helper, which is why `git push` works while every API call 401s. What DOES work, and is how PR #82 was opened:
  ```bash
  TOK=$(printf 'protocol=https\nhost=github.com\n\n' | git credential fill | grep '^password=' | cut -d= -f2-)
  curl -s -X POST -H "Authorization: Bearer $TOK" -H "Accept: application/vnd.github+json" \
    https://api.github.com/repos/lama9811/ORAnavigator/pulls -d @body.json
  ```
  The credential is the same one `git push` uses and carries repo scope. **Never echo it** — pipe it straight into the call. Two things worth knowing before reaching for this: it may raise a macOS keychain prompt the user has to approve, and it is the USER's personal credential, so an action taken with it is attributable to them — ask before using it for anything they did not explicitly request. Installing `gh` (`brew install gh`) would remove the whole detour and is the better fix if PRs become routine.
- **Backend service-account IAM** (`oranavigator-backend@<proj>.iam.gserviceaccount.com`): the admin dashboard **writes** to the KB datastore, so the SA needs **`roles/discoveryengine.editor`**. With `roles/discoveryengine.viewer` alone (what `deploy-cloudrun.sh setup_iam` used to grant) reads work — the KB browser lists and searches fine — but every **Save 403s** with `discoveryengine.documents.update denied`, which reads as a UI bug rather than a permissions one. Editor covers `documents.create/.update/.delete`. Note this is a **project-level** grant and therefore broader than needed (it also allows datastore/engine create+delete); least privilege would be a custom role limited to `discoveryengine.documents.*`. Accepted deliberately — revisit if the project gains other Discovery Engine resources. `roles/discoveryengine.viewer` is now redundant alongside it. **`gcloud` may not be on `PATH` locally** — it lives at `~/google-cloud-sdk/bin/gcloud`.
- **Secrets** live in Secret Manager as `ora-database-url`, `ora-jwt-secret`, `ora-admin-email`, `ora-admin-password`, `ora-redis-url`, `ora-firecrawl-api-key`, plus **`SMTP_USER` / `SMTP_PASS`** (mapped in `cloudbuild.yaml`). Note the SMTP pair **breaks the `ora-*` naming convention** — they were created by hand on 2026-07-28 and replace the retired `ora-smtp`; don't "fix" the names without re-pointing `--set-secrets`. The Cloud Build runs as the **default compute service account** (`<projnum>-compute@…`), which lacks `secretmanager.secrets.create` — so the `deploy-backend` step **self-heals** `ora-firecrawl-api-key` (creates a `unset` placeholder if missing) so a missing optional secret never breaks the deploy. Create real secret values via the Console (the project picker prevents the "wrong project" mistake). **A hand-created secret has an EMPTY IAM policy** — the backend SA can't read it and the deploy fails outright. `SMTP_USER`/`SMTP_PASS` both needed an explicit grant: `gcloud secrets add-iam-policy-binding <name> --member=serviceAccount:oranavigator-backend@<proj>.iam.gserviceaccount.com --role=roles/secretmanager.secretAccessor`. The pre-existing `ora-*` secrets already carry this binding, which is why the omission isn't obvious.
- **`GOOGLE_GENAI_USE_VERTEXAI=TRUE`** must be set on the ADK **and** backend services (in `cloudbuild.yaml`) or Gemini falls back to the developer API and every call fails with "No API key was provided."
- **The KB scrape Job is built separately** — `gcloud builds submit --config=cloudbuild.kb-scraper.yaml . --substitutions=SHORT_SHA=$(git rev-parse --short HEAD)`, **not** from `cloudbuild.yaml`. Deliberate: the image is ~1.5GB (Playwright + Chromium), changes rarely, and putting it on the path every merge takes would slow every deploy and widen the blast radius of the file that already blocks all deploys when it breaks. **The `SHORT_SHA` substitution is required** — it's populated only for repo-triggered builds, so a local directory submit leaves it empty and the image tag becomes the invalid `kb-scraper:`. Two IAM grants are needed once, and **the second is the easy one to miss** — without it the admin button 403s while the run sits `queued` forever with nothing in the app logs: `roles/cloudsql.client` on the project for the backend SA, and `gcloud run jobs add-iam-policy-binding oranavigator-kb-scraper --region=us-central1 --member=serviceAccount:oranavigator-backend@<proj>.iam.gserviceaccount.com --role=roles/run.jobsExecutorWithOverrides`. **That role, NOT `roles/run.invoker`** — this file and `cloudbuild.kb-scraper.yaml` both said `run.invoker` until 2026-07-29 and it was wrong. `run.invoker` grants `run.jobs.run` but **not** `run.jobs.runWithOverrides`, and `kb_scrape_service.py` passes a container override to inject `SCRAPE_RUN_ID`. The backend SA already holds `run.invoker` **project-wide**, so following the old instruction granted nothing and left the identical 403 — a fix that looks applied and changes nothing. `roles/run.developer` also works but carries ~80 permissions including `run.services.delete`; the scoped role is exactly three (`run.jobs.run`, `run.jobs.runWithOverrides`, `run.executions.cancel`).
- **The scrape engine is Playwright + Chromium (`kb_scraper/crawler.py`), the default since 2026-08-03.** A headless browser reads each page, so there is **no model in the read path**: no refusals, byte-stable text, and the accordions get expanded. Measured against the live site 2026-08-03, before any code change: **21/21 pages readable, including all 18 the Gemini engine refused** (the whole of research-compliance, budget development, the internal routing form, the PI handbooks, `/ora`); the same page read three times gave an **identical hash** (`fanda-cost-rates` 1540 chars, `iacuc-sops` 3102 chars, three times each); the IACUC SOPs page yields **50 `SOP n.n:` entries**, confirming `_expand_accordions()` reaches content absent from the served HTML; ~2.9s/page median, so a 59-page crawl is ~3 min against the 3600s task timeout. Because the text is byte-stable **the fingerprint gate is live** — only pages that actually moved cost a model call, where the old engine re-adjudicated everything every run. Playwright also **discovers links**, so pages with no KB document surface as `new` proposals (a genuine content-gap inventory; expect a batch on the first run). `gemini-3.6-flash` is still the **adjudicator** — only page-reading changed.
- **The Gemini reader is still available as `--engine=gemini` / `SCRAPE_ENGINE=gemini`, and its limits are why it is no longer the default.** `kb_scraper/gemini_crawler.py` sends each URL to the model with `"tools":[{"urlContext":{}}]` and asks for a markdown **extraction**. Three measured properties: **(1) 26 of 59 ORA URLs (44%) come back EMPTY with `finishReason: RECITATION`** — the safety filter for reproducing source material. The blocked set is the compliance core: *all* of research-compliance (human subjects, animal research, COI, research security, NSPM-33, RCR, Maryland ethics), most of pre-award (budget development, internal routing form, proposal components, PI role), post-award reporting, the PI handbooks, and `/ora` itself. Under this engine those pages are **not monitored**. The readable 33 are the brochure pages (About, Mission, Announcements, Staff Directory, Funding Sources). Prompt wording matters: "reproduce the full text verbatim" is blocked on nearly every page, "extract into markdown" on 44%. **(2) A RECITATION block is reported UNREADABLE, never as an empty or deleted page** — same invariant as `looks_unreadable()`, and the most destructive bug this job could have. **(3) The extraction is NOT byte-stable** — one unchanged page read three times at temperature 0 measured 1444/1466/1478 chars, three different hashes — so `resolve_audit()` forces `--audit` for this engine rather than leaving a gate in place that silently claims every page moved every run. Under it, quote grounding verifies against the model's own extraction rather than the page, which is weaker than golden rule 2 gives you on Playwright. **It calls REST, not the google-genai SDK** — this image pins `google-genai==1.14.0` to match `backend/requirements.txt` and 1.14.0 has no `url_context` field on `types.Tool`.
- **Fingerprints are engine-specific (`KbPageFingerprint.engine`).** A fingerprint hashes the extracted page **text**, and the two engines extract the same unchanged page differently, so a hash is only a valid baseline for the engine that wrote it. `load_baseline()` filters by engine and `upsert_fingerprint()` stamps it; a row from the other engine — or a pre-migration `NULL` — reads as a **first sighting**, which records the hash and proposes nothing. Two consequences worth knowing: this is what makes switching engines a silent re-baseline instead of ~59 bogus "changed" proposals, and it scopes the **removed-page sweep** (`prior` minus the URLs seen this run) so a switch doesn't propose that every previously-known URL was deleted from the site. **`url` stays `unique=True`**, so there is one row per page and switching engines overwrites rather than preserving a per-engine baseline — which is why both write paths upsert: a bare INSERT raises `IntegrityError` on a page the other engine already saw. Added with the usual self-healing migration in `init_db()` (golden rule 5).
- **`SCRAPE_ENGINE` was set BY HAND on the live Job, and the repo does not show it.** `cloudbuild.kb-scraper.yaml` does not mention `SCRAPE_ENGINE` — but the deployed Job carried `SCRAPE_ENGINE=gemini` as an env var (observed 2026-08-03 on execution `…-dlccm`), set by someone running `gcloud run jobs update` after the 2026-07-29 deploy. **An env var beats the code default**, so changing the default in `build_parser()` and shipping it would have changed nothing, silently — the Job would have kept using Gemini while the code, the tests and the docs all said Playwright. What saves it is that line 107 of the yaml uses **`--set-env-vars`**, which *replaces* the whole environment set rather than merging, so a deploy through that file clears the hand-set value. Two rules follow: **never reason about this Job's configuration from the repo alone** (`gcloud run jobs executions describe <exec>` prints the env actually used), and **never "fix" line 107 to `--update-env-vars`** — that would preserve the stale override forever.
- **The scraper's Gemini model is pinned separately from every other caller.** `SCRAPE_MODEL=gemini-3.6-flash` + **`SCRAPE_MODEL_LOCATION=global`**, both read only by `kb_scraper/adjudicator.py` (the location falls back to `GOOGLE_CLOUD_LOCATION`, so no other caller moves). Adjudication is offline batch work over ~59 pages where judgment quality matters and latency doesn't — the opposite of the chat path, which stays on `gemini-2.5-flash` in `us-central1`. **The location override is required, not a preference:** `gemini-3.6-flash` **404s in `us-central1`** and answers only on the `global` endpoint (verified by direct `generateContent` calls). Because a model failure falls back to "not material" (golden rule 3), getting this wrong makes every adjudication 404 and the run reports **zero changes and looks like a clean crawl**. `thinking_config(thinking_budget=0)` is accepted by 3.6-flash and bills no thinking tokens (verified), so the existing anti-truncation setting carries over unchanged.

## Golden rules (followed everywhere; keep following them)
1. **AI is advisory; the deterministic core is authoritative.** Numbers, pass/fail, and eligibility verdicts are computed by code — the LLM only explains, drafts prose, outlines, or gives advisory feedback. It can never change a figure.
2. **Ground every AI claim.** Any "covered/strong" finding must quote the source text; unquotable claims are dropped. The shared membership test is **`services/text_match.quote_in`** — used by `draft_review` (a positive finding without a verifiable quote is demoted to `not_found`) and by `solicitation_requirements` (an extracted requirement whose quote is not in the document is dropped, and counted). The membership check **collapses whitespace on both sides** (`" ".join(s.split())`) — a pasted/hard-wrapped draft has newlines where Gemini's quote has spaces, and a raw substring match would wrongly reject a real quote and mark a complete section "NOT FOUND".
3. **Graceful fallback.** Every AI path returns a deterministic result when Gemini is unavailable (`gemini_client.generate_json/text` return `None` → caller falls back). Never error because the model is down.
4. **Human reviews before commit.** Extractions and budgets are returned for review; nothing is saved until the user confirms.
5. **Conventions:** structured data stored as **TEXT columns + `json.dumps/loads`** (e.g. `Submission.budget_json`, `compliance_json`, `sections_json`, `notes`). New columns are added with a **self-healing migration** in `main.py:init_db()` (try `SELECT col`; on error `ALTER TABLE ADD COLUMN`) — safe on prod MySQL, no Alembic.
6. **One feature = one focused change.** Don't refactor unrelated code. Verify before claiming done.
7. **Never push automatically.** Do NOT run `git push` (and don't open PRs) on your own — only when the user explicitly asks in that message. Pushing to `main` fires the **`deploy-to-main` trigger** and ships to production — CONFIRMED 2026-08-07, and note this rule was right while the Deploy section above said the opposite for months. An unrequested push is an unrequested deploy. A push is also sufficient: do NOT follow it with a manual `builds submit`, which races the trigger's build on the same image tag. Committing locally is fine to stage work, but wait for an explicit "push"/"deploy" before it leaves the machine. Approval to push once does not carry over to later changes.

## The Proposal Toolkit (backend `services/` + per-proposal modals in `frontend/src/components/`)
Each proposal (`Submission` + `SubmissionTask` in `models.py`, logic in `proposals_service.py`, sponsor task templates in `proposal_templates.py`) has a toolbar of tools:

- **Solicitation ingestion** — `solicitation_extractor.py` (PDF→text via pdfplumber → Gemini → contract dict with `source_quotes`; fabricated quotes flagged via `_verify_source_quotes`). Two entry points: upload a PDF, or **paste a URL** (`url_fetcher.py`). The URL fetcher is SSRF-guarded (resolve + reject private/loopback/link-local IPs, pin the validated IP to defeat DNS rebinding). Some funder sites (NSF/Akamai) **block cloud-datacenter IPs**, so a direct fetch falls back through a scraper chain: **Firecrawl** (`_fetch_via_firecrawl`, preferred — runs a real browser from non-blocked IPs; needs `FIRECRAWL_API_KEY`, and treats the `unset` deploy-placeholder as no-key), then `r.jina.ai` (set `JINA_API_KEY`), then `allorigins`. Uploaded PDFs are 100% local (pdfplumber) and never hit an IP block. `create_submission_from_solicitation` seeds the checklist; multi-category deadlines are surfaced via `deadline_details`. The extracted **eligibility text** is stored in `notes`. This pass extracts the CONTRACT only (deadline, cap, page limits, attachments) — the solicitation's **requirements** are a separate, much deeper read (`services/solicitation_requirements.py`, below) and the document's text is never stored. **Model: `gemini-3.6-flash` on `location="global"`** (`SOLICITATION_MODEL` / `SOLICITATION_MODEL_LOCATION`, moved off 2.5-flash 2026-08-10). The location is REQUIRED, not a preference — 3.6-flash **404s in us-central1**, so the pair must move together. Why upgrade this one: it is the highest-stakes extraction in the app (a wrong cap/deadline/page limit propagates into the checklist, the Budget Helper's prefilled cap and the Draft Critic, and nothing downstream re-verifies it) and it is offline, one-shot and latency-tolerant. Measured on NSF 23-598: 3.6-flash found **5 required attachments incl. the Data Management Plan and Postdoctoral Mentoring Plan**, which 2.5-flash missed on both runs (3 each). **Neither is deterministic** — 3.6-flash returned 3 then 5 on identical input at temperature 0, so human review before commit stays load-bearing. **This module builds its OWN Gemini client and has NO retry/backoff**, unlike `gemini_client`: an observed transient 429 returned an empty extraction that reads as "the PDF had nothing in it".
- **Budget Helper** — `budget_helper.py`: deterministic F&A/MTDC/fringe math (Morgan's real rates), sponsor-cap check. **Coaching layer:** `budget_advisories()` (sanity flags), `suggest_trims()` (concrete cuts to get under cap), category/F&A/fringe tooltips. **v2:** multi-year projection with escalation (`compute_budget` detects `project_years`; single-year output unchanged), CSV export (`budget_to_csv` + `/budget.csv`), per-line justification, and a **deterministic prose** budget justification (`draft_justification` — labeled paragraphs, reads like a real submission). AI only polishes the prose and is forbidden to change figures; the `/api/budget/justification` endpoint falls back to the template when the AI output is empty **or truncated** (missing the total-cost figure — a Gemini-under-load failure mode). The UI **effort→person-months helper is read-only**, derived from each person's `effort_pct` in the People rows (not a separate input), so it can never disagree with the budget. **Per-category caps:** when a solicitation defines multiple proposal categories with different award maxima (e.g. NSF IDSS Category I/II/III), `solicitation_extractor` returns `budget_cap_details` (`[{category, cap}]`, a stated **range → max**), persisted as a `Category caps:` line in `notes` (round-tripped by `reconstruct_solicitation_context`; category names sanitized of `;`/em-dash); the single `budget_cap` stays the smallest. **`budget_cap_status` (2026-08-12) separates "this solicitation sets NO per-award cap" from "we couldn't find one"** — an empty `budget_cap` used to mean both. Three values: `"stated"` (a cap was extracted), `"not_stated"` (a POSITIVE finding), `None` (unknown). The **status is derived from the value, never from the model's claim** (`_coerce_extracted`: a non-null `budget_cap` forces `"stated"`, so the two can never contradict each other on screen; only `"not_stated"` is taken from the model, and only as that exact token). Round-trips through `notes` as the line **`Budget cap: none stated`** — absence of any cap line stays `None`, because every proposal predating this reads that way and must not be relabelled "no cap". Deliberately **NOT in `_VERIFIABLE_FIELDS`**: it is an ABSENCE claim and most no-cap solicitations simply say nothing, so requiring a quote would flag every correct read as unverified — but when the document *does* say so outright, the model puts that sentence in `source_quotes.budget_cap_status` and the modal shows it. In the UI a `"not_stated"` field drops the `critical` "verify" tag and its warning, and says *"This solicitation states no per-award cap — leave this blank."* **Why this matters more than it looks:** on NSF 23-598 the nag told the PI to go confirm a cap against the PDF, and the only dollar figure in all 17 pages is the **$28,000,000 program-wide pool** across "30 to 35" awards — so the honest-looking empty field actively steered them toward entering a number ~35× the real award, which would then flow into the Budget Helper's cap check. Verified live 2026-08-12: `budget_cap=None`, `budget_cap_status="not_stated"`, quoted with *"While HBCU-EiR research proposals do not have budgetary restrictions…"*, `unverified_fields=[]`. A percentage limit on one cost category (equipment ≤30%) is **not** a cap and must not set `"stated"`. The modal shows a **Funding category** picker that fills the Sponsor cap — default is a blank "Select your category…" so the PI consciously picks; the picker appears only for 2+ categories. **Spreadsheet view:** `compute_budget` also returns a render-ready `table` (`{columns, rows}`; single-year = one `Amount` column, multi-year = a column per year + `Total`) built by `_budget_table` from the per-year breakdowns (now retained instead of discarded). The modal renders it as a read-only **Budget table** grid — it formats numbers but computes none, so it always matches the summary and the CSV (row order mirrors `budget_to_csv`). **Modal layout:** the Budget table's `<table>` uses class **`bh-table`** — NOT `bh-grid`, which is the form's two-column `display:grid` and silently turned the table into a broken 2-column grid (thead/tbody split, amounts clipped). The whole modal body (form + summary + table + justification) scrolls as **one** `.bh-scroll` region; the navy **Summary is `position:sticky`** so the total stays in view while the taller form scrolls (disabled on mobile). The **justification draft panel has a Close (×)** that clears `justification`+`perLine` without closing the whole modal. **v3 (multi-year coaching):** the multi-year path is otherwise unchanged but now *explains itself* to a first-time PI — `ESCALATION_GUIDANCE` tooltip (escalation hits **salaries only**, ~2-3% federal norm; exposed via `rate_options()`), per-year **salary deltas** in `multi_year.years` (`salary_delta`/`salary_delta_pct`, shown as "salary +$X (Y%)"), the cap badge spells out it's the **cumulative N-year TOTAL** (`multi_year.cap_basis="cumulative"`), `draft_justification` adds a **"Period of performance"** paragraph (figures from `multi_year`; placed *before* the final total so the endpoint truncation guard still trips), and `multi_year_advisories()` flags **0% escalation** (info) / **>10%** (warn) — appended to `advisories` at the `compute_budget` level since `_compute_single` never sees `escalation_pct`. All additive/deterministic; AI still only polishes. Out of scope (deliberately): per-person escalation rates and ramp-up/down spending profiles.
- **Budget Helper — five multi-year bugs fixed 2026-08-10** (each reproduced by running the module; two could put a wrong dollar figure into a real submission). **(1) One-time costs were multiplied by the project length** — every non-salary line was re-run verbatim per year, so a single $60,000 instrument became $180,000 in the table, the cumulative total AND the cap check, with no input able to say "Year 1 only". `one_time_categories` now defaults to `["equipment"]` (capital purchase; travel/supplies stay annual), is overridable both ways, and the resolved treatment is reported on `multi_year.one_time_categories`, in an advisory, and in the CSV — the original bug was not that equipment repeated but that **nothing said so**. **(2) `draft_justification` stated two contradictory totals** — the cumulative, then Year 1 relabelled "total project cost" two paragraphs later. **(3) `budget_to_csv` labelled Year 1 `TOTAL PROJECT COST`** while its own appendix showed the cumulative — and the CSV is the artifact most likely to be forwarded to ORA. **(4) Trim suggestions were dead for multi-year** (`cap: None` is passed into the per-year computation, so `cap_status` was always `"none"`) — i.e. the over-cap coaching never rendered for the proposals that have caps; they are now recomputed at the multi-year level. **(5) A per-year cap was compared against the cumulative total** — `solicitation_extractor` is explicitly instructed "If stated per year, return the per-year value", so "$200k/year for 3 years" reported as over a $200k cap. `cap_basis` is now an input (`"cumulative"` default, `"per_year"` compares each year and reports the WORST year's overage). **Nothing sets `cap_basis="per_year"` yet** — the extractor knows whether a cap was per-year but does not record it; the engine is fixed, threading that signal through `notes` + the modal is a separate change. **NOT a bug, do not "fix" it:** one F&A rate across all years is CORRECT — PAPPG cites 2 CFR 200 App III C.7 ("the negotiated F&A rate in effect at the time of the initial award throughout the life of the sponsored agreement"), and the awarded Morgan EiR budget confirms it (51% across 3 years). **Still open:** the `other` tooltip routes tuition remission into an F&A-bearing line though 2 CFR 200.1 excludes it from MTDC (~$16k over-recovery on $30k tuition); `SUBAWARD_MTDC_CAP` stays $25,000 pending Morgan's NICRA date (2 CFR 200.1 says $50,000, but NSF honours pre-Oct-2024 NICRAs, so the operative figure is the agreement's, not the calendar's — **ask ORA**).
- **REMOVED 2026-08-10 — the Drafting Coach.** `services/section_coach.py`, `SectionCoachModal.jsx/.css`, their five test files, and SIX endpoints (`GET /sections`, `POST /section-coach`, `POST /coherence`, `POST /responsiveness`, `GET|PUT /sections/drafts`) were deleted per product decision. **Do not re-add.** Three things survive it: the **`sections_json` column stays** (it holds real PI drafts — dropping it destroys user data, and an unused nullable column costs nothing), `has_sections` is still emitted by the serializer over that column (nothing reads it today), and **`_quote_in` was extracted to `services/text_match.py` as `quote_in`** BEFORE the delete, because the draft reviewer imports it and it is the whitespace-collapsing check golden rule 2 rests on — it now has a home that doesn't belong to any one feature. `_eligibility_text_from_notes` went with the Coach (its only caller); the eligibility TEXT is still extracted into `notes` by the solicitation pipeline. Consequence worth knowing: **`nextStep()` lost its writing step**, so no tool now helps a PI *write* — only ones that check what they wrote. (The chain is Budget → Compliance → Attach solicitation → Draft Review since Draft Critic was removed 2026-08-11.)
- **REMOVED 2026-08-11 — Draft Critic.** `services/draft_critic.py`, its four test files, the endpoint `POST /api/me/submissions/{id}/critique`, and `DraftCritiqueModal.jsx/.css` were deleted per product decision. **Do not re-add.** Draft Review replaces it and does strictly more: the same mechanical checks (page limits, required attachments, budget vs cap) now run as deterministic rows in `services/generic_checks.py` — driven by the solicitation stored on the proposal rather than by sponsor defaults — plus the per-requirement coverage the Critic never had. A stray `services/draft_critic 2.py` (an editor-duplicated copy committed by accident in `dc84bdd`, never imported) went with it. **`proposals_service.reconstruct_solicitation_context` SURVIVES with no consumer**: it is the only parser of the `notes` solicitation lines, two write paths still produce them, and its tests document that round-trip — same call as keeping `sections_json` when the Drafting Coach went. Since the Coach was removed there is no per-section writing coach either.
- **The checklist is driven by the SOLICITATION, and every task records where it came from** (`SubmissionTask.source` / `source_ref` / `source_quote`, `proposals_service.sync_solicitation_requirement_tasks`, `services/checklist_filter.py` — 2026-08-12). Before this the checklist mixed two kinds of item and styled them identically, so a hardcoded *"NSF requires a 2-page Data Management Plan"* read as something **this** solicitation had said. Three sources, and the rules differ per source:
  **THE CHECKLIST IS A STORED SNAPSHOT, NOT A RECOMPUTED VIEW — and that is why it differs between two proposals from the SAME solicitation.** Tasks are rows written once when the solicitation is attached; Draft Review recomputes everything on each run, which is why improving `delegated_rules`/`draft_scope`/`canon_section` fixes existing proposals while the checklist keeps whatever it was created with. Combined with the extraction's measured non-determinism (43 vs 47 requirements on identical input), two proposals from one solicitation legitimately get different checklists — observed 2026-08-13: 38 requirements → 14 tasks locally, 18 on a live proposal read at a different time. Re-attaching rebuilds it and ticked items survive (`source_ref`). **Known gap:** `checklist_filter` decides by the SHAPE of an ask and has no notion of WHEN the obligation lands or whether a person can act on it, so a live checklist carries *"Submit annual project report 90 days prior to the end of the budget period"* (post-award) and *"Comply with career limit on HBCU-EiR awards"* (eligibility). `draft_scope` already classifies the second correctly; the two tools disagree about the same requirement.
  **THE CHECKLIST IS A STORED SNAPSHOT, NOT A RECOMPUTED VIEW — and that is why two proposals from the SAME solicitation get different checklists.** Tasks are rows written once when the solicitation is attached. Draft Review recomputes everything on every run, which is why improving `delegated_rules` / `draft_scope` / `canon_section` retroactively fixes existing proposals **while the checklist keeps whatever it was created with**. Combined with the extraction's measured non-determinism (43 vs 47 requirements on identical input), two proposals from one solicitation legitimately diverge — observed 2026-08-13: 38 stored requirements → **14 tasks** locally, **18** on a live proposal attached at a different time. Re-attaching rebuilds it and ticked items survive (keyed by `source_ref`). **Known gap:** `checklist_filter` decides by the SHAPE of an ask and has no notion of WHEN the obligation lands or whether a person can act on it, so a live checklist carries *"Submit annual project report 90 days prior to the end of the budget period"* (post-award, before the award exists) and *"Comply with career limit on HBCU-EiR awards"* (eligibility, not a task). `draft_scope` already classifies the second correctly — **the two tools disagree about the same requirement.**
  - **`solicitation`** — one task per stored requirement, carrying the funder's own sentence in `source_quote`. **A row we cannot quote never becomes a task** (golden rule 2). Keyed by `source_ref` (the requirement id) so a re-read is idempotent and a ticked task stays ticked.
  - **`sponsor_template`** — a guess from the sponsor NAME, **retired once the real document is read**, and only while still `pending`: deleting a task the PI ticked off erases their record of having done it.
  - **`ora_process`** — Morgan workflow (internal routing form, AOR submission). In no solicitation, mandatory regardless of funder, **never touched**. A task with **no** `source` is never touched either — that is every task predating the column, plus anything the PI added by hand.
  Wired at BOTH entry points (`/from-solicitation/confirm` at create, `PUT .../solicitation` when attaching later), so it fires whichever way the PI got there. **Not every requirement earns a checkbox:** `checklist_filter.is_checklist_task` keeps only the asks a tick-box can help with — measured on the human-verified NSF 23-598 list that is **24 requirements → 7 tasks**, and none of the thirteen Project Description rows survives, because those are content *inside* a section that Draft Review checks against the draft itself (a checkbox there could only invite the PI to assert they had covered it). **The stored profile keeps all of them; this filters the checklist, not the review.** Known limit, documented rather than papered over: **requirement ids are not stable across two reads of one document** (see the `solicitation_requirements` section), so status is preserved *within* a profile, not across a fresh extraction. Tests: `tests/test_checklist_provenance.py`, `tests/test_checklist_filter.py`.
- **Guided steps** — `task_guidance.py`: keyword-matched how-to + sample per checklist task, attached in `_submission_task_to_dict`; per-task "add to calendar".
- **Guided Proposal Pathway** (the proposal detail view in `MyProposals.jsx`, presentation/orchestration — **no new tool logic**) — for inexperienced PIs, the flat toolbar is a **staged path**: Set up → Build → Review & submit, each tool carrying a ✓ status badge from serializer flags (`has_budget`/`has_compliance`/`has_solicitation_requirements`; the Solicitation badge reads **"attached"** only when the requirements are stored, **"rules only"** when `notes` carry the funder's numbers but no profile — those are different states and showing both as "attached" put two contradicting badges on one screen). A pure `nextStep(submission)` picks the single recommended action — only that button gets the accent (the rest share one calm style) — and a plain-language **"What to do next"** card explains the current step + why (active proposals only). Delete + Add-to-Calendar live in a `⋯` overflow. The summary shows the **internal ORA routing deadline** (`proposals_service.internal_routing_deadline` → `internal_deadline` field, **5 business days before** the sponsor date, weekends skipped) so a first-timer plans backward from the real institutional cutoff.
- **Compliance Sentinel** (`compliance_sentinel.py`) — IRB/IACUC/COI/RCR/export rules. **Fully deterministic — no AI anywhere in it**, not even the explanations (golden rule 1 in its purest form): five rules, each a small `(answers, sponsor) -> (status, doc_id)` function, fed by a 5-question yes/no questionnaire plus the proposal's `sponsor` string. Only `{"answers": …}` is persisted to `compliance_json`; **the verdict is recomputed on every load**, so improving a rule retroactively fixes every existing proposal — keep it that way.
  - **Sponsor matching is on WORD BOUNDARIES and knows both spellings, and both halves of that are load-bearing (fixed 2026-08-12).** A bare substring test failed in *both* directions: `"Maryland Technology Transfer Fund"` contains `nsf` (tra-**nsf**-er) and picked up NSF's RCR mandate, while `"National Institutes of Health"` matched nothing and was told a federal FCOI disclosure was **not required**. Proposals created from a solicitation are canonicalized to a token by `solicitation_extractor._canon_sponsor`, but **`create_submission` — the by-hand path — stores whatever the PI typed**, so the spelled-out agency name is the common case, not an edge case. Fixed in the matcher rather than at write time, because canonicalizing on write would not touch rows already in the database.
  - **`not_required` is never asserted from ignorance.** RCR used to clear every sponsor that is not NSF/NIH — *"this sponsor does not mandate RCR training"* for DOE, DOD, NASA, USDA and every foundation. NSF/NIH is the mandate we can **confirm**, not the set of sponsors that have one (the CHIPS and Science Act §10634 directs *all* federal research agencies to require RCR), so unrecognized now resolves to **`review`**. **`"Internal"` stays `not_required`** — it is the app's own token for *no external funder* (`_INTERNAL_SPONSORS`), and a federal mandate flows from a federal award. Same error class as `could_not_locate` ≠ `not_found` and "an unreadable page is not a deleted one".
  - **A rule with two triggers may not be cleared on one answer**, and the explanation must never assert input the PI never gave. The export rule cleared on *either* trigger being answered, so a single "no" both resolved a rule while half its input was unknown **and** printed *"you indicated no export-controlled technology AND no foreign collaboration"*. Now requires both. Because `review` became reachable two ways that mean different things, **a rule may return an optional THIRD element naming the `why` key** (`review_foreign` reports what the PI declared; `review_unanswered` reports that we do not know yet) — two-element returns are unchanged, so the other four rules are untouched.
  - **Still open, deliberately not done:** compliance tasks carry **no due date** though the rule table knows the lead times ("allow 4–8 weeks for IACUC") and the app computes `internal_routing_deadline` — backing one off the other is pure deterministic arithmetic; `add_compliance_tasks` dedupes by **title string**, so rewording any `task_title` silently duplicates the task on every existing proposal (the checklist solved this with `source_ref`); and `compliance_json` stores bare answers with **no timestamp**, so "when did the PI attest to no human subjects?" is unanswerable.
  **Deadline Watcher** (`deadline_watcher.py`) — AI-personalized reminder emails (cron). `ics_export.py` — calendar feeds.

- **Draft Review** (`services/draft_review.py` + `services/solicitation_profile.py` + `services/generic_checks.py` + `services/solicitation_requirements.py`, `DraftReviewModal.jsx`) — the PI pastes or uploads their **whole package** and gets completeness against **that proposal's own solicitation**, whichever funder it came from. Endpoints **`POST /api/me/submissions/{id}/draft-review`** and **`/draft-review/upload`**. The button appears on **every** proposal. **Stateless — the paste is never persisted** (it's the PI's unpublished manuscript). Four stages, unchanged: **locate** (model finds section boundaries; every marker is verified present in the paste before it's accepted) → **deterministic** (code-only yes/no rules) → **semantic** (model assigns coverage, must quote) → **score** (arithmetic in code).
  - **NSF 23-598 IS NO LONGER SPECIAL-CASED, ANYWHERE (2026-08-11).** It used to be the only solicitation this worked for: ~25 requirements a human read out of the PDF once and typed into `eir_solicitation.py`, behind an `isEirProposal()` title regex that hid the tool on 4 of 5 proposals. Deleted: `services/eir_review.py`, `services/eir_checks`-worth of eight NSF-specific check functions, the two `/eir-review` routes, `isEirProposal()`. **Do not re-add a funder branch.** `services/solicitation_profile.py` is the shape the engine is handed; `draft_review.review_draft(text, profile=…)` is the only entry point. **A grep gate belongs in any change here — but not the one this file used to state.** `grep -ril "eir_solicitation\|23-598" backend/services/` is **not** that gate: it already matched **5 files** before the rulebook-baseline branch even started (`checklist_filter.py`, `compliance_sentinel.py`, `delegated_rules.py`, `proposals_service.py`, `solicitation_extractor.py`), and all **9** hits across them are docstrings citing NSF 23-598 as **measured evidence** ("measured on NSF 23-598", "the human-verified list") — not one is a conditional. The literal grep would either block a correct change or force deleting the repo's most valuable measurement notes. The gate that tests the actual intent, in two parts: **(a)** `grep -rnE "\b(isEir|eir_solicitation|eir_review|eir_checks)\b" backend/services/ frontend/src/` — the deleted modules' own identifiers — must come back empty; **(b)** no funder-name conditional in the review engine specifically (`draft_review.py`, `solicitation_profile.py`, `generic_checks.py`, `rulebook_baseline.py`, `rulebook_checks.py`, `delegated_rules.py`, `draft_scope.py`). **`proposal_templates.py:203`'s `if key == "NSF" and _is_education_program(...)` is CORRECT and out of scope** — that module's whole job is per-sponsor checklist templates, and reading the no-funder-branch rule repo-wide would condemn it for doing its job. (Also do **not** grep bare `hbcu` — it legitimately appears in `opportunity_finder.py` and `sample_proposals.py`.)
  - **`tests/fixtures/nsf_23_598.py` is TEST DATA THAT LOOKS LIKE DEAD CODE. Do not delete it.** No service imports it. It is the only human-verified requirement list in the repo and therefore the only yardstick for whether generic extraction is any good.
  - **What was lost with the eight deterministic checks, deliberately.** The 30% equipment cap, the 2-page institutional letter, the mandated single-sentence collaboration letter, the ban on collaborator support letters, the DC grantee-meeting travel and the `Excellence in Research:` title prefix were arithmetic in code. They now reach a PI only if the extractor pulls them out of the PDF as quoted semantic rows. **Measured on the live NSF 23-598 page: it does** — all six came back quoted, in a run that found 43 requirements with 0 dropped as unquotable.
  - **The requirement universe is fixed by what was STORED, and the model can only assign coverage to it.** A row with an unknown id is dropped; a row the model *omits* becomes `unclear`, not `not_found` — the model skipping something is a fact about the model, not the draft. What changed is where the universe comes from: extraction + PI confirmation, rather than a hand-typed list. Every row carries a verbatim solicitation quote, and the modal shows the whole list, because that is the only real check on a model-derived set.
  - **ONE FACT, ONE PLACE — the delegation UI was saying the same thing FOUR times (2026-08-13).** Built in one sitting, each piece defensible alone: a notice above the findings, a line on every section heading (*"the PAPPG sets further rules for this section…"*), a `defers to the PAPPG` tag on each row, and a *"Heads up: this requirement also defers to…"* sentence appended to each rider's note. On a real review that put the identical caveat on screen four times and **buried the reviewer's actual feedback underneath it** — the PI's words were "it's completely crowding the draft review". Removed: the per-section line and the appended note. Kept: the notice (compressed to one line per rulebook) and the tag. **The rule to hold: a caveat belongs in exactly one place, and a per-row note must carry what the reviewer said about THAT row, nothing else.** Same failure shape as the copy that had to be rewritten twice above — when a screen explains itself repeatedly it stops being read at all.
  - **`_RULEBOOK_SYSTEM` FAILED its main goal, measured head-to-head on PAPPG Chapter II (2026-08-13). Do not assume a prompt fixes this.** Against the solicitation prompt: rows **598 → 380** (-36%), time **245s → 170s**, unquotable drops 80 → 33. But the thing it was written for — cutting submission-mechanics noise — **did not move: 25% → 26%**. And it made the target case WORSE: the Project Summary went from four usable rows (`Include Overview` / `Include Intellectual Merit statement` / `Include Broader Impacts statement` / one-page limit) to **two, one of them the merged and useless "Include required distinct components in Project Summary"** — losing exactly the granularity that would catch a summary missing its Intellectual Merit statement, which is the entire reason to read the PAPPG. Its own rule 5 says split, never merge; the model did not obey. **The deterministic filter is the better instrument but is not enough either**: `draft_scope.is_draft_checkable` over the raw extraction removes cover-sheet checkboxes, AOR signing and preaward disclosures — the right rows — but only **11-12%**, leaving **527 requirements and 97 sections** against a solicitation's 38 and ~10. (The earlier "25% noise" figure came from a loose keyword probe matching `submit|certif|reviewer|renewal|supplement` anywhere and over-counted; 11% is the real not-draft-checkable share.) **The remaining bulk is proposal-TYPE variants** — GOALI, RAPID, EAGER, Antarctic, renewals, supplements — which do not apply to a standard research proposal at all. So the next lever is **SECTION SCOPING of the source text**, not prompt tuning and not filtering the output: extract only the subsections a standard proposal needs. Ignoring that and shipping either extraction would take a review from 44 findings to ~640 and bury the four rows that matter. **DONE 2026-08-17 and it worked — 161 rules, 0 invented sections, see `kb_pappg/slicer.py` under Known limitations.** `_RULEBOOK_SYSTEM` and `_RULEBOOK_SWEEP_SYSTEM` are still the prompts used, unchanged; what fixed the noise was slicing the source so the section is an INPUT, not the prompt.
  - **PAPPG Chapter II IS extractable, and a naive extraction is NOT usable — measured 2026-08-13.** Fetched `nsf.gov/policies/pappg/24-1/ch-2-proposal-preparation` (**219,957 chars, 4 chunks**, 4x a typical solicitation) and ran the ordinary solicitation pipeline over it: **598 requirements, 8 rounds, 245s, hit the round cap, 80 dropped unquotable (13%)**. The rules we came for are there and correctly quoted — *"The Project Summary consists of an overview, a statement on the intellectual merit … and a statement on the broader impacts"*, *"not more than one page in length"* — which is exactly what would fail a five-line Project Summary. But: **25% of rows are submission mechanics / post-award / review process** ("Use AOR function to sign and submit", "Omit page numbers on uploaded files"), it invented **105 distinct sections** (`special_processing_instruction`, `e_special_processing_instruction`, `goali`, `antarctic` — the doc-heading leak at scale, and each section costs the reviewer its own Gemini call), and the same rule returns three times in different words. Hence `_RULEBOOK_SYSTEM` + `_RULEBOOK_SWEEP_SYSTEM` (`solicitation_requirements`), a reader scoped to what constrains the proposal DOCUMENT, with the four out-of-scope families named explicitly and conditional rules kept as `scored: false`. **Both prompts must move together** — leaving the sweep on the solicitation prompt lets round two re-import everything round one was told to skip. **Not yet wired into a review**: dedup, section discipline and per-proposal-type conditionality all need solving first, and the comparison run under the new prompt is the next measurement.
  - **REQUIREMENTS A DRAFT CANNOT SATISFY (`services/draft_scope.py`, status `not_in_draft`, 2026-08-13).** Measured on a real proposal: **7 of 29 assessed requirements were things no document can contain** — "Select secondary unit of consideration in Research.gov", "Ensure the LOI is submitted by an Authorized Organizational Representative", "Limit career total awards per PI". Every one scored `not_found`, counted against the draft, and sat in "Fix these first"; a PI cannot fix a portal click by editing their Project Description. They now become `not_in_draft` — absent from `_CREDIT`, so out of the denominator — and their note names the tool that DOES handle them: the checklist, where `checklist_filter` already routes them. Effect on that proposal: score **22% → 27%**, assessed **29 → 24**, fix-list **23 → 18**. Deterministic (named submission systems, named submitting officers, registrations, per-PERSON limits), applied at review time like `delegated_rules` so stored profiles are fixed retroactively, and **content wording always wins** — "the budget justification uploaded to Research.gov **must explain** each cost" stays a content rule. **The risk is OVER-excluding**: a real content requirement dropped here would never be learned about, so an unrecognised row stays scored. Two of the original seven correctly stayed in ("Designate secondary unit of consideration", "List the PI as point of contact on the LOI") — an LOI document really does contain those lines. Tests: `tests/test_draft_scope.py`.
  - **A SHARED CONTRACT QUOTE MUST NOT BE STAMPED ON EVERY ROW (`generic_checks._quote_if_it_names`, 2026-08-13).** `source_quotes` carries ONE entry for `required_attachments` and one for `page_limits`, and it was copied onto every row built from them — so "Project Summary included" was justified by *"The proposal must include a letter by the chair, dean, or chief academic officer"*, and "Why is this required?" showed the same wrong sentence under all five attachment rows. **Worse than no quote**: golden rule 2 exists so a claim can be checked against the funder's words, and a quote that does not support its row looks like evidence while being none. The row's name must now appear in the quote verbatim, else an honest derived line. **It fails safe and that costs something** — the institutional-support letter's own quote never uses the phrase "Letter of Institutional Support", so that row gets the derived line too. Losing a correct quote costs context; keeping a wrong one is a grounding violation, and only the first is recoverable by the PI reading their solicitation. Found by a user, not a test.
  - **SAVED REVIEWS (`Submission.draft_review_json`, 2026-08-13).** `POST|GET|DELETE .../draft-review/saved` plus a client-side Markdown download. **Explicit action only** — the review is otherwise stateless because the paste is an unpublished manuscript — and what it stores is the RESULT, which **does carry evidence quotes from that draft**; the draft text itself is still never stored. Deliberate: one saved review per proposal (most recent, no history table); the PI can DELETE it without deleting the proposal, because they stored fragments of their own manuscript and get to take that back; and **only the timestamp rides on the submission list** (`draft_review_saved_at`) because `list_submissions` loads whole rows — shipping a 40KB review with every proposal is the mistake that kept the solicitation TEXT off this table. Verified live: 44 findings saved and reopened identically, list row **1,255 bytes**. Self-healing migration, ran clean on prod MySQL. Tests: `tests/test_saved_draft_review.py`.
  - **MECHANICAL MISTAKES (`services/mechanical_checks.py`, 2026-08-13) — errors, deliberately NOT a quality grade.** Asked "how do we rate how well written it is", the answer this app can defend is: **it cannot**. There is nothing to calibrate a quality score against — `sample_proposals` holds **22 FUNDED proposals and zero declined ones**, so no signal exists anywhere in the system for what separates a funded draft from a rejected one, and a number would be the model's opinion presented as a measurement. (A reviewer-lens scoring tool existed and was deleted by product decision; the completeness score already has to be captioned defensively because a percentage next to a proposal reads as "odds of funding".) **A MISTAKE needs no calibration** — the placeholder is in the text or it is not. So this reports errors, each **quoting the words it found**, and is the first deterministic layer beyond the three checks that existed (`page_limit`, `attachment_present`, `budget_cap`); nothing had ever looked for an actual error. Five rules: unfilled placeholders (`TBD`, `[insert name]`, literal `Figure X`), figure/table references with no caption, paragraphs pasted twice, a narrative total contradicting the SAVED budget (the app held both numbers and had never compared them), and citations with no reference list. Returned as `result["mistakes"]`, **outside `findings` and outside the score** — a leftover "TBD" is not incompleteness against the solicitation, and a number that is already over-read must not absorb a second kind of problem. **FALSE POSITIVES ARE THE RISK, and every guard has its own test**: `[12]` is a citation not a placeholder; a draft with **no captions at all** is never flagged for a dangling figure reference (pasted text loses captions routinely, so absent captions mean the paste dropped them — without this guard the feature is pure noise); short repeated lines are headings, not duplicated paragraphs; a dollar figure is compared only when the text calls it a total AND a budget was actually saved (with no budget there is nothing to conflict with, and picking a figure to call "the" total would be a fabricated error); citation markers need a threshold of 3 because "(2019)" appears in ordinary prose. Verified live: a clean draft reports nothing, a deliberately messy one reports all five with quotes. Tests: `tests/test_mechanical_checks.py`. **LANGUAGE SLIPS — five rules, no dictionary, added 2026-08-20 after a PI asked whether the wording is checked at all.** It was not: nine errors planted in a Project Summary (`zwiterionic`, `The objectives is`, `the the`, `Chesepeake`, a missing final period, `The work advance`, `models fails`, `rather then`, `Univeristy`, `Departement`) returned an **unchanged 7 of 7 (100%) with 0 mistakes**. Nothing in the app had ever looked at language, and grepping for it finds only the word *"spellings"* in comments about section names. **THE DICTIONARY IS THE TRAP, and refusing one is the design.** A proposal's correct vocabulary — `zwitterionic`, `Donnan`, `estuarine`, `potentiostat`, `MTDC`, `PSU`, every reagent and instrument name — is exactly what a dictionary lacks, and a checker that yells at correct science is ignored by the second page, taking the real errors with it. That is this module's own recorded failure (`TO DO` matching *"allow us to do much more work"*). So: doubled words, never-valid spellings (a curated ~35-entry table; British/American pairs deliberately ABSENT), confused two-word phrases (`rather then`), spacing (`word.Word`, `word ,`), and a prose paragraph with no terminal punctuation. **Measured: 5 of the 9 caught, 0 false positives** on the clean original AND on a References Cited section (bibliography entries end on page ranges, and it carries a DOI). **What it CANNOT catch, and no tuning here will:** a one-off typo that is not a known misspelling (`Chesepeake`, `zwiterionic`) and grammar (`The objectives is`). Both need a dictionary or a parser. **Say so rather than implying the section was proofread.** SIX false-positive guards, and **each was mutation-tested — two of them turned out to guard nothing under the tests as first written and needed new cases before they would bite**: `had had`/`that that` are ordinary English; NSF's own headings (`Overview`) carry no period by design and would otherwise fire on every correctly formatted Project Summary; list items and bibliography entries legitimately end without one; the `(?<=[a-z]{2})` lookbehind keeps outline numbering (`II.Background`, `1.Introduction`) out, at the accepted cost of missing a real `Ph.D.Chemistry`; and a URL guard keeps `www.Morgan.edu` out. Two output bugs fixed on the way, both found by reading real output rather than by a test: the no-period row quoted the **entire 445-character paragraph** instead of its tail, and `Univeristy` was corrected to lower-case *"university"*, which reads as a second mistake in a tool about spelling. These rows join `mistakes` — **outside the score**, same as the other five checks. Tests: `tests/test_language_slips.py` (16). **AND THE DETERMINISTIC CEILING WAS HIT WITHIN THE HOUR (`services/proofread.py`, 2026-08-20).** The PI pasted a Project Summary with three more errors and got **0 rows**: `fourr` (a one-off typo on no list), `trains fourr undergraduates, and two graduate students` (a spurious comma), and a sentence ending `students...` (the last character is a period, so every punctuation rule passes). **Three errors, three different causes, none reachable by regex** — a typo that is not a known misspelling needs a dictionary, a comma needs a parser. So a model pass, and it is **the one job in this app where the model is asked about ENGLISH rather than about a funder's rules** — a question it is actually authoritative on, unlike "is this proposal good". **FENCED FOUR WAYS, and the first is the one to keep:** its own `wording` key, **NOT `mistakes`** — that list is model-free by contract and the modal captions it *"found by a rule, not a judgement"*, which model rows would make false; never scored (a comma splice is not incompleteness against a solicitation); **every row must quote the draft**, verified in code with the same `quote_in` (golden rule 2) — a proofreader that invents the sentence it is correcting sends an author hunting for text that was never there; and **errors only, never style** — the prompt bans tone, readability, rewrites and "consider rephrasing", which is the line between this and the **Drafting Coach deleted by product decision**. `use_ai=False` suppresses it, tested, so this does not become a second instance of the `review_draft` gap this file already records open. **Measured live: 2 of the PI's 3 caught** (`fourr` and the trailing ellipsis; the comma was not reported and is arguably defensible), **stable across 3 runs**, and — the number that decides whether anyone keeps it on — **0 false positives across four clean drafts**, including References Cited (author names, DOIs) and a Project Description dense with `zwitterionic`, `Donnan`, `estuarine`, `potentiostat`. That is what the prompt's LEAVE TECHNICAL VOCABULARY ALONE section buys. ~5-7s. **Section Check only** — `review_draft` deliberately does NOT proofread, because a whole package is many thousands of words and that is a different feature with a different cost, not this one scaled up. **NOT deterministic and the UI says so** (*"a second reader, not a rule"*), which is affordable precisely because these rows change no number. Caught in review: `quote_in(text, quote)` had its arguments **backwards** on the first write — the grounding check silently rejected every real quote, and the test that watched it fail is the only reason it did not ship. **The comma it missed was a PROMPT gap, not a grounding drop, and the difference was measured rather than assumed (2026-08-20).** The PI's *"trains fourr undergraduates, and two graduate students"* went unreported three runs running. **Instrumenting the RAW response before the quote gate is what settled it** — the model returned exactly 2 issues each time and nothing was being dropped, so `quote_in` was never involved. A model reliably skips a discretionary comma unless the shape is NAMED, and nothing deterministic can see one. **A/B measured over 3 runs each before shipping:** the user's text went **2 rows -> 3**, four clean drafts stayed at **0**, and a fifth gained one row that turned out to be a **REAL missing comma after an introductory clause in a draft this repo had written** (*"Should both be awarded the PI will reduce R21 effort"*) — a find, not a false positive, and the reason to check what a new flag IS before calling it noise. Both shapes are now named in `_SYSTEM`. **The generalisable bit: when a model-backed check misses something, instrument the raw response first.** "The model did not report it" and "our gate dropped it" look identical from the outside and have opposite fixes. Tests: `tests/test_proofread.py` (29 as of 2026-08-31, one SKIPPED on purpose — it asserts the vote threshold that was tried and reverted, kept as the record of what was measured. The file grew the artifact filters, the `word_choice` drop and the voting; see the 2026-08-31 entry under Draft Review). **Do not fold these into the score, and do not turn them into a grade.** **Shipped with a false positive and a user found it**: `\bTO\s?DO\b` compiled case-INSENSITIVE matched ordinary English — *"would allow us **to do** much more work"* was reported as unfilled template text. Placeholder detection is now split by case: case-insensitive for forms that are never prose (`TBD`, one-word `TODO`, `lorem ipsum`, `[insert name]`), case-SENSITIVE for `TO DO`, `XXX` and literal `Figure X`, whose lower-case forms are ordinary writing. This is the exact failure the module's own docstring warns about, shipped anyway — the guards need tests, not prose.
  - **DELEGATED REQUIREMENTS — a rule the solicitation hands to a rulebook we never read (`services/delegated_rules.py`, 2026-08-13).** Found by a PI, not a test: a **five-line Project Summary with no Intellectual Merit or Broader Impacts statement was reported "Addressed"**. It was not judged and approved — it was never given the rule. NSF 23-598 states exactly ONE Project Summary rule, *"must include the LOI number **in addition to all the requirements outlined in the PAPPG**"*, so the whole of what could be checked was (a) does a section by that name exist and (b) is the LOI number in it. The rules that would actually fail that summary live in the PAPPG. `solicitation_requirements._SYSTEM` rule 6 is why the row exists at all ("return the pointer itself as a requirement, quoted. Never invent what the referenced document says") — correct, but it puts unassessable rows in the list rendered exactly like assessed ones. **Deterministic, no model** (golden rule 1): the funder's own sentence is matched against a table of named rulebooks (PAPPG, NIH GPS, 2 CFR 200, Uniform Guidance, Grants.gov Applicant Guide, SF424, BABA, FAR). **Applied in `draft_review.apply_delegation`, NOT stored on the requirement** — same reason `compliance_sentinel` recomputes verdicts on every load: improving the detector retroactively fixes every proposal already in the database, including profiles extracted before this existed. **TWO shapes, and conflating them deletes a true finding:** a **POINTER-ONLY** row ("Adhere to PAPPG guidelines" — a compliance verb *plus* a named rulebook) becomes status **`delegated`**, absent from `_CREDIT` and so out of the score's denominator, beside `could_not_locate`; a **RIDER** (a concrete checkable ask that also cites a rulebook) **keeps the status it earned** — the LOI-number row IS checkable, WAS checked and came back `not_found`, and demoting it to make room for a caveat would erase a real fact about the draft. A compliance verb with **no** rulebook is not delegated ("Comply with the 30% equipment cap" is ours to check). **Fails safe both ways:** an unrecognised rulebook leaves the row exactly as before, and a missed pointer-only row stays scored rather than vanishing. UI: a `defers to X` tag on every delegated row, a **`Not ours to check`** chip (its own word — "Not checked" reads as our omission that a re-run might fix; this one never will), a notice under the score, and a per-section line, because the global caveat is not what the PI is reading when they look at Project Summary. Verified live on the real proposal: the LOI row kept `Not found` + tag, the Build America Buy America row became `Not ours to check` and left the denominator, and the Project Summary group carries *"the PAPPG sets further rules for this section that were not checked here."* Tests: `tests/test_delegated_rules.py`. **Open:** nothing reads the PAPPG itself — ingesting its proposal-preparation rules as a second profile layer is the real fix for an NSF-heavy office, and is a much bigger change.
  - **The solicitation's own headings are not sections of a proposal, and `canon_section` now says so (2026-08-13).** `_SYSTEM` rule 9 forbids them and the model mostly obeys, but a minority leaked every run — observed live: `vii_award_administration_information`, `preparation_instruction`, `budgetary_information`. **Two artefacts in that one string**: `vii` is the FOA's section NUMBER (it survived because the non-alphanumeric scrub removes the period *before* any numbering regex could see it, so it arrives as the plain word "vii" — stripped as a leading TOKEN now), and "Award Administration Information" is the FOA's own heading (matched against `_DOCUMENT_HEADINGS` **after** normalising, so one entry covers every phrasing). **Why a false positive here is tolerable and the status quo was not:** a section naming no real part of a proposal can never be located in a draft, so every requirement under it reported "Not located" and **dropped out of the score's denominator, silently unchecked**; resolving to `None` files the row at whole-document scope where `draft_review` assesses it against the Project Description span or the whole paste. The wrong outcome is a **wider search, not a lost requirement** — which is exactly why the deny-list was judged too risky before that asymmetry was understood. Deliberately ABSENT from it: `introduction`, `background`, `results from prior support` — a PI's own Project Description routinely opens with those. **Applied RETROACTIVELY**: `load_solicitation_profile` re-canonicalises stored section keys on load (same principle as compliance_sentinel's verdicts), so every profile already in the database is repaired without re-reading its solicitation — verified live, the two artefact sections vanished from an existing proposal and `assessed` went **27 → 29**, i.e. two requirements that were being dropped are now checked. `id` is left alone on purpose: it is the checklist's `source_ref`, and renumbering would orphan a task the PI has ticked off.
  - **One part of a proposal, named two ways, became two sections — and declared a present attachment MISSING (2026-08-13).** `sections_from` assembles the section universe from three sources with **different vocabulary**: requirement rows carry a canonicalised key (`canon_section` strips filler, so `letter_intent`), while required attachments arrive **verbatim** from the solicitation (`Letter of Intent`, `Budget and Budget Justification`). Those produced separate keys, so one Budget Justification was two sections and one Letter of Intent was two. Spotted as cosmetic noise in the section map; it is not. `_check_attachment_present` looked its span up by the **verbatim** key, missed (the PI wrote the short heading), then its heading fallback searched for the LONG name as a whole line and its loose fallback as a phrase — both miss — so a Budget Justification **sitting in the draft** was reported *"No Budget and Budget Justification found"*. A required attachment declared missing is the compliance rejection this tool exists to prevent, invented by the tool. Worse for `letter_intent`: its only alias was *"letter intent"*, a heading **nobody writes**, so its seven LOI requirements sat under a section that could never be located while the alias that would have matched lived on the other key. Fixed by merging on **`section_signature`** — the SET of meaning-carrying words (filler stripped, singularised), so `Budget and Budget Justification` → `{budget, justification}` collapses the repeat and `Letter of Intent` → `{letter, intent}`. **Set EQUALITY, never containment, and that is the safety property**: containment would fold `Project Description Supplementary Documents` into `Project Description` and lose a real section (guarded by a test). The merge keeps the key already in use (requirement rows point at it), **widens the aliases with both headings**, and adopts the fuller authored label. `resolve_section_key` is public so `generic_checks` resolves an attachment through the section universe rather than the raw name. `_SECTION_FILLER` is **duplicated** in `solicitation_profile` rather than imported — that module is data-only and the import would drag `gemini_client` in — with `test_the_two_filler_sets_have_not_drifted` guarding the copy. Verified live: `Budget and Budget Justification (26w)` as ONE located section from a draft whose heading reads "Budget Justification", and its attachment check `not_found` → **addressed**.
  - **The section map is a LOCATE result and must never be styled as a verdict (2026-08-13).** "What the reviewer found in your paste" rendered each located section as a pill with a **✓ in `--eir-ok`, the same tick and the same green as an Addressed finding** — so an 85-word Project Summary sat under a green check and read as approved. All that is ever measured there is that a heading by that name exists in the paste. Now: neutral ink, a `FileText` icon instead of a tick, the heading reads *"Where your sections were found"*, and a lede says *"Finding a section is not a judgement of what it says — the checks below are."* The **word count is promoted** (`85 words`, not `85w`, bolder) because it is the one honest signal about size and a PI can judge it themselves — the tool cannot state a minimum the solicitation never sets. Same failure family as the delegated rules above and as `could_not_locate` ≠ `not_found`: **presence rendered as approval**. Worth watching for elsewhere — any green ✓ in this modal should be traceable to something actually assessed.
  - **`could_not_locate` is NOT `not_found`, and conflating them is the whole risk of the one-box design.** A section whose marker can't be verified is reported unlocated and **dropped from the score's denominator**. Telling a PI their sustainability plan is missing when we merely failed to find the Project Description would send them rewriting something they already wrote — same class of error as the scraper treating an unreadable page as a deleted one.
  - **The score is `completeness against this solicitation`, computed by code from coverage counts — never model-assigned, and never a funding prediction.** Deliberately a *different thing* from the deleted Fundability tool (reviewer-lens quality + go/no-go); the caption spelling out the basis is load-bearing UI, not decoration. **Withheld entirely (`score: None`) when the AI layer is down** — a percentage computed with the semantic half missing reads as a verdict on the draft when it's a verdict on our availability.
  - **No solicitation → 409, never a review.** `load_solicitation_profile` returns `None` rather than an empty profile precisely so this cannot be fudged: the engine would happily score a draft against zero requirements and return a confident number meaning nothing. Deliberately NOT falling back to a thin profile from `reconstruct_solicitation_context` (cap + page limits + attachments) for the same reason.
  - **Missing required attachments COUNT against the score** (product decision, 2026-08-11) — a missing attachment is a compliance rejection, so the completeness tool must not quietly skip the thing that most often kills a submission. The modal says to paste or upload the **whole package**; the upload path takes 12 files.
  - **`not_found` must mean ABSENT, not THIN.** The first prompt said "be strict" and the model duly returned `not_found` for *"improves research opportunities for students"* on a draft that said "four undergraduates per year will be trained". The review system prompt's rule 4 defines the three statuses as presence-only with an explicit floor: any sentence speaking to a requirement is `partial` at minimum. Measured effect on one draft: 61% → 71%, and the 71% was right.
  - **Broader Impacts is a labeled sub-section INSIDE the Project Description**, so its heading must act as a locate boundary (that heading existing *is* the "separately labeled" requirement) **without** hiding its text from the Project Description's own requirements. `_project_description_span()` folds it back in, now guarded so a solicitation with neither section is unaffected.
  - **Conditional asks are `scored: False`** ("if available and appropriate", "if applicable", "optional", "may"). Counting those against a compliant proposal would be wrong; they still appear as advisory rows.
  - **The section fan-out is capped at 6 workers** (`draft_review.py`). It was `len(jobs) + 1`, which was fine when the section list was hand-written and ~6 long. An EXTRACTED profile can name 25 sections, and one Gemini call per section, unbounded, is a 429 storm inside a 300s request cap.
  - **Model is `gemini-3.6-flash` on `location="global"`** (`EIR_REVIEW_MODEL` / `EIR_REVIEW_LOCATION` — names kept so the deployed env vars still bind) — it **404s in us-central1**, the same region trap as the scraper's adjudicator. Verified live in `infra-vertex-494621-v1`: 2.5-flash/2.5-pro answer in both regions, 3.6-flash **global only**, and there is **no Gemini 3 Pro** in this project.
  - **REMOVED with the hardcoding:** the LOI / full-proposal `cycle` dates (`loi_deadline` / `full_proposal_deadline`, computed from NSF 23-598's recurrence rules). No other solicitation states deadlines as rules, and the proposal's own `deadline` column carries what a PI needs. The functions survive in the fixture.
  - **THE PAPPG RULEBOOK BASELINE — the rules a solicitation defers to but never states (`services/rulebook_baseline.py` + `services/rulebook_checks.py`, 2026-08-17).** `delegated_rules` (above) already explains the failure this closes: NSF 23-598 states exactly one Project Summary rule — *"must include the LOI number in addition to all the requirements outlined in the PAPPG"* — so a five-line Project Summary with no Intellectual Merit or Broader Impacts statement came back **Addressed**. Not judged and passed — never given the rule at all. **The finding that changes the answer:** Research.gov's proposal-preparation UI publishes the PAPPG already distilled, one screen per section. Captured live 2026-08-17 from a real Morgan submission (#329981, NSF 23-601), the Project Summary page reads, verbatim: *"Your file must include three separate section headers: Overview, Intellectual Merit, and Broader Impacts. To be valid, a heading must be on its own line with no other text on that line."* Plus a one-page limit. Four sections — Project Summary, Project Description, References Cited, Facilities/Equipment/Other Resources — cover **14 rules** (7 deterministic, 7 semantic), against the **598 requirements across 97 invented sections** three earlier attempts got from reading PAPPG Chapter II directly (documented above, and in Known limitations). **THESE 14 ARE NOW THE CURATED HALF OF 156** — reading the PAPPG itself finally worked (slice the source, see Known limitations) and added 142 reviewed rows across nine sections; the paragraph below, "THE PAPPG RULES ARE IN", is the current state. The 14 stay separate and authoritative: they carry every deterministic check, and where the PAPPG restates one the extracted row is DROPPED rather than shipped beside it. **Hand-curated is FORCED, not lazy:** every Research.gov screenshot carries `Welcome <name> | Sign Out` — the pages are behind NSF login, `kb_scraper` can never reach them, and no refresh path exists to design for. Same shape as every other authoritative table in this repo: `compliance_sentinel`'s five rules, `budget_helper`'s F&A rates, `forms_catalog`. **Keyed on the rulebook, never the funder:** `RULES["the PAPPG"]` in `rulebook_baseline.py` is data, keyed on the rulebook name `delegated_rules` already knows — `RULES["the NIH Grants Policy Statement"]` slots in later with zero engine change. **The trigger is the solicitation's own CITATION of the rulebook** (`delegated_rules.rulebooks_in`, already shipped), not the sponsor string, matched on **`\bPAPPG\b`** — a word-boundary regex, which is what structurally prevents the "Maryland Technology **Tra-nsf-er** Fund" substring bug this file already records biting `compliance_sentinel`. **Retroactive:** wired in `proposals_service.load_solicitation_profile`, which rebuilds the profile on **every** load — so every NSF proposal already in the database gained these rules the moment this shipped, no re-extraction, no migration, the same pattern `canon_section` and `compliance_sentinel` already use for their own retroactive repairs. **Page rules report an ESTIMATE from a paste and never a verdict.** Pages are a PDF property; only the section-check upload path has a real count (one file IS one section, and `pdfplumber`/`read_pdf()` already reports it). Presenting a word-count estimate as pass/fail would be the same error class as the scraper treating an unreadable page as a deleted one. **Scores moved on every NSF proposal already in the database** — the new rows entered the completeness score's denominator, so a fresh Draft Review run on an existing NSF proposal will not reproduce its old percentage. That's correct (the old number was computed with these rules missing), but it will surprise someone. A **saved** review (`Submission.draft_review_json`) keeps its stored numbers untouched — only a fresh run differs. **False-positive guards, each with its own test:** an email address is not a hyperlink; the word "funds" is not financial information (NSF's own Facilities sentence reads *"for whom no funds are being requested"* — a word-level match would flag a section for complying with it); *"et alia"* and the surname *"Etal"* are not *"et al."*; a heading word inside a sentence is not a heading (NSF's own "own line, no other text" rule is what does that work). **The worked example is a structural skeleton, not a sample proposal** — the three headings, on their own lines, with what each must answer — labeled in the UI as structural, never AI-written prose about the PI's science (golden rule 1). `sample_proposals.py` is **19 link-only entries with no text, and the product decision is never to rehost**, so there is nothing real here to quote from; extracting real Project Summaries from Morgan ORA's own six awarded, publicly-posted proposals is the better v2 and is separate work. **New surface:** `services/rulebook_baseline.py` (the rule table), `services/rulebook_checks.py` (the deterministic checks), `draft_review.review_section` (the same engine as `review_draft`, filtered to one section, no score returned — Draft Review's 409-on-no-solicitation exists to protect a percentage that this endpoint never computes), and three endpoints — `GET /api/me/section-check/sections`, `POST .../section-check`, `POST .../section-check/upload` — so a PI can check one section **while still writing it**, restoring a sliver of the writing help `nextStep()` lost when the Drafting Coach was removed 2026-08-10.
    **Two things this design got wrong, caught before either shipped.** The spec asserted `apply_delegation` would demote every new baseline row because *"every baseline row quotes the PAPPG"* — **false, verified directly**: `classify()` returns `target=None` for all 14 rows, because their `source` is NSF's own rule text ("Your file must include three separate section headers…"), not a pointer sentence naming the PAPPG. No real row was ever at risk. The guard shipped anyway is genuine defence-in-depth — NSF's own *"Please refer to the Format of the proposal (PAPPG) for all margin, spacing, font requirements"* is exactly the pointer shape a *future* rule could take — and it only became reachable at all once `_finding()` was fixed to propagate `req["rulebook"]` onto the finding it builds (dead code as first shipped: the guard checked a key nothing wrote). **`build_generic` had NO test caller at all before this work.** The whole backend suite passed unchanged the moment the injection landed — guaranteed, regardless of whether the injection worked, ran backwards, or threw, because nothing exercised the function. The 8 tests in `tests/test_rulebook_baseline.py` are the first ever to call it; a future green suite is not on its own evidence this path is correct.

  - **THE PAPPG RULES ARE IN — 142 of them, reviewed row by row, and four bugs the review itself surfaced (2026-08-17).** `kb_pappg/build_rules.py` turns the UNREVIEWED extraction into `backend/kb_structured/_pappg_24_1_rules.json`, which `rulebook_baseline` loads and merges with the 14 curated literals. **161 extracted -> 142 live**, merged with the 14 curated for **156 rules total**: 19 dropped, 3 re-placed, 48 marked unverifiable-from-text, 26 conditional, 19 marked prohibitions. Sections in `RULES` went **4 -> 9**; Table of Contents is the tenth slice and correctly yields none, because Research.gov generates it. **`sections_offered` returned 7, not 9 — and returns 4 since 2026-08-26**, when a review narrowed to the rulebook's BASIC rows plus the solicitation (see "BASICS PLUS THE SOLICITATION" below). Of the 156, **60 are scored and checkable**, 48 report `not_checked` by construction, and 24 are prohibitions. **A script, not a hand-edit**, so the review is auditable and a re-extraction can be re-reviewed instead of re-typed; every row carries the `review` key that kept it, and `--report` prints every decision with its reason.
    **The quote gate passed 161/161 before any of this, so grounding was never the problem — SELECTION was.** The passes that matter: a duplicate of a curated row is DROPPED, because half the curated rows carry deterministic checks and shipping both puts one rule on screen twice, once decided by code and once as a model opinion, free to disagree in front of the PI. The extraction's merged *"Include overview, intellectual merit, and broader impacts in Project Summary"* is the exact four-rows-into-one failure this file already records, and it is dropped in favour of the curated deterministic heading check plus three separate content rows. **Three curated sources were UPGRADED and only three**: `pappg_ps_overview` / `_merit` / `_impacts` carried *"Derived from … NSF's own wording is in the PAPPG"*, written that way deliberately because inventing an NSF sentence would be worse — the PAPPG slice closes the gap with the real sentence. Research.gov's wording elsewhere is NSF's wording, correctly attributed, and churning it would move the only yardstick `test_pappg_recall.py` has.
    **PLACEMENT IS THE THING SLICING DOES NOT SOLVE, and it was predicted before it was seen.** The PAPPG states a rule WHERE THE SITUATION ARISES; Research.gov states it WHERE YOU NEED IT. So *"unfunded senior personnel are described in the Facilities section"* is stated in the **Budget** section, and *"collaboration letters go in Supplementary Documents"* in the **Project Description**. Filed under the slice they were read from, all three land in a section they do not govern, where they can never be located. Moved rows have their id regenerated by `_pappg_id(dest, label)` — **never string-patched**, because the id's section prefix is a `canon_section` SLUG, not the key (`budget_and_budget_justification` -> `budget_budget_justification`), so a `replace()` on the key matches nothing and the row silently keeps an id that reads as belonging where it just left.
    **48 rules are `not_checked` BY CONSTRUCTION, via `rulebook_checks.rb_not_in_text`, and that is the whole of how Format / Cover Sheet / SciENcv rules can be held at all.** A one-inch margin, a 10-point font, a Research.gov cover-sheet field and a certification SciENcv inserts are real NSF rules and not one is a property of pasted text. Judged against a paste every one returns `not_found` — **against a fully compliant draft**. `not_checked` is absent from `_CREDIT`, so the row keeps its place and its quote and leaves the score's denominator. **`handled_by` is REQUIRED and is not decoration** ("the PDF you upload", "Research.gov", "SciENcv") — "not checked here" with no address reads as our omission, the same way "Not checked" did before it became "Not ours to check". **Done as DATA, deliberately NOT by widening `draft_scope`**: that module's docstring says over-excluding is its dangerous direction, and a regex broad enough to catch *"Maintain one-inch margins"* would start dropping real content requirements out of real solicitations. Measured first: **`draft_scope` excludes 0 of the 161** — the ingest already ran it, so these are its survivors, and its vocabulary of submission systems and submitting officers reads *"Enter LOI ID Number"* as a content rule.
    **`sections_offered` now filters the PICKER, not the rulebook.** Cover Sheet and Format of the Proposal hold nine rules each and every one is `rb_not_in_text`, so offering them in Section Check hands a PI nine rows of "not checked here" and nothing else — a dead end dressed as a tool. They keep their rules for a full Draft Review, where a PI who has never met NSF's font rules should meet them once. Same distinction `checklist_filter` draws keeping 7 of 24 requirements as tick-boxes while the profile keeps all 24.
    **FOUR BUGS THE REVIEW SURFACED, three of them found by RUNNING THE APP rather than by a test:**
    1. **A rule filed under a section key that LOST a merge can never be located.** `sections_from` already collapses two names for one part of a proposal — that is what stopped a present Budget Justification being reported missing — but it merges the section UNIVERSE and keeps whichever key was already in use. The three row sources spell a section with three different functions: a solicitation's own rows arrive via `canon_section` (`budget_justification`), contract rows verbatim, rulebook rows via `section_key` (`budget_and_budget_justification`). Measured live: the universe merged to the first and **45 PAPPG rules kept pointing at the second**, so every one reported "Not located" and left the denominator — recovered and then never checked, which is worse than not having them because the row looks handled. Fixed once at the profile level in `solicitation_profile._refile_rows`, which only ever moves a row ONTO a section that exists and leaves an unknown one alone. **The SAME merge bit Section Check separately, and `_refile_rows` could not reach it (2026-08-20).** That repair fixes the PROFILE, and Draft Review reads the whole profile — but `review_section` keys off the PICKER, which sends a fixed **rulebook** key (`budget_and_budget_justification`) while the universe kept the solicitation's (`budget_justification`). `requirements_for` is an exact-key match, so it returned nothing and the section was reviewed against the PAPPG's rows ALONE. Measured on a real NSF 23-598 proposal: **45 rules shown, 51 owed — 6 scored solicitation rows dropped**, including *"No more than 30% of the budget can be allocated for equipment"* and *"Inclusion of voluntary committed cost sharing is prohibited"*. Exactly the program-specific asks the PAPPG cannot state, and **the failure is invisible**: the rulebook's 45 still fill the page, so nothing on screen says six were skipped. `review_section` now resolves the picker's key through `resolve_section_key` before merging, keeping the exact key when it already matches so the common case is byte-identical. Verified live: proposal #8 **45 -> 51 findings** with all six checked and quoted; proposal #2, whose profile genuinely has no budget rows, stays at 45 — the fix adds nothing that was not there. **Newly visible, and NOT introduced by this fix:** a solicitation-extracted prohibition carries no `flag_if_present`, so *"Do not include voluntary committed cost sharing"* returns `partial` rather than `flagged` on a draft that violates it. The PAPPG's own curated row catches it; the solicitation's duplicate reads as partial compliance. Marking extracted prohibitions is separate work. Test: `test_section_check_finds_the_solicitations_rows_when_it_spells_the_section_differently`.
    **THE SAME MERGE HAD A SECOND HALF, AND FIXING ONE LAYER WAS NOT ENOUGH (2026-08-20).** Auditing which rows Section Check can actually reach turned up two NSF 23-598 rules on letters of support filed under `supplementary_document`, while the picker offers `special_information_and_supplementary_documentation`. `section_signature` compares the SET of meaning-carrying words and requires EQUALITY — its whole safety property, since containment would fold *"Project Description Supplementary Documents"* into *"Project Description"* and lose a real section (guarded by a test). `{supplementary, document}` and `{special, information, supplementary, documentation}` are simply different sets. The fix is a **NAMED equivalence** (`_EQUIVALENT_SECTIONS`), never a looser matcher — each entry is a judgement someone made deliberately and can read back. **It has to bite in TWO places and the first alone did nothing:** adding it to `resolve_section_key` looked right and changed no live number, because `sections_from` had already emitted BOTH names as separate sections, so the lookup took its exact-key hit on one and never consulted the equivalence. The universe merge in `sections_from` is the half that mattered; the lookup half is still needed for a universe carrying only one of the names. **Measured live: Special Information went 21+0 -> 21+2 rules, and across three real proposals the solicitation rows reachable from the picker went 42 -> 47.** Caught only because the audit asked "can anything reach this row", not because anything was visibly wrong — an orphaned row renders normally and is simply never checked.
    **THE STANDING GUARD IS THE POINT, not the one fix.** Three rows have now been orphaned by three different mechanisms — 45 rulebook rules on a key that lost a merge, 6 solicitation rules on a picker/profile key mismatch, 2 more on names that share no word-set — and every time the symptom was invisible. `test_no_requirement_can_point_at_a_section_that_does_not_exist` asserts every row is reachable by SOMETHING (the picker, Draft Review's wider universe, or whole-document scope) with no fourth option, and `test_every_rulebook_rule_lands_in_a_section_the_picker_or_a_review_can_reach` asserts every section holding rules has a real label and every offered section holds rules. **Full audit over three live proposals: 566 rules, 0 unreachable** — 468 rulebook (414 reachable from the picker, 54 Draft-Review-only: Cover Sheet and Format of the Proposal, correctly withheld), 98 solicitation (47 picker, 19 Draft-Review-only, 32 whole-document).

  - **A 50-RUN SOAK OF EVERY OFFERED SECTION, AND THE FIVE BUGS IT FOUND (2026-08-28).** Every section the picker offers, ten uploads each, through the real endpoint, against the AWARDED NSF EiR package split into its 11 real section PDFs (`~/Desktop/NSF-EiR-Sections`, original pages lifted from the submitted proposal, so page counts and typesetting are real). **50/50 returned 200 both before and after** — nothing crashed, and the deterministic rows were byte-stable across all ten runs of every section (page limits, headings, hyperlinks, dollar figures, `et al.`, narrative). The bugs were all in what happened AROUND the model.
    1. **AN AI OUTAGE RENDERED AS A PERFECT DRAFT.** Three of ten Project Summary runs returned in **0.3s with `ai=False`**, every semantic rule `unclear` — and scored **100%, green, "No problems found"**, against 93%/"Needs work" on the seven runs that reached the model. `unclear` is absent from `_CREDIT`, so the denominator collapsed to the two deterministic rules, both of which passed. `verdict()` already refuses to call anything "clean" without a score and `review_draft` already withholds the number when the AI layer is down — `review_section` was the one entry point that computed it anyway. Now withheld, with a message. **NOT merged with `use_ai=False`**, and the difference is who chose it: that flag is a caller asking for the rules alone and is scored on them by the 2026-08-20 product decision, no endpoint passes it, and six existing tests encode that contract. The outage is the case nobody chose. Tests: `test_ai_outage_is_not_a_clean_bill.py`.
    2. **THE CAUSE OF THAT OUTAGE WAS OUR OWN CONCURRENCY — `gemini_client.get_client()` had an unlocked lazy init.** It set `_init_attempted = True` BEFORE building the client, so while the first thread sat in the constructor every other thread read the flag, took it for a finished failure, and got None. **A Section Check is itself the first thing to race this**: `_voted_batch` asks the model three times at once. Measured: **3 of 4 concurrent callers** told the AI was unavailable. The flag now means "the attempt has FINISHED", set in a `finally` under a lock, so the documented fast-None-forever contract on a REAL failure is unchanged (its own test). After the fix: **50/50 runs reached the model, 0 unclear rows, 0 withheld scores** — the 0.3s runs are gone.
    3. **A FOURTH CHARACTER-LEVEL PDF ARTIFACT: page furniture injected MID-SENTENCE.** Research.gov stamps every page of a submitted proposal, so a sentence crossing a page boundary extracts as `"Hence, your research programs are strongly / Page 52 of 56 / Revised Proposal Budget Revision #1 ... / Submitted/PI: ... / supported by me and they firmly align..."`. The reviewer reads it correctly and quotes it whole; `quote_in` matches contiguously, fails, and golden rule 2 demotes `addressed` to `not_found` — so **"Attach Letter of Institutional Support" was reported MISSING on a funded proposal**, under a note describing the letter. Same family as the welded words, dash breaks and lost ligatures already recorded, and the same fix shape: an ADDITIONAL reading, the extracted text still a haystack. **ANCHORED TO THE `Page N of M` MARKER, NEVER TO REPETITION ALONE** — in that same file `"Sincerely,"` occurs three times and is real prose in three letters; dropping lines because they repeat would delete an author's words from the text every claim is checked against. A line is furniture only if it sits in an unbroken run beside a marker AND recurs at least as often as the markers do. After: that rule is **`addressed` 10/10** (was 7/10) and the section's score floor went **0% -> 75%**. Tests: `test_page_furniture_quotes.py`.
    4. **A FIFTH ARTIFACT, AND IT SCRAMBLES READING ORDER RATHER THAN SPACING (`PDF_Y_TOLERANCE = 5`).** pdfplumber groups characters into lines by baseline, and its default `y_tolerance` of 3 starts a NEW line whenever part of one sits slightly off. On the awarded Project Description that lifts superscript-styled numerals and an italic run OUT of their sentences: *"two undergraduates in Year ; three / 1 / undergraduates in Year ; and,"* and *"a part of my / Super Representation / with two primary goals: ... / Theory research program / representation theory of..."*. The reviewer reassembles the sentence correctly, quotes it, and `quote_in` compares it against our scrambled copy — so **"describe how the project improves student research opportunities" was `not_found` 10 runs of 10 on a funded proposal**, its note reading "Detailed plans for involving undergraduate and graduate students are provided". **5 not 8**: both fix it, 8 also merges genuinely separate lines (+1,117 chars on the same document), 5 moves its length by 0.1%. **Safe against this file's own yardstick for the x-ratio** — on the whole awarded package the ordinary-vocabulary counts are UNCHANGED (representation 40, mathematics 72, undergraduate 48), no welded token appears, and **NSF 23-598 extracts BYTE-IDENTICALLY**, so the solicitation path (which shares the constant, deliberately) reads exactly what it read before. The one count that moves is on page 1, the NSF **cover sheet** — a multi-column form whose rules are `rb_not_in_text` and never checked against text. Tests: `test_pdf_reading_order.py`; its synthetic PDF raises the numeral **4pt**, above the default tolerance and below ours, because a 2pt raise reproduces nothing and the guard passed either way until that was fixed.
    **THE GATE, NOT THE MODEL, IS WHAT MAKES PROJECT DESCRIPTION LOOK UNSTABLE — correcting this file's first reading of the soak.** Of 350 findings over 50 runs, **30 were quote-gate demotions and every one was in Project Description**; they account for that section's entire instability, rule for rule (5/10 runs, 4/10, 3/10, 3/10, 3/10, 1/10, 1/10, and one at 10/10). The model returned `addressed` every time. The reading-order fix took it to 26 and the section's ceiling to 100%. **The dominant remainder is a shape nothing here handles: the model ABRIDGES its quote with an ellipsis** (*"There are presently 50 math and actuarial science majors... Faculty at MSU are expected to carry 12 credit hours"*), which is not a contiguous span, so golden rule 2 drops it. Measured after the reading-order fix: **32 of 55 dropped quotes contain an ellipsis and 25 of those have EVERY fragment present in the draft, in order** — i.e. the words are all there and only the abridgement fails. This file already records the same shape defeating the curated NSF fixture row. **FIXED by accepting ordered fragments** (`text_match._abridged_fragments`), chosen over the two alternatives — reporting such a row as unverified rather than `not_found` is more honest but moves rows out of the score, and forbidding abridgement in the prompt is the class of fix this file twice records as unreliable. **What is given up is ADJACENCY, not grounding**: every word of the quote must still appear in the draft, in the sequence the reviewer wrote it, so a fabrication fails under any reading. **The minimum fragment length is the guard that matters** — without it this degrades into a bag-of-words match, since *"the PI... the work... the results"* appears in order in almost every proposal ever written; ONE short fragment rejects the WHOLE quote, the conservative direction, because a lost quote costs a row and a wrong one costs the guarantee. Reached only when the quote itself contains an ellipsis, so it cannot touch a quote that does not use one. Tests: `test_abridged_quotes.py` (11), six of which are the rejection guards and passed BEFORE the fix — they exist to stay passing after it.
    **MEASURED END TO END, three soaks of 10 uploads per offered section.** The baseline below is ALREADY past bugs 1-3 (the outage, the race and the page furniture), which is why its gate-drop count is 30 rather than the higher number those carried: **baseline -> +reading-order -> +ellipsis**. Gate-drops per 50 runs **30 -> 26 -> 8**; unstable rules **9 -> 10 -> 5**; Project Description alone **7/18 -> 7/18 -> 3/18** unstable with its score range **69-94% -> 69-100% -> 88-100%**. The four sections that never had a gate-drop were unmoved throughout, which is the evidence that the widening did not leak into text it should not reach.

    **WHAT REMAINS AFTER ALL FIVE FIXES, and it IS the model's this time.** On identical input: References Cited and Facilities **100% every run**, Project Summary **86-100%** (1 of 7 rules flips), Special Information **75-100%** (1 of 2 — the unmarked prohibition below), Project Description **88-100%** (3 of 18). Five unstable rules across every section, down from nine, and none of the five is a gate demotion. `SEMANTIC_VOTES = 3` narrows this without closing it, and the remedy this file twice records as unreliable for the class is a prompt change. **Do not read a single run's percentage as a fact about the draft.** The first reading of this soak blamed the model for Project Description's variance when the quote gate was causing all of it — check which side is failing before reaching for the prompt.
    **AND THE PI'S OWN TEST FOLDER CANNOT REACH 6 OF ITS 11 FILES.** The picker offered five sections for a live NSF 23-598 proposal; Budget Justification, Biographical Sketch, Current and Pending Support, Synergistic Activities, Data Management and Mentoring Plan have nowhere to go. For the first three the rules EXIST and are retired — Budget holds **45 PAPPG rules, Senior/Key Personnel 34, Special Information 21, and every one of them is `extended`**, so the 2026-08-26 basics-only decision took Budget and Senior/Key off the picker entirely (Special Information survives only because that solicitation happens to give it rows). That is the "142 extended rules are live in NOTHING" entry under Known limitations, seen from the PI's side. The last three sections have no PAPPG rules at all.

  - **"IT GIVES A DIFFERENT ANSWER EVERY UPLOAD" — a PI's report, and MOST OF IT WAS OUR OWN PDF DAMAGE, not the model (2026-08-31).** Ten real HTTP uploads of one awarded Project Summary PDF (`~/Desktop/NSF-EiR-Sections/01-Project-Summary.pdf`, 4,220 chars, 1 page — the extraction itself is byte-identical every run and was never the variable). Before: scores 93x8/86x2 and a wording count of **0,2,2,3,2,6,2,3,2,4**. The loud half was **`services/proofread.py` reporting our own extraction artifacts as the author's spelling** — seven distinct rows, each surfacing in ONE run of ten: `superal- gebra`, `theo- retical`, `fi- nite`, `Histor- ically`, `commu- nities`, `dis- semination` (words the TYPESETTER split at a line end) and `Submitted/PI: Dwight A Williams Ii /Proposal No: 2503008` (the Research.gov stamp). Every one is spelled correctly in the PI's document; none is actionable. After: **both real errors 10/10, zero noise, wording count 2 in all ten.**
    **Two filters, and the shape of each was decided by measurement, not by reading the code.** (1) A word split by a line-end dash — `text_match.has_line_break_hyphen` is now PUBLIC so there is one definition of what that is (this module exists because "verified" was once implemented twice and drifted). **Testing the QUOTE for "dash + space" is not enough and three artifacts still got through**: the model quotes the word BOTH ways, and the draft's `superal- gebra` came back as `superal-gebra` with the gap closed. The damaged forms are now derived from the DRAFT, which is the only thing that knows a split happened. (2) A submission stamp, recognised by SHAPE (`Proposal No:`, `Page N of M`), **never by repetition** — `mechanical_checks._running_furniture` needs a line THREE times, and in a one-page section that header appears ONCE. The first version of that test used two copies of the line and passed against a detector that could not have caught the real thing.
    **`word_choice` is parsed and then DROPPED (`_REPORTED_KINDS`).** Both false positives measured over 18 runs were that kind, and both were rewrites of correct prose — *"use 'respectively' instead of the adjective 'respective'"* over NSF's own *"photons and electrons, for respective examples"*. `_SYSTEM` already bans rewrites in words and the model does it anyway; this file already records that tightening the prompt against that exact shape removed it (0 of 6) and cost TWO THIRDS of the recall on real errors. So it is filtered in code and **the prompt is not touched**.
    **`PROOFREAD_VOTES = 5`, unioned — AND A THRESHOLD WAS TRIED AND REVERTED. Do not re-add it without re-measuring.** Per single call after the filters above, the two real errors hit 8/12 and 9/12 and noise 1/12; the binomial arithmetic said 2-of-5 would hold 98% recall and cut noise 22% -> 5%. **Measured over 10 real uploads it gave ZERO noise and dropped the real errors from 8/10 to 5/10.** The votes are **not independent samples** — a single call returns both errors or neither, and that correlation carries across a whole run, which had already been visible in the per-call data and was reasoned past. A proofreader that misses a real comma half the time is worse than one that occasionally shows an extra. **The generalisable bit: binomial reasoning about these votes is wrong; measure the configuration, never the model of it.**
    **A NOTE THAT ARRIVES BROKEN IS NO LONGER SHOWN (`draft_review._looks_truncated`).** 15 of 210 notes across 30 uploads began mid-sentence — *" recorded explicitly in the Overview section."*, *" requirements are addressed in the Overview section."*, *"reach clear research and broader goals"* — nearly all on one rule, and on a screen ORA staff read they look like broken software. **Instrumented at the wire first** (this file's own rule): the MODEL emits them that way, with a leading space and a missing first word; our code only calls `.strip()`. Nothing is invented to fix it — several readers answer every row, so a readable note is taken from a row that **already won the vote**; the status is never changed to obtain one, and if every reader broke it the broken note stands rather than a fabricated sentence about a draft. Measured after: **0 of 70**.
    **`borderline` is on every finding the readers split on**, set in `merge_votes` and rendered as a tag in BOTH modals with the same word. The median still decides the status and the score is untouched — this only says the call was close, which is the one signal a PI running the same file twice was never given.
    **FIVE THINGS WERE TRIED AND MEASURED NOT TO WORK. Each is recorded where it would be re-attempted:** a fixed sampling SEED (verified reaching the SDK at the wire, `config.seed == 11`; Vertex ignores it — 10 runs still 86/93); **more votes** (3, 5 and 7 give IDENTICAL stability, 7 votes just costs 2s); **resolving a split DOWNWARD** (sounds safer, measurably destroys consistency: 4 distinct scores and 3 moving rules against the median's 2 and 1, because one dissenting reader of three flips a rule where the median needs two); the vote THRESHOLD above; and **a result CACHE, built twice and removed twice by product decision** — see Known limitations.
    **WHAT REMAINS: one rule, and it is the draft's ambiguity, not a defect.** Final round of ten uploads: **93% in 8 of 10, 86% once, 100% once**, six of seven rules identical every run, and the seventh — *"The Overview states the methods to be employed"* — carrying `borderline` in 9 of 10. That Overview states what the work ADDRESSES and never names a method; the methods are in the Intellectual Merit paragraph. **The percentage exaggerates it**: with 7 rules one rule is worth 14 points and half a rule 7, so `partial -> not_found` on ONE row IS the 93->86 swing. That is why the modal leads with `6.5 of 7` and demotes the percentage.
  - **CHECK A SECTION IS UPLOAD-ONLY (2026-08-31, product decision).** The paste box was removed from `SectionCheckModal`. A paste and an upload of one section **could never agree, and not because of model variance**: a page count is a property of a PDF, so `pappg_ps_one_page` is CHECKED on an upload and `not_checked` on a paste — **7 rules assessed against 6**, a different denominator AND a passing mark lost from the numerator, every single time. Measured from identical text: upload 93%, paste 92%. For a tool PIs hand to ORA, and that ORA staff run themselves, that mismatch costs more than pasting saves. **The paste ENDPOINT survives and is still tested** (`POST /api/me/submissions/{id}/section-check`); only the screen stopped calling it, so restoring it is small. What it costs, stated: a PI can no longer check a section they have not turned into a PDF, which was the "while you are still writing it" case this tool was built for. The alternative not taken was dropping `pappg_ps_one_page` from the score on both paths so the two numbers are comparable — that removes a real check from the upload path.
  - **SECTION CHECK NOW SCORES, AND THE SCORE NAMES ITS AUTHORITIES (2026-08-20, by request).** `review_section` returned `score: None` on the reasoning — written into the code as a comment and into `SectionCheckModal.jsx` as one of its three carried-over rules — that a percentage would read as *"your Project Summary is 60% written"*, which these rules cannot measure since they are NSF's **floor**, not a completeness universe. **That concern is real and is answered by the DENOMINATOR, not by withholding the number:** only rules actually CHECKED are counted, so `not_checked`, `could_not_locate` and `unclear` are absent from `_CREDIT` and never enter, and an unscored conditional never enters either. `basis` states that in words under the number. **No second scorer** — `review_section` calls the same `score()` and the same `_CREDIT` as Draft Review, the same "one engine, two entry points" rule that governs the rest of this module, so the two can never disagree about one section.
    **`score.by_source` is the half that makes it honest.** A section is judged against TWO authorities at once and they are not interchangeable: a draft meeting every PAPPG rule and missing every solicitation rule reads the same combined number as one that did the reverse, and only the second is likely to come back from NSF without review. Measured live on a CLEAN Budget Justification against NSF 23-598: **72% overall — the PAPPG 88% (15 of 17), NSF 23-598 25% (1.5 of 6)**. One number would have said "72%, mostly fine"; the split says the program-specific asks are the problem. Rendered only when there is more than one source — a lone row repeating the headline number is noise.
    **The caption was WRONG on arrival and a test now guards it.** `basis` interpolated `solicitation_id` into the COUNT — *"100% of the 7 NSF 23-598 requirements"* on a section where **6 of the 7 were the PAPPG's**, attributing NSF's standing policy to a document that never stated it. Harmless in Draft Review, where every row genuinely came from the profile; wrong the moment a rulebook contributes rows. The id now appears only when `by_source` has exactly one entry. Same error class as a shared contract quote stamped on every row.
    **Four existing tests asserted `score is None`** and were updated, not deleted — each now asserts what the score must be instead, and `test_a_section_check_needs_no_solicitation` gained the stronger claim that with no solicitation `by_source` names **the PAPPG alone** (a score citing a solicitation that was never supplied would be a fabricated authority). Discrimination measured across five real drafts: Project Summary GOOD **100%** / WEAK **36%**, Budget CLEAN **72%** / VIOLATIONS **33%**. Tests: `test_a_section_check_returns_a_score`, `test_the_score_says_how_much_came_from_the_rulebook_and_how_much_from_the_solicitation`, `test_the_basis_does_not_credit_every_rule_to_the_solicitation`. **Still NOT a quality grade** — `sample_proposals` holds 22 funded proposals and zero declined, so nothing here knows what separates a funded draft from a rejected one; this counts rules met, and the caption says so.
    **AND THE FIRST SHIPPED VERSION READ AS A GRADE — caught by the PI the same day, not by a test.** A **152-word** Project Summary scored **100%**, and all seven rules behind it are PRESENCE checks (*has a heading*, *mentions objectives*, *speaks to broader impacts*). The arithmetic was right and the screen still lied: a 2.5rem green **100%** with a full progress bar, beside a faint grey line reading *"152 words … about 28% of the space you are given"*. Their words: *"the words are not enough, why would you rate it 100"*. **Fourth time this repo has rendered presence as approval** — the green ticks came off the section map, `addressed` became "Meets the rule", `delegated` became "Not ours to check", and this re-introduced it in a bigger font. **The math was NOT changed** — thinness is not a rule violation, the PAPPG sets a MAXIMUM and never a minimum, and making the number fall for shortness would invent the rule `section_guidance` has a test forbidding it to state. Four presentation changes instead: the **fraction is the headline** (`7 of 7`, with the percentage demoted to a 0.85rem footnote — a bare "100%" reads as a grade no matter what sits under it, "7 of 7" reads as the count it is); the **progress bar is gone** (a full green bar is the strongest "you are done" signal on the screen and presence checks have not earned it); the **length MEASUREMENT moved into the score box** at equal weight, so the count and the 28% are one glance apart rather than a heading and a footnote; and the presence caveat is authored in **`score()`'s `basis`**, not in either modal, so neither can render the number without it. The length line is now rendered in **exactly one place** — it stays in the guidance panel only when **over** the limit, which is a real violation and belongs beside the to-do list rather than beside a score it never enters. Guard: `test_the_score_caption_says_the_rules_check_presence_not_strength`. **Open, same failure shape:** `DraftReviewModal`'s `ScorePanel` still leads with a large percentage and a full bar. Its number spans ~200 rules across 14 sections where a fraction reads worse, so it was left alone — but it carries the new caption and nothing else, and the two modals now present the same kind of number differently.
    2. **PROHIBITIONS WERE SCORED AS CONTENT RULES, so a compliant draft was told to fix them.** A live Section Check of a clean Budget Justification put *"Do not request NSF funds for alcoholic beverages"* in the fix list — the draft never mentions alcohol, which is precisely compliance — and **9 of the 23 items it told the PI to fix were rules the draft already obeyed**. Absence means PASS for a prohibition and FAIL for everything else, and `_review_batch` only had the second vocabulary; the engine already had the first (`clear`/`flagged`, both in `_CREDIT`) but only deterministic rows could reach it. Rows are marked `flag_if_present` at build time (19, all checked by hand) and the prompt now names them. **`flagged` needs a verifiable quote and `clear` does not** — a flag is a POSITIVE claim about the draft, so golden rule 2 applies to it exactly as to `addressed`; an unquotable flag falls back to `clear`, never the other way, because a draft is presumed to comply until something in it can be quoted to the contrary. The MIRROR risk is the dangerous one: marking a CONTENT rule as a prohibition makes a draft that omits it come back `clear`, reporting a missing requirement as compliance — named rules guard it.
    3. **An UNSCORED conditional in the fix-list is 71% noise.** Of 14 rows a clean Budget Justification returned `not_found`, **10 were conditionals it was never subject to** (*"if you request consultants, detail them"*). The engine had them right — `scored: false`, out of the score — but both modals bucketed by STATUS alone, so they sat in "Fix these first" beside real gaps. The fix-list and the "Needs work" group now take **scored rows only**; the rest go to "If this applies to you", folded. Draft Review's fix-list went **63 -> 34** on one real draft; Section Check's Budget Justification went **23 -> 4**.
    4. **The always-open flat list had to go, and this is the change the earlier attempts predicted.** This file already recorded that shipping a PAPPG extraction "would take a review from 44 findings to ~640 and bury the four rows that matter". A real review is now **209 findings across 14 sections**. Both modals collapse: Draft Review groups by section and **opens a section iff it has a scored `not_found`/`flagged`**, Section Check groups by status with only "Needs work" and "Partly there" open. Measured on one draft: 5 sections open with 26 real items, **84 rows fold shut**. Each closed header states what it hides — a fold that does not say how much it hides is worse than no fold.
    **Still open:** the 142 rows are reviewed but have never been read by ORA; `SKELETONS` still covers only the original four sections, so the five new ones offer no worked structure; and nothing yet re-runs `build_rules.py` against a fresh extraction, so a PAPPG 25-1 would be a re-review, not a re-run.

  - **BASICS PLUS THE SOLICITATION — what a review actually checks (product decision, 2026-08-26).** `RULES["the PAPPG"]` is two populations merged without a label, and merging them is what let one bury the other: **14 CURATED** rows read off Research.gov's own per-section Content Instructions (NSF's own distillation, and where **every deterministic check** lives), and **142 EXTENDED** rows machine-read from Chapter II (real, reviewed, but the long tail — fonts, margins, conditionals, per-proposal-type variants). Rows now carry `tier: "basic" | "extended"`, written once in `rulebook_baseline` rather than left for callers to infer by comparing against a private list. **Both entry points take `basic` + the solicitation.** Measured on a live proposal: Check a Section was **138 PAPPG rules against 33 from the solicitation**, and **45 against 6** on Budget — six rules NSF wrote for that PI's own program, under forty-five general ones. The PI's words: *"I don't want the check a section to be on PAPPG rule entirely."* Draft Review followed the same day: **204 rules -> 62** (48 solicitation + 14 basics). **THIS RETIRES THE 142 ENTIRELY** — no path reads them for a review now, including the **19 prohibitions** (*"Do not request NSF funds for alcoholic beverages"*, *"Prohibit voluntary committed cost sharing"*), which are the rules a PI is least able to discover without reading the PAPPG. Taken knowingly; what makes it defensible is that **none of the 142 carries a deterministic check**, so this narrowed the model-judged half and left the arithmetic untouched. One line reverses it. **THE ROWS COME IN BY TWO DOORS and narrowing one is not narrowing:** `review_section` looks the rulebook up directly AND merges the profile's rows, and `baseline_rows` has already injected every rulebook rule INTO the profile at load — that is what makes the rulebook retroactive. Filtering only the direct lookup left a live Budget check at **51 rules, 45 of them extended**; the unit test missed it because its fixture's `source` never CITES the PAPPG, so the FIXTURE was what made the filter look like it worked. **`_basics_and_solicitation` drops ONLY the sections an extended row INTRODUCED**, never every section left empty — a required attachment puts a section in the universe with no requirement rows of its own, and dropping those would silently stop reporting a missing attachment, which is the compliance rejection this tool exists to prevent. Caught by the existing suite, not by the new tests. Tests: `test_section_check_basics_only.py`, `test_draft_review_basics_only.py`.

  - **THE PICKER IS A PROPERTY OF THE PROPOSAL, and one flat list (2026-08-26).** `GET /api/me/section-check/sections` is auth-free and takes no submission, so it could only ever answer for the rulebook: every proposal was offered the PAPPG's sections whatever its own solicitation asked for, and `_section_check_inputs` 400'd anything the rulebook had no rules for — unreachable twice over. Measured on a live NSF 23-598 proposal: of the solicitation's 53 rules only **24** were reachable, and the largest unreachable group was a whole deliverable — **Letter of Intent, 8 scored rules**, the first thing that program requires. `solicitation_profile.sections_offered_for(profile, rulebook)` merges both lists; a new auth'd `GET /api/me/submissions/{id}/section-check/sections` serves it, the auth-free route stays as the no-solicitation fallback. **The rulebook's KEY wins for a shared section** — `review_section` looks its rulebook rows up by exact key and only resolves the PROFILE's rows through `resolve_section_key`, so emitting the solicitation's spelling would silently return zero rulebook rows. **NAMING comes from every section the rulebook knows, INCLUSION from the offered ones**, or the picker's key would depend on which tier happened to be empty. The three headings ("From your solicitation" / "…+ NSF baseline" / "NSF baseline") were **removed within hours**: every section is checked against BOTH, so they described provenance and a PI read them as *"these are checked differently"* — *"IF IT CHECKS BOTH THEN DON'T GROUP IT."* The split still appears in `score.by_source`, after the run, where it changes what you do about it. Order then had to carry the message: solicitation-only parts first (a letter of intent genuinely is written first), then the rulebook's own order — flattened as it was, Budget led the list and Project Summary sat fifth.

  - **A SECTION'S NAME IS MINED FROM THE ROWS, never title-cased off the key (2026-08-26).** `canon_section` strips filler, so "Letter of Intent" becomes `letter_intent` and title-casing it back gives **"Letter Intent"** — a name nobody writes, offered in the picker. `section_label` on a row is the intended source and the extraction emits it *sometimes*. When it does not, the funder's wording is still in the requirement labels (*"Include required title format in Letter of Intent"*), so `sections_from` mines it. **SET EQUALITY is the safety property** — a phrase is accepted only when its meaning-carrying words are EXACTLY the key's, which is what stops "Project Description Supplementary Documents" naming `project_description`. An explicit `section_label` always wins; a phrase mined from mid-sentence is capitalised ONLY if it arrived wholly lower-case, so a funder's own casing is never overwritten. Tests: `test_section_labels_from_rows.py`.

  - **THE VERDICT READS BOTH HALVES — rules AND writing (2026-08-26).** A Project Summary with a doubled word, two misspellings, a wrong word and eleven more problems reported **5 of 5 rules met, 100%**. All fifteen had been found — `mechanical_checks`, `language_slips` and the model proofreader all ran — and all fifteen were outside the score, on the reasoning that a typo is not incompleteness against a solicitation. The reasoning holds; the screen it produced does not. **NOTHING IS BLENDED**: an error is verifiable, a weight is an opinion, and folding fifteen errors into a percentage means deciding what fraction of a missing Broader Impacts statement one typo is worth. So `draft_review.verdict()` returns two counts and a sentence that reads both, and drives the panel colour so a section cannot look green with errors under it. The top level says **"No problems found"**, never "ready to submit" — a test bans that phrasing, because this repo has rendered presence as approval four times. **`priorities()` fell back to a row's `note` when it had no suggestion**, so a passing binary check was listed in the to-do list carrying its own confirmation — its docstring already forbade that. The fallback is now asymmetric: a FAILING row's note says what is missing and is actionable; a PASSING row's says what was found and is not. **"Do this first" over four met rules is approval rendered as failure** — `priorities_heading()` decides the name from what is in the list, in the backend, where neither modal can render it without. **`[prohibited]` sat beside "Project Summary fits on one page"**: `flag_if_present` steers the semantic reviewer, which never sees a deterministic row, so it bought nothing and contradicted its own label. Removed from both page-limit rules; the 19 real prohibitions are guarded by a test. **TWO CAPTION BUGS, both live before that day:** `basis` may name the solicitation in the COUNT only when every counted rule came from it — the guard asked whether `by_source` had ONE entry, never whether that entry WAS the solicitation, so a section whose only scored rule was the rulebook's reported *"100% of the 1 NSF 23-598 requirements"*; and `review_section` hardcoded `scope`, naming the PAPPG on a Letter of Intent it holds no rules for. `scope` is now DERIVED from `by_source`. Tests: `test_section_verdict.py`, `test_priorities_and_prohibition_tag.py`.

  - **WORDING ISSUES CARRY A LOCATION, computed in code (2026-08-26).** A quote is evidence, not a position: *"Remove the spurious comma after 'and'"* over a six-word fragment still means hunting 523 words. `proofread.locate_quote` returns `{line, context, heading}` — the model is asked about ENGLISH, which it is authoritative on, while where a string sits is arithmetic. **Fails to ABSENT, never to wrong**: a quote that cannot be placed gets no location, because a wrong line number is acted on. The section's headings are read off the RULE that enforces them (`rb_headings`' own `check_args`), never typed twice, and only those named headings count — a short line is never guessed at, and text above every heading gets none. **The false positive it exposed was NOT fixable by prompt.** The proofreader kept suggesting *"use 'respectively' instead of 'for respective examples'"* — a rewrite, which `_SYSTEM` already bans — in **3 of 4 runs**. Tightening the prompt to name that exact shape removed it (0 of 6) and **cost two thirds of the recall on the PI's real errors** (their two genuine comma flags went 4 of 6 runs to 1 of 6). Reverted. Same lesson this file already records for `_RULEBOOK_SYSTEM`: a prompt change is not a reliable fix for this class, and only measuring both directions tells you. Tests: `test_wording_location.py`.

  - **A LATE REQUIREMENT READ IS DELIVERED, not discarded (2026-08-26).** Measured from a real PI's database rows: the document was read at 19:18:02, Create was clicked at **19:18:38**, and the requirement read takes **60-150s**. Create deliberately never blocks on it — waiting out two minutes is the one thing that could lose a PI their work, and that rule does not change — but the answer was then thrown away, so the proposal kept the funder's numbers and the document and NO requirement list. The PI attached their solicitation and the app went on behaving as though they had not: Draft Review 409s, the badge reads "rules only", Section Check offers the rulebook's sections alone. The browser now keeps the in-flight read and PUTs the requirements onto the proposal it just created, through the SAME attach endpoint rather than a second write path. Re-attaching is keyed by requirement id, so a task ticked off in the intervening minute survives; a late read that comes back unusable is **400'd rather than allowed to clobber a real profile**. Tests: `test_late_requirement_attach.py`.

  - **"ADDRESSED" WAS OVER-CLAIMING BY DESIGN, and the fix is a suggestion on every row (`services/section_guidance.py`, 2026-08-17).** Reported by a PI, not a test: a **76-word Project Summary came back with six of eight rules "Addressed"**, and their reaction was *"it's not getting close to real life… I don't think everything is addressed over there."* **Nothing was wrong with the checks.** Those PAPPG rules are about PRESENCE, and the summary really does contain a sentence naming objectives and methods, so `addressed` was correct against the rule as written. Three separate faults sat behind one symptom:
    **(1) The WORD was wrong.** `addressed` means "the rule is satisfied at the floor" and reads to an author as "this is good". Renamed to **"Meets the rule"** in both modals, with a caption saying these check presence, not quality — the same move, for the same reason, as "Not checked" → "Not ours to check" and as stripping the green ticks off the section map. Third time this repo has shipped presence-rendered-as-approval and had to unship it.
    **(2) A passing row's note was PRAISE.** *"The draft clearly states the overarching objective"* tells an author nothing, and a row that passes with no advice reads as "you are done here". `suggestion` is now a REQUIRED field on **every** row including `addressed` and `clear` — one concrete thing that would strengthen it, never prose to paste. That last clause is the boundary that keeps this from re-adding the **Drafting Coach deleted by product decision**: the prompt's rule 5 already said "never write the prose for the author" and a test asserts it survives.
    **(3) The tool was silent on the only number that mattered.** It knew the summary was 76 words and knew `WORDS_PER_PAGE = 550`, and never put them together. `length_guidance()` now says *"76 words. One page allows roughly 550 words, so this uses about 14% of the space you are given."* **A MEASUREMENT, NEVER A VERDICT** — the PAPPG sets a MAXIMUM for a section and never a minimum, so "too short" would invent the rule this is careful not to state; a test bans the words *too short*, *fail*, *must*, *insufficient* from the message. Silent between 60% and 100% of the allowance: a tool that remarks on every draft is one whose remarks stop being read, which is the same lesson as cutting the delegation caveat from four places to one. The allowance is read off the RULE that enforces it (`_section_page_limit` → `rb_page_limit`'s `check_args`), never typed twice, so the two can never disagree.
    **`priorities()` is deterministic and takes no extra model call** — failures, then thin passes, then met-but-improvable, ties broken by the profile's own requirement order. Advisory rows rank BELOW everything real (10 of 14 `not_found` rows on a clean Budget Justification were conditionals that draft was never subject to), rows with nothing concrete to say are DROPPED rather than listed ("improve this" is worse than one fewer line), and `not_checked`/`could_not_locate`/`delegated`/`not_in_draft`/`unclear` are excluded outright — every one of them means "nobody looked" or "not yours", and none belongs in a to-do list.
    **MEASURED, AND THE VARIANCE IS NOT FIXED — do not claim it is.** Three live runs of the identical 76-word paste: `addressed` came back **2, 3, then 1** and `partial` **3, 2, then 4**. Requiring a suggestion did NOT stabilise the statuses, which is the same result CLAUDE.md already records for `_RULEBOOK_SYSTEM` — a prompt change does not fix this class. What IS stable is the half that was moved into code: the length line was **byte-identical all three runs**, `suggestion` appeared on **6 of 8 rows every run** (the two without are the deterministic heading check, where a binary presence test has nothing to suggest, and the `not_checked` page row), and the **top priority was the same every run**. Items 2 and 3 of the plan can still shuffle, because they are derived from statuses that move. The author's complaint was really about trusting the output, and the answer is that the advice no longer depends on which way the model swung.
    **Deliberately NOT built: a quality score.** `sample_proposals` holds 22 FUNDED proposals and zero declined, so nothing in this system knows what separates a funded draft from a rejected one, and a number would be the model's opinion presented as a measurement. A reviewer-lens scorer existed and was deleted for exactly that reason; `mechanical_checks`' docstring makes the same argument. Tests: `tests/test_section_guidance.py` (20), `tests/test_pappg_rules_wiring.py` (26).

  - **HOW TO EXERCISE ANY OF THIS BY HAND — `test-drafts/`** (**COMMITTED as of 2026-08-20**, not untracked as this file said until then). Tracked deliberately: every expected number in its READMEs was MEASURED against a running backend, and `3-budget-justification-VIOLATIONS.txt` is the only artifact in the repo that exercises the prohibition path in both directions. **`test-drafts/sections/` is the per-section half** — one draft for each of the seven sections the Section Check picker offers, each carrying DELIBERATE, named violations so the checker has something to catch: an abstract-framed Project Summary, a hyperlink in the Project Description, an `et al.` plus an editorial paragraph in References Cited, a dollar figure in Facilities, a date of birth in a biosketch, an endorsement letter in Supplementary Documents. `4-budget-justification-CLEAN.txt` is the CONTROL — **0 flagged is the correct result**, against 6 for the VIOLATIONS file on the same section. Two more cover the language layer: `8-project-summary-TYPOS.txt` (5 of 9 planted errors caught deterministically) and `9-project-summary-WORDING.txt` (3 of 3 caught by the model proofreader). **Known flake, documented in that README:** Senior/Key Personnel has 19 semantic rules, which splits into two model batches, and one batch failing returns 15 rows as `unclear` — observed once in three runs. The other six sections were stable. Numbers below are from the ORIGINAL four drafts. Four drafts plus a README whose every expected number was MEASURED against a running backend rather than predicted, because a "should produce roughly" note is worth nothing on a path this non-deterministic. The two that earn their place: `3-budget-justification-VIOLATIONS.txt` deliberately requests alcohol, home-office rent, internal-meeting catering, local per diem, subject incentives and voluntary committed cost sharing — **all six must come back `flagged`, each quoting the offending sentence**, and deleting those sentences must flip all six to `clear`; that is the prohibition path in both directions, and it is the bug a clean draft exposed. `eir-1-package-WEAK.txt` / `eir-2-package-STRONG.txt` are the same science written badly and well against NSF 23-598's own 46 requirements — measured **53% / fix-list 25** against **85% / fix-list 7**. The weak one leaves out exactly what EiR asks for and no other program does: the sustainability plan, the success metrics, and the institutional-research-capacity paragraph. **A caveat the strong package exposes and the drafts cannot fix:** four rows stay `not_found` on a fully compliant package, two of them POST-AWARD reports due after an award that does not exist yet — the `draft_scope` gap this file already records under the checklist, capping the ceiling near 85%.

- **Reading a solicitation's requirements** (`services/solicitation_requirements.py`, endpoint **`POST /api/me/solicitation-requirements`**) — the deep read Draft Review depends on. Chunks the whole document (60k chars, 4k overlap), fans the chunks out **concurrently**, then **sweeps until dry** — a second reader with a *different* system prompt, asked what the first pass missed — verifying every row against a verbatim quote before keeping it.
  - **MEASURED against the only human-verified list we have** (`tests/test_solicitation_requirements_recall.py`, opt-in via `SOLICITATION_URL=` or `SOLICITATION_PDF=`; live model, never in CI). On NSF 23-598, 2026-08-11: **45 requirements from 54,065 chars, recall 24/24 = 100%, 0 dropped as unquotable**, 3 sweep rounds, 90s, and it still hit the round cap (so the list is a floor, not a ceiling). The 23 rows beyond the curated 24 are expected — a real solicitation states more than a human wrote down. Recall is matched by **5-gram overlap of the two `source` quotes**, not keywords: both quote the same document, so this asks "did it find this sentence?" An earlier keyword matcher would have passed while recovering nothing.
  - **Run-to-run: ~95% of requirements recur, but almost NO ids do.** Two reads of one document: 42 rows each, **95% / 93% coverage overlap by quote, id-jaccard 0.04**. `make_id` derives from the model's own label, and the same rule comes back as "cap equipment budget allocation at 30 percent" one run and "cap equipment expenses at 30 of total budget" the next. Consequences: (a) never compare extractions by id — an id-based stability check reported 0.12 on runs that had found the same rules, measuring wording churn and calling it instability; (b) dedup across chunks and sweep rounds is id-based, so a requirement the sweep restates in different words *can* survive as a second row. The sweep prompt's "do NOT repeat these" is the mitigation, the PI reviewing the list is the backstop, and quote-based dedup is NOT the fix — rule 4 deliberately splits compound sentences, so several legitimate rows share one quote.
  - **Gemini "thinking" is OFF on both solicitation reads, and it is the biggest latency lever in the app (2026-08-12).** `gemini-3.6-flash` thinks by default and on these extraction tasks the thinking dominated wall clock **without improving the answer**. Measured end-to-end on a 17-page / 50,508-char federal solicitation: **contract read 15.9s / 21.9s → 3.1s / 3.7s (~5x)**, **requirement read 72.2s → 25.9s / 27.1s (~2.7x)**, so **attach went ~91s → ~32s**. Quality moved the *right* way in both: the contract found 3 and 5 required attachments thinking-on vs 3 and 6 off, and recall against the human-verified curated list went **79% → 83% / 92%** — because the slow run spent its own `budget_s` on thinking and got through **fewer sweep rounds**. Two things to know: `solicitation_extractor` builds its **OWN** client so it does not inherit `gemini_client`'s config and must set `thinking_config` itself; and the `max_output_tokens=8192` comment there had already noticed implicit reasoning eating the output budget — it raised the ceiling instead of turning the reasoning off. Regression guards: `test_the_requirement_read_disables_thinking`, `test_the_contract_read_disables_thinking` (delete one kwarg and the flow silently gets 3x slower with nothing red). **`draft_review` CAPS thinking instead of disabling it (`THINKING_BUDGET = 1024`) — same model, opposite answer, so measure per caller rather than copying the setting.** Disabling it outright is faster still and is the WRONG trade there: over three paired runs the review went 15.4s → 8.0s but `assessed` fell **38.7 → 35.3**, one run collapsing to **27**. The reviewer **omits rows** when it cannot think, an omitted row becomes `unclear`, and `unclear` drops out of the score's denominator — so the speed is paid for in coverage, silently, which is the exact failure this tool exists to prevent. 1024 buys the latency without the loss: **six runs, mean 8.0s and `assessed=37` EVERY time**, against ~17s / 37 on the default — faster than thinking-on *and* more deterministic than thinking-off. Guard: `test_the_review_caps_thinking_rather_than_disabling_it`.
  - **The worker pool does nothing on a typical solicitation.** `CHUNK_CHARS` is 60,000, so anything under that is **ONE chunk** and `MAX_WORKERS=4` never engages — the first pass plus each sweep round is a single call, and the sweeps are inherently **serial** (each needs the `known` list from the round before). A 50,508-char document is therefore 4 sequential calls, 100% model time, ~0 overhead (measured). Do not look to concurrency for wins below `CHUNK_CHARS`; the lever is per-call latency.
  - **A CONVERGED SWEEP IS NOT AN EXHAUSTED DOCUMENT — there is a THIRD reader now (2026-08-13).** The sweep stops when a round finds nothing new, and that was being read as "we found everything". Measured on NSF 23-598: both readers ran to convergence, **no cap hit**, and **four requirements from the curated list were still missing** — including the outright prohibition *"Inclusion of voluntary committed cost sharing is prohibited"*, which `_SYSTEM` names in so many words ("prohibitions", "is prohibited"). **More rounds does not find them, and that was measured too**: `max_rounds=3` → 43 rows, `max_rounds=12` → 41 rows, the same four missing both times. What works is a different **TARGET**, not a different job description: `_TARGETED_SYSTEM` sweeps six named categories (A prohibitions, B compound "must describe how A, B and C" clauses, C what each named part must contain, D letters/attachments, E formatting, **F asks stated outside the preparation instructions**). Measured over the same document: categories A–E added +5 rows in 4.5s (recovering the cost-sharing prohibition); **adding F took recall 20/24 → 23/24**, live end-to-end **96%**. **F is the one to keep if this is ever trimmed** — the three rows it recovered are the sentence defining what a funded project must *accomplish*, and it sits mid-paragraph in the program-description prose, after *"The most competitive proposals will…"*, which every reader treats as advice. The pass runs **after** the sweep including after convergence, verifies quotes like everything else, and is reported (`targeted_added` / `targeted_ran`). Guards: `test_the_targeted_pass_runs_even_when_the_sweep_found_nothing_new` (an "optimisation" that runs it only when the sweep is still productive would skip exactly the documents that need it, and nothing else would go red), `test_the_targeted_pass_still_verifies_quotes`, `test_a_deliberate_skip_is_not_reported_as_a_time_cap`. **The 24th row is a MATCHER artifact, not a miss** — the Letter of Institutional Support IS extracted, quoting one sentence where the curated row's `source` joins two with an ellipsis (so it is not verbatim and `quote_in` rejects it against the document). Left alone deliberately: editing the yardstick to raise the score is the wrong move.
  - **`section` MUST name a part of the applicant's proposal, and the targeted reader broke that rule on arrival.** `_SYSTEM` rule 9 forbids the solicitation's own headings; `_TARGETED_SYSTEM` shipped without the rule and its category F ("asks stated in the program description") actively nudged toward it. Caught only by **running the app**, not by any test: the recovered cost-sharing prohibition and the three Project Description clauses came back with `section: program_description` / `budgetary_information`, which can never be located in a draft, so all four rendered **"Not located"** and dropped out of the score's denominator — recovered and then never checked, which is worse than useless because the row looks handled. `_TARGETED_SYSTEM` now carries the rule and says why. Measured after: those two sections gone, doc-heading rows **6 → 3** of ~40, recall unchanged at **23/24**. **The remaining 3 are pre-existing** (`preparation_instruction`, `vii_award_administration_information`) and come from the GENERIC readers — the same leak CLAUDE.md already records rule 9 as having mostly fixed. A deterministic deny-list in `canon_section` (doc heading → `None`, i.e. assess against the whole draft) would close it for every reader at once and is the obvious next fix; **not done deliberately** — "Program Description" is a plausible *proposal* section name for some sponsors, and one curated document is not enough evidence to risk mapping a real section to null.
  - **The targeted pass is NOT deterministic, and one run in five adds nothing.** Observed `targeted_added` across five live runs of the same document: **+5, +3, +4, +0, and a run where the recall test still scored 23/24**. The +0 run produced 38 rows and contained neither the cost-sharing prohibition nor the Project Description clauses. So this raises the *expected* completeness of a read; it does not guarantee any particular requirement is found. Same caveat as everything else here — human review before commit stays load-bearing (golden rule 4), and a PI re-reading a solicitation may legitimately get a different list.
  - **`max_rounds` is 8, was 3, and 3 had become the binding constraint for the wrong reason.** Disabling thinking (2026-08-12) made a round ~3x cheaper, so the counter started stopping the sweep with ~80% of `budget_s` unspent — and a counter-stop sets `hit_round_cap`, which tells the PI *"the list may be incomplete"* when nothing had actually run out. That is the warning a real user hit. Measured: the sweep **converges in 3–7 rounds and 27–35s** of the 150s budget, so 8 lets convergence be the reason it stops while `budget_s` stays the real bound. **Raising it further is not the lever** — see the third reader above.
  - **One pass is not enough, and that is measured, not assumed.** On identical input at temperature 0, the extractor returned **3 required attachments on one run and 5 on the next**. Asking the same prompt twice returns the same list; asking "what did the first reader miss?" is what finds the tail.
  - **`max_output_tokens` is 32768, and 8192 was a data-loss bug.** One chunk produced enough rows to hit the ceiling mid-string; the JSON came back unterminated, the parse failed, and **every requirement in that response was lost** with only a log line. A truncated response is indistinguishable from a chunk that had no requirements in it. Found by running it against a live solicitation, not by a test.
  - **`section` means the part of the APPLICANT'S PROPOSAL, never the solicitation's own document structure.** The first prompt said "named as this solicitation names it" and got `preparation_submission_instruction` for everything — headings from the FOA, which the locate stage can never match against a real draft. Fixing that one rule took a live run from **20 requirements to 43**.
  - **`quote_in` grew `drop_list_noise=True` for this caller** (default `False`, so `draft_review`'s grounding is byte-identical). pdfplumber leaves `(cid:127)` and bullet glyphs inside list items, and **a bulleted list is the commonest shape a requirement takes** — without noise-tolerant matching those rows fail verification and vanish silently, systematically, exactly where requirements are densest.
  - **Bounded twice, and it says so.** `budget_s=150` (half the 300s Cloud Run cap) and `max_rounds=3`, both reported as `hit_time_cap` / `hit_round_cap` and rendered as warnings. A silent stop reads as "we found everything" when it means "we stopped looking" — the same lie as the 250k truncation in `solicitation_extractor.extract_from_text`, which is now reported too (`truncated` / `input_chars`).
  - **The read report is not decoration.** `solicitation_extractor.read_pdf()` returns `pages` / `pages_without_text` / `chars`, and a scan surfaces as *"4 of 34 pages had no extractable text"*. There is no OCR. A partial read returns **200 with a warning, never 422** — turning it into an error would throw away the pages that DID read. Only a zero-character read is a 422.
  - **It runs in its OWN request, fired from the upload modal's review step**, not inside `/from-solicitation` (a 504 would lose the contract too) and not inside `/confirm` (a 504 there leaves the PI unsure whether their proposal exists). It costs 10-18 model calls and 60-150s, so it runs **once at attach time and is stored** — never per review. **Create is never blocked on it**; the button offers "Create without the requirement list".
  - **The DOCUMENT is stored by the CONTRACT step, not by this one (fixed 2026-08-12).** `solicitation_sources` used to be written only here, and this read deliberately does not block Create — so the two commonest paths through the modal kept the funder's numbers and threw the document away: a PI who clicked Create during the 60-150s read, and a read that failed. Both then hit *"upload the same solicitation again"* in Draft Review, which is the exact thing storing the text exists to prevent. Three parts to the fix, and the third is the one that actually bit: `/from-solicitation` + `/from-solicitation/url` now save the text the moment they read it and return a `source_id` (the PDF path reads via `read_pdf()` — `extract_from_pdf_bytes()` discards the text, and the text is the point); this endpoint takes that `source_id` **in preference to a file or url**, so one document is read once and leaves one row rather than two; and `SolicitationUploadModal` sends `source_id` at save **outside** the `reqState === "ready"` gate that used to hide it. `PUT .../solicitation` no longer 400s when there is no requirement list but there IS a document — its own button says "Attach without the requirement list", and refusing it discarded the document too. Still a 400 when neither is present. Tests: `tests/test_solicitation_source_persistence.py`.

- **Both solicitation migrations RAN CLEAN on production MySQL (2026-08-11, revision `00223-prd`).** Startup logged `'solicitation_json' column missing. Adding it now...` -> `Successfully added`, and `solicitation_sources table exists`. **One thing is still unverified:** whether `solicitation_sources.text` materialized as `MEDIUMTEXT` rather than `TEXT`. The deployed model declares `Text().with_variant(MEDIUMTEXT, "mysql")`, so it should have — but reading the live schema needs Cloud SQL access, and the instance's authorized networks are two fixed IPs (`73.132.171.58`, `158.103.2.5`). It only bites on a solicitation over **65,535 bytes**; NSF 23-598 is 54,065 and will not exercise it. Check with `SHOW CREATE TABLE solicitation_sources` when someone is on an authorized IP.
- **Every proposal that predates 2026-08-11 needs its solicitation uploaded ONCE.** `solicitation_sources` starts empty in every environment, and the old ingestion flow discarded the document after reading the deadline and cap — a 665-character `notes` summary cannot be turned back into 43 requirements. The UI states this rather than implying the PI skipped a step: the Solicitation badge reads **"rules only"** (not "attached") and Draft Review's panel explains why. After that one upload the document is kept and the PI is never asked again.

- **`Submission.solicitation_json`** — the stored profile: `{version, id, title, url, source, contract, requirements, merit_criteria, eligibility_notes, read_report, extraction, extracted_at, model}`. Written at `/from-solicitation/confirm` (create) or `PUT /api/me/submissions/{id}/solicitation` (attach later); `DELETE` detaches. **Three things are deliberately NOT stored and are rebuilt on load**: the `checks` callables (they don't serialize), the section universe, and the deterministic rows — regenerating the last two from `contract` means editing a page limit moves the check that enforces it, instead of leaving two copies to drift. **The raw solicitation text is not stored in THIS column either** (~300KB/row, and `list_submissions` loads whole rows) — it lives in `solicitation_sources`, one row per proposal, written by the contract read and bound at save. Consequence, and it is the one users notice: **a proposal created BEFORE 2026-08-11 has the funder's numbers in `notes` but no requirements, and they cannot be recovered — the document must be read once more.** The UI says so honestly (badge reads **"rules only"**, not "attached"; the Draft Review panel explains it rather than implying the PI skipped a step). Getting this wrong once already produced "Solicitation: attached" beside "Draft review: needs solicitation" on the same screen.


**Removed:** the former **Fundability / Eligibility self-check** tool (reviewer-lens scoring + go/no-go) was deleted per product decision — `services/fundability.py`, `services/eligibility.py`, their tests, the `FundabilityModal`, and the `/fundability/criteria` · `/eligibility` · `/fundability` endpoints are gone. Do not re-add. The eligibility *text* extracted from the solicitation is still stored in `notes` by the ingestion pipeline; the `_eligibility_text_from_notes` helper went with the Drafting Coach (2026-08-10), which was its only caller.

## Sample Proposals Library (standalone page, not a per-proposal tool)
A browse surface at **`/sample-proposals`** (navbar "Samples" button → `frontend/src/components/SampleProposalsLibrary.jsx`, styled like `FormsCatalog`) so inexperienced PIs can see what a fundable proposal actually reads like. **Curated, direct-to-document only** (product decision, 2026-07): every entry is a hand-vetted **`link` to an actual *funded* proposal** on an official funder's or a reputable university research office's own site (we never rehost), and each URL was verified live (HTTP 200 / `application/pdf`) when added. **19 today**, filtered by chip **NSF / NIH / NEH / Education / Early-career**: NSF full proposal (Duke) + CAREER/research/MRI/IUSE (Carleton–SERC); NIH R01×2 + R21 early-career (NIDCD, each with the reviewers' summary statement); NEH ×3 and Dept. of Education ×2 (incl. **Title III Strengthening Institutions** — HBCU-relevant); and **Morgan State ORA's own 6 awarded proposals** (NSF EiR / sociology / geoscience / math + 2 NIH R01s, hosted on morgan.edu). **Removed** this pass: the ORA-authored `"pdf"` samples we used to host, the generic **Open Grants** link, and the **live Open Grants community merge** (noise + PII risk). **DOE/DOD deliberately absent** — those agencies publish only templates/guides, not full funded samples. `services/ogrants_finder.py` + its unit tests remain in the tree but are **no longer merged** by the `/api/sample-proposals` endpoints. To add a sample: verify the URL is a live real funded proposal, add a `link` entry to `SAMPLE_PROPOSALS` (no `"pdf"` type anymore).

**Interest ranking:** a PI can type their research in an Opportunity-Finder-style box and the whole shelf reorders best-first. `rank_samples(items, query)` is **deterministic keyword overlap** (no LLM): title matches weigh 3×, **whole-word** matching (so "AI" ≠ "tr**ai**ning"; small `_KEEP_SHORT` allowlist keeps AI/ML/… despite <3 chars), unmatched items keep authored-first order and sort last, each match carries `{score, terms}`. Endpoint **`POST /api/sample-proposals/search`** (auth'd — the only sample endpoint that is, so it can fold in the PI's **saved `UserMemory` interests** like the Opportunity Finder); chips still filter in place; an empty query / zero matches reverts to browse order and the UI says so honestly ("no close matches yet — showing the full library").

Data + helpers are a constant in `services/sample_proposals.py` (mirrors `forms_catalog.py` — no DB, no LLM, and **no network** now that the live ogrants merge is off). Endpoints: public `GET /api/sample-proposals?category=` (listing) and public `GET /api/sample-proposals/{id}/download` (FileResponse; now **404s for every entry** since all entries are `link`-type — kept only for backwards-compat), plus the **auth'd** `POST /api/sample-proposals/search` (interest ranking + saved interests). The page sits behind `RequireAuth`; the two GETs are auth-free, the search POST is not. The old `make_sample_proposals.py` generator and the committed `backend/sample_proposals/*.pdf` files are now **orphaned** (no `"pdf"` entries reference them). (The root one-offs `scripts/make_demo_pdfs.py` / `make_nsf_real_draft.py` remain intentionally **local-only**, not in the repo.)

## Opportunity Finder + Fit Advisor (standalone page, not a per-proposal tool)
A discovery surface at **`/opportunities`** (navbar "Find Funding" button → `frontend/src/components/OpportunityFinder.jsx`, styled like the Samples/Forms surfaces) that closes the gap *before* a PI has a solicitation: they describe their research in plain text and get a ranked list of **live, open federal opportunities**, each with a grounded fit explanation, an eligibility verdict, and a mechanism note. Logic in **`services/opportunity_finder.py`**; endpoint **`POST /api/opportunities/search`** (auth'd; enriches the query with the user's saved interests from `UserMemory`).
- **Source = Grants.gov public REST API** (fixed host `api.grants.gov`, no key, covers all 26 federal agencies — NSF/NIH/DOE/…): `search2` (keyword → opp ids/basics) then `fetchOpportunity` per top result (the rich synopsis: `applicantTypes`, `applicantEligibilityDesc`, `synopsisDesc`, award ceiling, close date, contact). Foundations are out of v1 (no free API). Returns `[]` (never fabricates) on API failure.
- **Eligibility = deterministic STOP-gate** (`eligibility_gate`): Morgan State's baked-in profile (public IHE / HBCU / MSI — like the F&A rates are baked in) matched against the opportunity's applicant-type *descriptions* → `eligible` / `unrestricted` / `see_text` / `ineligible`. AI advisory only for the free-text PI-level eligibility.
- **Ranking = AI advisory, grounded** (`rank_and_explain`): Gemini re-ranks + gives a 1–2 sentence fit, each backed by a verbatim quote **verified against `synopsisDesc`** (unquotable quotes dropped, same whitespace-collapse pattern as `services/text_match.quote_in`). Falls back to API relevance order (no AI prose) when Gemini is down. `max_output_tokens=4096` (1536 truncated the ranking JSON → silent fallback). **The rank call passes `thinking_budget=0` + `timeout_s=8`, and `_MAX_RESULTS` is 8** (was 12): with 2.5-Flash "thinking" ON, the thinking tokens ate the 4096 output budget → the ranking JSON truncated → parse failed → the finder **silently fell back to unranked Grants.gov order with empty `fit_explanation`s**, AND the call took ~22s. Thinking OFF returns the full ranking in ~6s; the 8s timeout is a hard ceiling (falls back gracefully, never blows the <10s budget — Grants.gov itself is only ~1s).
- **Handoff:** "Start a proposal from this" routes the opp's solicitation URL into the existing `SolicitationUploadModal` via router state (`MyProposals` reads `location.state.solicitationUrl`; the modal gained an `initialUrl` prop), dropping the PI into the guided pathway. Tests: `tests/test_opportunity_finder.py` (unit) + `tests/test_opportunities_api_e2e.py` (route/auth).

## The admin KB panel (Cloud Datastore tab in `AdminDashboard.jsx`)
Three surfaces over the same 382 live documents. All admin-gated, all under `/api/admin/cloud-kb/`.

- **Navigation tree** (`backend/kb_tree.py` + `frontend/src/components/KbTree.jsx`, `GET .../tree`) — the flat 382-row list is now the 64-node hierarchy mirroring **morgan.edu/ora**. It joins two sources that each own exactly one thing: `_manifest.json` supplies **shape + the curated titles** that aren't derivable from slugs ("Animal Research (IACUC)", "F&A Cost Rates"), and each document's own `struct_data.kb_path` supplies **placement**. **Counts are computed from live documents, never from the manifest's frozen `doc_count`** — those were true on 2026-05-18 and would start lying the moment anything is filed. Children follow the **site's order, not the alphabet** (`_CHILD_ORDER`, verified against the live nav 2026-07-28). Placement precedence is `kb_path` → manifest fallback → unfiled, so **the tree works before `scripts/backfill_kb_path.py` has run** (it's a migration, not a prerequisite; `pending_backfill` counts documents still leaning on the manifest). A document with no `kb_path`, **or one pointing at a node that no longer exists**, lands in a pinned **Unfiled** bucket rather than vanishing — that second case is what surfaces orphans when ORA reorganizes and the manifest is regenerated. `PUT .../documents/{id}/placement` validates against `node_paths()`: you can file anywhere in the tree, you cannot invent a node.
- **Authoring** (`POST .../documents`, `create_kb_document`) — writes the full metadata a seeded document carries, with `category`/`subcategory` **derived from the chosen tree path** rather than asked for, plus `authored_in_dashboard: true`. `doc_id` is slugified from the title with a per-section prefix (`kb_tree.suggest_doc_id`), previewed live via `GET .../doc-id`, and **409s on collision rather than overwriting** — `update_document` is the edit path.
- **Web scrape** (`kb_scraper/`, `backend/kb_scrape_service.py`, `KbScrapePanel.jsx`) — see below.

**The `getCategoryColor` badge** now reads `doc.category`. It used to guess from an `academic|career|financial` doc_id prefix list inherited from a different KB that **no ORA document has ever matched**, so all 382 fell through to a meaningless "General". The real category was travelling from Vertex → `datastore_manager.py` → the browser and being discarded.

## KB web scrape (`kb_scraper/` — a Cloud Run **Job**, not backend work)
Admin clicks **Run scrape**; a Job crawls `morgan.edu/office-of-research-administration` with Playwright, and documents whose page materially changed are updated automatically and flagged for review.

**Why a Job.** All three of these bite and none are optional: the backend has **CPU throttled outside requests** (no `--no-cpu-throttling` anywhere) so a detached asyncio task stalls the moment the response closes; **`--timeout=300`** kills any single request and a full crawl is ~20 min (measured ~3s/page); and **`--max-instances=20` with no session affinity** makes in-process job state invisible to whichever instance serves the next poll — the **exact failure class that took the ADK service down**. So the work runs in a Job (CPU for its whole life, 3600s task timeout) and **all state lives in Cloud SQL** (`ScrapeRun`, `ScrapeChange`, `KbPageFingerprint`), polled every 2s. **Progress is polled, not SSE** — an SSE stream would hit the same 300s cap mid-crawl.

**Detection is a fingerprint gate, then AI.** The stored `content` is an **LLM summary of a page, not the page text**, so the two can never be diffed directly — comparison is scrape-to-scrape against a stored SHA-256 of the normalized text. Only moved hashes reach Gemini, which judges material vs cosmetic and drafts the replacement. A material verdict **must quote the live page verbatim**, verified in code with the same whitespace-collapsing test as `services/text_match.quote_in` (golden rule 2); unquotable or low-confidence is demoted to review and **never applied**.

**An unreadable page is NOT a change.** 404, empty JS render, timeout, or a 404 body served with a 200 → `looks_unreadable()` → the document is **left alone** and reported separately. Treating a failed read as "the content was deleted" is the single most destructive thing this feature could do.

**Nothing is applied automatically any more (2026-07-29).** The Job **never** modifies a document — not content, not metadata. Every material change becomes a `pending` row in `scrape_changes` and the document moves only when an admin clicks **Approve**; the tree's badge is a render-time join on pending rows, so an unapproved proposal leaves no trace on the document. It also **does not adjudicate a page's first sighting** — the stored `content` is an LLM summary written in May, so comparing it to live text would mark essentially every page "material" and propose rewrites of correct documents; first sighting records the fingerprint and `--audit` opts into the full sweep deliberately. Verified on the first real prod run: `changes_found=24`, **`changes_applied=0`**, zero rows with `status=applied`, zero rows carrying `previous_content`. The auto-apply paragraph below describes the **approve** path, which is what still writes.

**Approval is bounded and reversible.** `previous_content` is stored on every applied change and revert is one click. The review flag (`needs_review`, `last_auto_updated`, `what_changed`) goes in **`struct_data`, never in `content`** — Vertex indexes `content`, so a disclaimer written there could be quoted back to a PI asking about F&A rates. The tree renders an amber ⚠ badge with recursive `review_count` per node.

**Measured reality — only ~7% of the KB is auto-updatable.** 383 documents derive from just **59 distinct morgan.edu URLs**: 28 URLs map 1:1 (28 documents, auto-applicable) and 31 URLs feed 2+ documents (**355 documents**), because the KB was built by *splitting* pages — the IACUC SOPs page became **52** documents, D-RED seminars **49**, IACUC forms **37**. Re-splitting a changed page back across 52 documents unattended would corrupt all of them, so multi-document pages are **always reported, never rewritten**. Treat this feature as a change *reporter* that auto-applies where it safely can.

**`start_run` reaps stale runs** (>90 min without progress → `failed`). Without it a crashed Job — OOM, image pull failure, the missing `run.jobsExecutorWithOverrides` binding — leaves a row stuck in `running` and the one-at-a-time guard locks the feature permanently with no way back except a DB edit.

**The crawler was reading 9 pages at a fraction of their length, and missing a 10th entirely (both fixed 2026-08-06).** Two independent bugs, each silent — no error, no failed read, just less text than the page has:
- **morgan.edu's own accordion is `.accordion > a[href="#"]` toggling a sibling `.content` div that ships `style="display:none"`.** It carries **no `aria-expanded`, no `<details>`, and no bootstrap class**, so all three of `_expand_accordions()`'s original selectors matched *nothing* on such a page. Funding Sources extracted its 10 category headers and dropped all **66 programs** inside them; Human Subjects Research lost 7,004 characters including the **entire IRB meeting calendar and the continuing-review procedure**. Measured across the site: **9 pages, 36,332 characters, ~21% of all page text**. The site's own selector is now tried **first**, followed by a `display:block` sweep over `.accordion .content` for anything a click missed (an overlay or sticky header can eat the click; the text is what matters, not the animation). A page returning 200 with thin text is the failure signature — it looks like a thin page, not a broken read.
- **`is_in_scope()` was a bare prefix test, so ORA's vanity URLs were invisible.** The Trainings page links SPARK — ORA's flagship readiness program for research administrators — only as **`/spark`**, outside `/office-of-research-administration`. It was never crawled and had **no KB document at all**: not stale, absent. `_ORA_ALIASES = ("/ora", "/spark")` now matches a path *segment* (`path == a or path.startswith(a + "/")`), so `/sparkle` and `/sparkling-water` stay out. **Add an alias here whenever ORA publishes a short URL** — nothing else will find it. Regression tests: `test_ora_vanity_urls_are_in_scope` / `test_alias_matching_does_not_swallow_similarly_named_sections`.

**Never conclude "the KB covers this page" from a crawl that predates these fixes.** The 2026-08-06 audit ran first against the broken captures and cleared several pages that turned out to be hiding contradictions — a stale IRB calendar, a stipend lead time off by 15 days. Re-running the audit against corrected captures found **5 more contradictions and 18 more gaps** on the same pages.

## The 2026-08-06 website-vs-KB audit
ORA reported "gaps in the KB". All 75 live pages were crawled with accordions expanded and compared against the 469 documents by parallel agents, one per section. Result: **9 contradictions** (the KB stating something the live page refutes — worse than a gap, because the assistant answers confidently and wrongly) and **37 gaps**. Fixed in `scripts/apply_kb_gap_fixes.py` (snapshot, `--dry-run`/`--revert`) then `scripts/push_kb_gap_fixes.py` (live datastore). The worst were: **SAM registration reading as expired nine months ago** (KB `2025-11-15`, page `2026-10-14`) on the page that is otherwise entirely correct; the compliance-news document asserting the page was **"Coming Soon!"** while it carried a live NSF restriction on collaborations; the **IRB meeting calendar a full year stale**, so every date returned was wrong and in the past; and `pre_award_budget_development` summarising an 11,403-character page in 2,790 — missing the **MTDC definition, the $25,000 subaward cap**, the participant-support exclusion list, and that **Canada and Puerto Rico count as domestic travel**.

**`scripts/push_kb_gap_fixes.py` writes CONTENT ONLY, deliberately.** Measured 2026-08-06: every live document carries the **same seven** struct fields (`category`, `doc_id`, `file_path`, `playwright_verified`, `source_file`, `subcategory`, `title`) — the seeder wrote those and stopped. The snapshot's rich fields (`key_facts`, `staff_title`, the IRB schedule) have **never** been in the datastore, so pushing them would give 30 of 469 documents a shape no other document has. It is also unnecessary: Vertex indexes `content`, the chat path answers from `content`, and every correction is present there. **After any push, clear the chat answer cache** (admin → Sync All) — the cache is keyed on the question, not the content, so cached answers keep replaying exactly the facts the push just corrected.

**`datastore_manager`'s defaults point at a project that does not exist.** `GOOGLE_CLOUD_PROJECT` defaults to `oranavigator-vertex-ai` and `VERTEX_AI_DATASTORE_ID` to that project's **`oranavigator-kb-v7`**. Both are overridden in `cloudbuild.yaml`, so prod is fine and only *local* scripts hit it — as a `403 CONSUMER_INVALID` naming a project you have never heard of. Export both to run anything against the real datastore:
```bash
export GOOGLE_CLOUD_PROJECT=infra-vertex-494621-v1
export VERTEX_AI_DATASTORE_ID="projects/infra-vertex-494621-v1/locations/us/collections/default_collection/dataStores/oranavigator-kb-v8"
```

## The 2026-08-07 completeness pass (crawler fixes + what "complete" means here)

ORA asked whether the KB had everything on morgan.edu/ora. Answering it required fixing two
crawler bugs first, and the answer changed the KB's scope rules.

**THE KB HOLDS ORA KNOWLEDGE ONLY — being linked from morgan.edu/ora is NOT the test.** ORA's
site links a lot of material other offices own. The test is whether a research administrator
would ask ORA about it. Applied 2026-08-07 after the KB was found able to answer *what spill
supplies are stocked in the BioLabs* and *how long to hold an eye open at the eyewash station*:
**98 of the 110 documents ingested from site-linked files (543,924 of 593,941 characters, 88%)
were removed** — Title IX Prohibited Conduct Procedures and the Sexual Harassment Policy, the
Transformation Morgan 2030 strategic plan, the Biological Safety Manual, HR diversity plans and
EEO FAQs, Title VI, the Nondiscrimination Policy, IT 2FA instructions, the ARF emergency call
list. Kept the 12 that are research administration (SBIR/STTR + Subrecipient Monitoring decks,
ORA6 Subaward Process, Pivot-RP guide, FD syllabus and link lists, Research Misconduct flowchart
and deck, IRB-CITI). Nothing became undiscoverable: the ORA *page* documents still record that
these files exist and that ORA links them; only the ingested bodies are gone. Beyond looking
wrong, 543k characters of off-topic text competes for the same top-N retrieval slots as the real
answer. **Classify before loading — reversing it means deleting from both copies.**

**The crawler could not see ORA's documents at all.** `_extract()` read
`getAttribute('href')` — the raw string — and morgan.edu writes every PDF/DOCX link as a bare
relative path (`href="Documents/ADMINISTRATION/OFFICES/ora/x.pdf"`) resolved by a page-level
`<base href="https://www.morgan.edu/">`. `normalize_url()` understood absolute, `//` and
`/`-rooted forms only, so those strings normalized to `""` and were dropped at the point of
discovery: the templates page reported **0 of its 10** .docx, the PI Handbooks page **0 of 5**,
all 13 pre-award pages `file_links: []` while linking 17 PDFs. That disabled the one mechanism
the code calls *"the only way a file with no KB document is ever discovered."* `_extract()` now
reads the DOM **`href` property** (already absolute, already honours `<base>`); `normalize_url()`
learned the bare-relative form as a safety net. Both resolve against the **site root**, not the
containing page — `urljoin` against the page URL invents
`/office-of-research-administration/resources/Documents/...`, a plausible URL that 404s, which is
worse than dropping it. After the fix the crawl finds **277 distinct files** where it found 60.

**One staff profile is outside the section prefix.** The directory's "VIEW PROFILE" link for
Keyshawn Moncrieffe is the only one of 14 that leaves `/office-of-research-administration` —
it lives under `/research-and-economic-development/about-us/staff-directory/`. Added to
`_ORA_ALIASES`: same failure class as the `/spark` vanity URL, opposite direction (a *longer*
out-of-section path). **All 14 `staff_*` documents now point `source_url` at the person's PROFILE
page**, with the directory kept as `directory_url` — they previously all pointed at the directory,
leaving 22,708 characters of bios, degrees and certifications watched by nothing.

**`MAX_PDF_PAGES = 40` / `MAX_CHARS = 200_000` silently truncate.** Correct for the scrape job
(a change-detection gate over 277 files); wrong for ingestion, and the failure is invisible — a
truncated read returns clean text with no error, so the document looks complete and a coverage
check wrongly clears. Measured: MSU Procurement Policies is 118 pages / 245,842 chars and came
back **36% captured**; the Prohibited Conduct Procedures is 89 pages and came back at 40, losing
128,357 characters *after* it had already been pushed live. `extract_text()` now takes
`max_pages`/`max_chars`; **loaders must pass `None` for both.**

**Screenshot URLs were 404ing in production.** Rise asset keys arrive already URL-encoded and
`mirror_etraining_images.py` stored the last path segment verbatim, so the blob really is named
`...Screenshot%2520...` — but the URL was emitted unescaped, and the server decoded it back to a
name that does not exist. **74 of 262 stored URLs were dead**, and the survivors were Rise's stock
photos (plain filenames need no escaping), so the feature looked like it worked while exactly the
genuine Banner captures were broken. Repair is deliberately **empirical** (`fix_etraining_image_urls.py`
HEADs the bucket): a textual fix cannot be both correct and idempotent, because the broken form
`%2520` is itself a valid escape sequence — "escape any % that isn't already an escape" fixes
**0 of 78**, and unquote-then-requote mangles a correct URL back to the broken one.

**A whole-file manifest snapshot is the WRONG revert model.** `--revert` restored
`_all_documents.jsonl` from a snapshot taken at write time, which discarded 54 rows another
generator had added since and orphaned their files on disk — silently, with tests still green.
Reverts now remove **only their own rows, by doc_id**, and `scripts/merge_kb_payloads.py` is the
single writer of the manifest (concurrent writers to one file lose whole batches with no error).
After any bulk load, assert every manifest row has a file: exactly one legitimately does not
(`form_irb_informed_consent_template`).

**`create_kb_document` DROPS an unknown `kb_path` rather than rejecting the write.** It validates
against `kb_tree.node_paths()`, so an invented path yields a document that saves fine, searches
fine, and is quietly unplaced in the **Unfiled** bucket — visible only as a count in a panel
nobody watches. `trainings/training_presentations` and `trainings/spark` are NOT nodes.
`load_ora_site_files.py` now asserts its placements are real nodes at start-up.

**Quiz answer keys: the key is a PER-ANSWER `correct` boolean on each answer object**, not a
block-level `correct`/`corrects` id field — and Rise **omits the key entirely when false**, so
absent means incorrect. Measured against all 8 live payloads: 108 items carry a key, **105 were
already marked correctly, 0 mismarked** (387 options compared). A claim that 83% were inverted did
not survive checking — it compared the wrong field. The real gap was 3 `MULTIPLE_RESPONSE`
"select all that apply" items rendered as bare prose. **Two of those three are internally
inconsistent in ORA's own course data** (the key marks one option correct while
`feedbackIncorrect` says "One of your selections is not a requirement", implying three are), so
each rendered block carries an explicit note that the module disagrees with itself. Raise with ORA.

**There is no video or audio in the eTraining modules.** Zero blocks of family
`video`/`audio`/`embed`/`storyline` across all 8 payloads; the `mp4` and `vimeo` strings that
suggest otherwise are a random substring in a generated block id (`clx26dxn301kj35753yjqmp4v`) and
the `rise-vimeo-patch` feature flag. The 52 `multimedia` blocks are all `variant: attachment`.
**ORA's video narration IS capturable** — YouTube carries auto-generated captions for the ~10 site
recordings; 8 of 10 transcribe (222,217 chars). The 2 failures are external NIH/NLM clips with
captions disabled. Transcripts are labelled as speech recognition in their own first lines and
carry the video URL, because a mis-heard dollar figure is indistinguishable from an authored one
once it is prose in a KB. Duplicate uploads collapse by transcript hash (`qD6Efajaq5M` and
`zoDvRuwD0l4` are the same seminar under two ids).

**Screenshots are now READ, not just shown.** 235 of 262 yield text (93,252 chars), loaded as
**51 per-lesson documents** rather than 235 tiny ones — median transcription is 216 characters.
Transcriptions under 25 chars are dropped as captions on decorative photos. Vision is also a
second, content-based filter the filename rule cannot be: several files named `Screenshot ...`
are macOS wallpaper.

**KB is now 723 live documents / 724 manifest rows.**

## ORA eTraining modules (Articulate Rise) — `backend/kb_structured/_etraining_modules.json`

**8 modules, 294,190 characters, 106 knowledge checks, 52 embedded files.** Verified complete: a residual audit against the raw course payload found **1,426 prose strings, 0 missing — 100%**. Extracted 2026-08-05 after ORA asked whether the modules were in the KB. They were not: the KB held a **300–660 character description** of each ("a module about travel exists") and nothing they teach. This is Morgan's own grant-spending procedure, authored by ORA, and it exists nowhere else in the KB — not on the website pages, not in the PDFs.

**Rise ships the whole course to the browser in one payload.** The lesson sequence and the quiz gate are UI controls, not server-side ones, so the full text is one request per module. **Two endpoints are needed** because ORA's share links come in two shapes and only one host answers each:

```
share.articulate.com/{id}       ->  POST /api/instant-links/{id}/course
rise.articulate.com/share/{id}  ->  GET  /api/rise-runtime/boot/share/{id}
```

Attachments live on `articulateusercontent.com/{key}` (the `attachment.key` field), verified downloading.

**Gemini cannot read these pages and never will.** Measured with the URL Context tool: `URL_RETRIEVAL_STATUS_SUCCESS`, `finishReason: STOP`, **10 characters returned — the word `UNREADABLE`**. Not a safety block, not a fetch failure; the content simply isn't in the HTML. A real browser gets the cover page (~2.1k chars) and still not the lessons. **Only the course API works.**

**Three traps that cost real content, all found by re-checking rather than by the first pass:**

1. **Proofpoint-wrapped links.** The e-training page links several modules through `urldefense.proofpoint.com/v2/url?u=…`, which no `articulate.com` match will catch. Decoding (`-3A`→`:`, `-2D`→`-`, `-5F`→`_`, `_`→`/`) exposed **3 modules the KB had no record of**.
2. **The KB's links are STALE.** Republishing a Rise course mints a new share ID, and the KB kept the old one. `Purchasing with Purpose` is the bad case: the KB pointed at a **superseded 28k module** while the site links a rewritten **97k** one with different lessons (Uniform Guidance Subpart E, procurement thresholds). Always read the share IDs off the live `/trainings/e-training` page, never from `procedure_url`.
3. **Filter by JSON KEY, never by the shape of the value.** Loosening the value filter to catch single-word headings let Rise's own vocabulary flood in — block families (`impact`, `divider`), style enums (`WHITE`, `LIGHT`), asset paths (`rise/courses/.../x.jpg`), timestamps and author ids. Measured on one module: **52% of lines were structural junk**, ~10k characters. Dropping the structural keys (`type`, `family`, `variant`, `crushedKey`, `trackingContext`, `backgroundType`, `cardMode`, …) removes it while keeping the single-word CONTENT, because the cost principles arrive as `title` values and a value-shape filter cannot tell them apart from enums.
4. **A naive walker drops the load-bearing bits.** Requiring a space in a string kills single-word headings — *Allowability, Allocability, Reasonableness, Consistency*, the four cost principles. Skipping the `media` key kills labeled-graphic markers, which is where the procurement thresholds `$0 - $5,000` / `$5,001 - $25,000` live. That was ~134k characters, a third of the total.

**Multiple-choice answers MUST be rendered with the correct one marked.** This is a correctness rule, not a formatting one. The modules teach through worked problems whose distractors are plausible dollar figures — `$40,000` and `$18,410` sit beside the correct `$21,040` and `$17,431`. Flattened to prose, the assistant can quote a **deliberately wrong** number as Morgan policy. Every option carries `[CORRECT ANSWER]` or `[incorrect]`, with per-answer feedback.

**Loading:** `scripts/load_rise_etraining.py` (`--dry-run` / `--revert`). Content-only via `update_document`, so `struct_data` — placement, `procedure_url`, titles — is untouched; the previous bodies are written to `_etraining_kb_backup.json` first. The JSON is committed so the load is reproducible and the content reviewable in a diff.

**Blocks are self-contained — there is no nested content to chase.** `interactive`, `interactive-fullscreen` and `multimedia` blocks carry their items inline; a scan for iframes, embeds and Storyline packages found **zero** outbound content references. Everything is in the one payload.

**Rise stores answer explanations under THREE keys** — `feedback`, `feedbackCorrect`, `feedbackIncorrect` — and a quiz may use any of them. Reading only `feedback` silently dropped the teaching ("Try again. Remember the distinction between direct and indirect costs.") from every module.

**Screenshots are now SHOWN in chat answers (2026-08-06).** 183 real screenshots are mirrored to **`gs://oranavigator-kb-assets/etraining/{module}/`** and attached to the 69 lesson documents that have them, rendered under the answer via the same resolver and the same suppression guards as the Documents block. **Mirrored, not hot-linked:** the Articulate URL embeds the Rise course id, so republishing a module changes it — which has already happened to three modules. Public-read is deliberate (they are already served without auth from Articulate, and proxying them would put image traffic through a single-worker backend). **Filter by FILENAME, never by size.** Two rules, both name-based: drop Rise's stock photo library (`6_food.jpg`, `mountains.jpg`, `18_tech.jpg` — plain dictionary words, no upload prefix) and drop brand/UI assets (`logo`, `bear_head`, `Asset 1`, `favicon`). A size threshold fails in **both** directions and was measured doing so: a 60 KB floor discarded **86 genuine Banner screenshots** whose filenames read `fgibdst screenshot #2`, `budget transfer #2`, `enter time #3` — a third of the real set — while happily keeping a **1 MB `Morgan_logo.jpg`**. Real screenshots run from **2.9 KB to 2 MB**; there is no size that separates them. Block family does not separate them either. Built by `scripts/mirror_etraining_images.py` (`--dry-run`, idempotent) from `_etraining_images.json`.

**Still not captured (updated 2026-08-07):** nothing of substance. The three items previously listed here are done or were wrong. Video narration: **there is no video or audio in these modules at all** (see the 2026-08-07 section). The 52 attachments are **downloaded, extracted and loaded** (49 readable, 353,212 chars; 3 unreadable — a legacy `.doc`, a BIFF `.xls`, and a PNG needing OCR). Screenshots are **READ**: 235 of 262 transcribed by Gemini vision into 51 per-lesson documents. One real gap remains: pdfplumber does not read AcroForm field VALUES, so every "(Example)" filled form attachment extracts to exactly its blank template — the KB gets the blank, never the worked example. **SPLIT into per-lesson documents (2026-08-06).** Loading each module whole put 17k–84k characters into a KB averaging a few thousand, and `chunkingConfig` is create-time only and does not support `text/plain`, so every document is retrieved WHOLE — one procurement question pulled the entire course. The 8 modules are now **83 lesson documents** (median ~2,470 chars) at `trainings/e_training`, plus the 8 originals rewritten as **overviews** (description, lesson index, attachment list). The parents deliberately do NOT keep the full text: leaving it in both places would make every lesson compete with its own parent, which is the problem the split fixes. Each lesson carries a **deep link in `procedure_url`** (`{share}#/lessons/{id}`), so the chat attachment feature opens the learner at that exact lesson rather than the top of a 23-lesson course. Built by `scripts/split_etraining_lessons.py` (`--dry-run` / `--revert`) from `_etraining_lessons.json`; parent bodies are saved to `_etraining_parent_backup.json` first. **KB is now 723 live documents** (manifest 724 rows — the extra is the fileless `form_irb_informed_consent_template`; count last verified 2026-08-07). The 2026-08-06 audit added three: `trainings_spark` and the two test-prep chapters `form_racc_chapter_internal_controls` / `form_racc_chapter_herd_survey`.

## Caching (layers + lifetimes)
Caching exists to dodge the two real latency costs — the **Gemini call** and the **networked Cloud SQL read** from a **single-worker** backend — NOT to cache authoritative figures. **Cache the expensive (LLM answers), never the authoritative (budgets, statuses, verdicts, deadlines).**

**Chat answer cache** — `cache.py`, a 3-tier stack behind `query_cache.get/set` (keyed `md5(normalized_query : model)` — **no `user_id`**, so it's shared across users; personal/recall questions are gated out by `_should_cache`):
- **L1** in-memory (`cachetools.TTLCache`) — **24h**, 500 entries, per-instance.
- **L2** Redis — **7 days**, shared across instances, persistent. Degrades gracefully to "no L2" if Redis is down.
- **L3 semantic** (Google `text-embedding-004`, 256-dim) — matches re-worded questions at **cosine ≥ 0.95**; 100 in-memory entries + a 7-day Redis copy.
- **Citations** ride the same tiers under a `cit:` key (L1 24h / L2 7 days) so a cache HIT re-emits Sources.
- The chat HTTP responses are **`Cache-Control: no-cache`** (the cache is server-side only).

**Other server caches:** popular questions (`popular_questions.py`, in-process **6h** + Redis 7 days, daily-cron refreshed; the welcome-screen chips are now the **actual most-asked real user questions, Gemini-polished** — typos/grammar/semantic rewrite — and **PII-filtered** (`_is_personal_or_junk` drops SSN/salary/email/greetings/recall), NOT a fixed 36-question pool; `DEFAULT_QUESTION_POOL` is only the safe fallback/top-up. The one Gemini polish call runs on the daily refresh, never on a page load), Open Grants samples (`ogrants_finder.py`, **6h** TTLCache), forms catalog (`forms_catalog.py`, `@lru_cache` — **process-lifetime**, until redeploy). **Browser:** PWA service-worker precache of the app shell (workbox `autoUpdate` — until next deploy), Google-Fonts runtime `CacheFirst` (**1 year**), and `localStorage` (token/theme — until logout). TTLs are hard-coded constants per file (not yet env-configurable).

**Never cached:** personal-recall answers, errors/outages, transient "busy/try again" text, refusals (all gated in `cache.set`), and any answer's **personalized greeting** (stripped before storage — see Load-bearing fixes).

## Cron jobs (Cloud Scheduler → internal endpoints, auth via `X-Research-Secret` / `RESEARCH_SECRET`)
**7 endpoints — 5 recurring + 1 manual + 1 unscheduled**: `/api/internal/research/run` (daily ~2am), `/api/internal/memory/consolidate` (daily ~3am — rolls conversations into long-term `UserMemory`), `/api/internal/memory/idle-sweep` (every 5 min), `/api/internal/deadlines/check` (daily — Deadline Watcher, idempotent so a retry never double-emails), `/api/internal/popular-questions/recompute` (daily — rebuilds the welcome-screen chips; this is the one Gemini polish call, kept off the page-load path), `/api/internal/memory/backfill-profiles` (one-off), and **`/api/internal/kb-scrape/run`** (**no schedule yet** — starts the same Cloud Run Job as the admin button; returns `{"skipped": true}` rather than erroring if a scrape is already running, so a cron can't stack them). The actual schedules live in **Cloud Scheduler in GCP**, not the repo. Note these endpoints return as soon as the Job is *started* — unlike the others, which block for the whole batch inside the 300s request cap.

## Memory
Two tiers: **short-term** (current chat history) and **long-term** (`UserMemory` rows: department, role, active_grant, interests, …). The 3am consolidate cron moves chat facts into long-term; `mirror_profile_to_memories` mirrors saved profile fields into `UserMemory` on profile save. The chat endpoint also injects the user's **saved profile (name/department/title/role) directly from the `User` row every turn** as an authoritative `USER PROFILE` block — independent of memory selection — so "what department am I in?" always works (the top-N memory fetch alone could crowd the `[department]` row out). `build_memory_context` assembles long-term + semantic-recall + verbatim-turn sections for the agent.

## Load-bearing fixes (don't regress)
- **Gemini 429 on the chat path** (`vertex_agent.py`): a quota error arrives as response *text*; `_is_rate_limited` detects it, retries with backoff, then flags an outage — instead of laundering it into "system is busy" and amplifying via Pass-2 regeneration.
- **Two lanes — social chat (LANE 1) vs ORA (LANE 2)** (`agent.py` `BASE_INSTRUCTION` + `vertex_agent.py`): every chat message is either **LANE 1 social chat** (Gemini answers it itself, no KB) or **LANE 2 an ORA question** (answer ONLY from the KB). The `agent.py` instruction names both lanes explicitly; when a message could be either it must be treated as **LANE 2 and searched** — Gemini is FORBIDDEN to answer a possible ORA fact from its own training/memory (the strict "never fabricate a rate/deadline/name/ID" grounding preamble). **LANE 1** ("hi", "how are you", "thanks", "very good", "bye", plus bot-directed remarks like "do you miss me", "are you real?", "who are you") gets a warm KB-free reply — NOT the "I can only help with ORA" refusal, and NOT the "developed for Morgan State / ora.inavigator.ai" identity blurb (that blurb is only for "who made this app"). LANE 1 is answered by Gemini **naturally, to the message actually sent** — the per-kind example replies are now TONE guidance, not scripts. Do NOT re-add a fixed responder or force every pleasantry into one template (an earlier `smalltalk_reply()` fixed-per-category responder was removed as robotic; fix wording in the instruction instead). A substantive NON-ORA *knowledge* question (trivia, coding, recipes, other schools) is NOT LANE 1 — it is still refused per SECURITY. **Routing (`skip_kb_search`):** `_do_agent_pass` sets `state_delta["skip_kb_search"] = _is_smalltalk(msg) or _is_personal_identity(msg)`, and `agent.py`'s `before_model_callback` (`_select_model`) **drops the `VertexAiSearchTool` + KB prefetch** for those turns so the model physically can't run a stray KB search (which used to staple bogus Sources onto a greeting/identity turn). `_is_smalltalk` (whole message is pleasantry tokens via `_SMALLTALK_RE`, **OR** a bot-directed social question via `_SOCIAL_QUESTION_RE`) feeds BOTH this routing AND the Layer-3 verdict override (a LANE 1 turn is never graded "weak" and regenerated into a refusal) AND the `_is_non_kb_reply` Source-strip. Kept **NARROW**: `_is_personal_identity` (name/dept/role/email) and `_SOCIAL_QUESTION_RE` must NOT match institutional "my F&A rate" / "do you know the UEI?" — those stay LANE 2 and KEEP their KB search + Sources.
- **THREE CHARACTER-LEVEL PDF ARTIFACTS, all with one signature (2026-08-26).** Each made a tool say something FALSE about the PROPOSAL instead of reporting a bad read, and each surfaced by running a real document rather than by the suite. **(1) Welded words** — pdfplumber segments words on a FIXED 3pt gap and TeX moves the pen ~2pt, so a real awarded package produced **863 run-together tokens** including `IntellectualMerit` and `BroaderImpacts`; `x_tolerance_ratio=0.15` scales the tolerance with the font (863 -> 1) without chopping real words up. **(2) Dash-broken words** — a PI pasted a Project Summary out of a typeset PDF with twelve words split at a line end; the reviewer judged all three content rules `addressed` and two were dropped by the quote gate, so the screen read "Not found" under a note saying the section covered it. `quote_in` now reads text **BOTH ways** at a dash (`un-dergraduate` as one word AND as a hyphenated compound), because the break is genuinely ambiguous and joining always would break `bosonic-fermionic`. Only a DASH licenses joining. **The first version covered hyphen-minus alone**, recovered two of three rules, and left the third — that paragraph breaks on an EN DASH. 2 of 4 became 3 of 4 and the suite was green; only re-running the real draft caught it. **(3) Lost ligatures** — a typeset PDF sets `fi`/`ff`/`ffi` as single glyphs, pdfplumber emits a control character (**112 times** in one NSF solicitation) and `read_pdf` STRIPS them, so `justification` is stored as `justication` and `Office` as `Oce`: **72 mangled words**. Un-stripping is unavailable — the same character stands for several ligatures — so the COMPARISON is made aware instead, tried only after an honest match fails and only when the quote contains such a sequence, which is what keeps the widening away from ordinary text. **AND FIXING `quote_in` CHANGED NOTHING, because `solicitation_requirements` had a SECOND, private definition of verified** (`normalize(...) not in haystack`, with the document hoisted out of the loop) on the main extraction path — a reasonable optimisation that made it the only place golden rule 2 is implemented twice, and it received none of these fixes while being the component that reads solicitations out of typeset PDFs. The hoisted fast path is kept; `quote_in` is consulted only for rows it would otherwise drop. Measured on a real solicitation: *"2 proposed requirements were dropped because they could not be quoted"* -> **0**, both of them genuine NSF rules. Tests: `test_pdf_word_spacing.py`, `test_soft_hyphen_quotes.py`, `test_extractor_uses_one_gate.py`.
- **`gemini_client.get_client()` is LOCKED, and the lock is what keeps a Section Check honest (2026-08-28).** The lazy init set its "already attempted" flag before building the client, so a concurrent caller read the flag while the first thread was still in the constructor and got None — indistinguishable from "AI unavailable". `_voted_batch` calls the model three times at once, so Section Check races it on its own; measured 3 of 4 callers, and end to end an uploaded Project Summary returning in 0.3s scored **100% "No problems found"**. The flag now means "the attempt FINISHED" and is set in a `finally` under a lock. **Do not "simplify" this back to a bare flag check** — the fast-None-forever contract on a genuine failure (which `tests/conftest.py` rests on) is preserved by the `finally`, not by setting the flag early, and it has its own test.
- **Evidence whitespace** (`services/text_match.quote_in`, used by `draft_review` and `solicitation_requirements`): collapse whitespace on BOTH sides before the substring match — see golden rule 2. The optional `drop_list_noise=True` (default off) additionally ignores bullet glyphs and `(cid:N)` artifacts, and only the requirement extractor passes it — matching model output against PDF-extracted text is the one place those appear. `kb_scraper` re-implements it independently because it runs as a separate Cloud Run Job and cannot import from `backend/`.
- **Grounded citations from the real KB search** (`vertex_agent.py` + `agent.py` + `kb_prefetch.py`): a *correct but uncited* chat answer was the symptom of the model answering from the **prefetched KB context** (injected into the instruction) instead of the native `VertexAiSearchTool` retrieval, so `groundingMetadata` came back empty (no Sources) AND it tripped the weak-grounding verdict (the slow Pass-2 regeneration). Two-part fix: **(B)** the prefetch block is now labeled **`[REFERENCE CONTEXT - FACT-CHECK ONLY]`** (was "use this to ground your answer") + grounding **rule 8** in `BASE_INSTRUCTION`, so the answer + Sources rest on the model's own KB search. **(C)** `_fallback_citations(query)` runs a live Vertex AI Search over the same unified datastore and resolves titles → URLs via `_get_kb_url_map()`, attached at the DELIVER step when a *trusted* ORA answer came back uncited — gated by `_wants_fallback_citations()` (skips small talk, refusals, outages, `kb_fail`, already-cited). Forcing the retrieval tool is **not** possible: `vertex_ai_search` is native retrieval grounding, and `tool_config mode=ANY` governs only `function_declarations`. (You *can* still guarantee a search happens — by calling `SearchServiceClient` yourself, which `_fallback_citations` already does — you just cannot compel the model to call its own tool.)
- **Layer 1 prefetch now computes real TF-IDF** (`kb_prefetch.py`): the scorer was named TF-IDF in nine places but had **no IDF term** — `(tf/doc_len) * qf * 2.0` weighted a word in 189/382 docs (`research`) the same as a word in one. It now multiplies by `_idf(term) = log(N/df)`, with `_document_frequencies()` building the DF index once per cache load (memoized on `_cache_ts` + doc count, own `_df_lock` so it never nests with `_cache_lock`). A term in every doc scores 0; a term in one of 382 scores ~5.9. Measured against the 324 question→doc pairs in `eval/per_doc_results.jsonl`: **recall@5 90.4% → 91.7%**, 9.2ms/query. Tests: `backend/tests/test_kb_prefetch.py` (loads the module **by file path** — importing the package pulls `agent.py` → `google-adk`, which isn't always installed, which is why `test_agent_instruction.py` is excluded from the standard run). **Deliberately NOT changed:** the `[REFERENCE CONTEXT - FACT-CHECK ONLY]` wording (a test now asserts it survives) — relabelling it as a grounding source is what emptied `groundingMetadata` in the first place. Two known bigger levers left alone: the title bonus at `kb_prefetch.py:189` is a **substring** match (`"st(rate)gy"` matches the query token `rate`, worth +3) while the tokenizer uses word boundaries; and `category`/`subcategory` are loaded into the cache (`dict(doc.struct_data)`) but **never read by the scorer**, which only builds `f"{title} {content}"`. An offline bench combining BM25 + those labels measured **96.3%** recall@5, so most of the remaining headroom is there, not in the TF weighting — the flat `+10` entity / `+3` title bonuses still decide 94-99% of the score mass.
- **Chat latency budget** (`vertex_agent.py`): a turn can stack a full **Pass-2 regeneration** on Pass-1. `_over_latency_budget(elapsed)` (`CHAT_LATENCY_BUDGET_S`, code default 7s, **set to 5s in prod via `cloudbuild.yaml`**) skips the second agent round-trip when a *usable* Pass-1 answer graded weak and the turn has spent its budget — deliver Pass-1 + caution note instead (empty Pass-1 still regenerates; the streaming path already skips Pass-2). A `[LATENCY]` per-turn log (ms + verdict + chunks) makes the <10s target measurable. The bigger lever is warm instances (see Deploy) — cold starts, not the pipeline, were the ~40s.
- **Layer 3 grounding verification — surgical re-grounding REMOVED** (`vertex_agent.py` `_run_verified` / `_run_verified_stream`): the weak-answer path is now **grade (coverage ≥ 50% backed → "ok"; chunk count is NOT a gate) → LANE 1 / personal-recall guard (ship as-is) → latency bail (over budget → ship Pass-1 + `_WEAK_NOTE`) → strict-regenerate once (`_STRICT_PREFIX`, KB-only) → refuse (`_REFUSAL_MSG`)**. The streaming path can't regenerate, so a weak streamed answer just appends `_WEAK_NOTE`. **The UI only ever takes the streaming path** — `App.jsx` loads `Chatbox.jsx` → `/chat/stream` → `_run_verified_stream`; the non-streaming `Chat.jsx` is imported nowhere, so **the Pass-2 / `_REFUSAL_MSG` ladder is unreachable from the app** and `_run_verified` serves only `query_agent` callers (e.g. `/chat/guest`). On the live path Layer 3 is a **post-hoc annotator, not a gate**: the answer is already on screen, and all it can still do is append `_WEAK_NOTE`, strip or attach Sources, add the staff-name disclaimer, and inject procedure links (the terminal `done` event's text replaces the streamed preview in `Chatbox.jsx`). The former **surgical per-sentence re-grounding** (`_unsupported_sentences` / `_surgical_reground` + grounded-span math + identifier-rejection, ~220 lines) was **deleted** — it fired **0 times in 148 production turns** (measured with `scripts/analyze_layer3_logs.py`, which parses the `[LAYER3]`/`[LATENCY]` logs). **Do NOT re-add it.** `_check_identifier_faithfulness` stays (still called by `_finalize_answer` for logging; the user-facing footer remains disabled), as do the coverage gate `_evaluate_grounding` and the latency bail.
- **The coverage gate: what it measures, and who it actually applies to** (`vertex_agent.py` `_do_agent_pass` + `_evaluate_grounding`). Three things about Layer 3's only numeric signal that are NOT obvious from reading either function alone:
  - **`coverage` is not "% of the topic covered"** — it is `grounded_chars / len(answer)`, the fraction of the *answer's characters* falling inside Gemini's self-reported `groundingSupports` spans. It measures **citation density, not completeness**, and it is asserted by the model being graded (nothing compares a span's text to the chunk's text — that's the separate, log-only `_check_identifier_faithfulness`). A thorough answer that adds correct context beyond the retrieved passage scores *lower*, not higher. Spans are summed without an interval merge, so overlapping supports would double-count and `coverage` can exceed 1.0.
  - **Chunks-without-supports fails closed (fixed 2026-07-23).** When `groundingChunks` come back but `groundingSupports` is empty, nothing was measured. That branch used to set `coverage = 0.5` and log it as "conservative" — but the gate is `coverage >= _GROUNDING_MIN_COVERAGE` (**0.5**), so it cleared the bar on the `>=` boundary *every time*, silently claiming a verification that never ran. It now scores **0.0** → grades `weak`. Regression tests: `test_chunks_without_supports_score_zero_coverage` / `test_real_supports_still_compute_a_real_coverage` in `tests/test_grounding.py` (they drive the real SSE parser via the `_drive_pass` harness). **Don't restore the 0.5.**
  - **The gate does not run for authenticated users at all.** `_evaluate_grounding` returns `"ok"` on `has_attached_context` **before** it reaches the coverage comparison; `has_attached_context` is `bool(context)`, `context` is the `USER PROFILE (from account)` block, and `get_current_user` (`deps.py`) rejects any token lacking an email — so `profile_parts` is never empty and the coverage check is **dead code on `/chat` and `/chat/stream`**. It only bites on **`/chat/guest`** (`context=""`). Treat "Layer 3 verifies grounding" as **guest-only** until that bypass is narrowed (the fix would be scoping `has_data` to *uploaded file content* rather than any profile string — a real behavior change that would start grading production answers; deliberately not done). Related: **`_GROUNDING_MIN_CHUNKS = 2` is a dead constant** — defined, documented, and read by nothing since `df161ca`. It was never an AND-requirement even when live; the original gate was `chunks >= 2` **OR** `coverage >= 0.3`, i.e. two sources was a *sufficient* pass route, never a necessary one. When that branch was deleted (Gemini reports the chunk count unreliably — the same flakiness that forced `_fallback_citations` to exist) the coverage bar was raised 0.3 → 0.5 to compensate.
- **`get_document_content` truncated at 50,000 chars BY DEFAULT, and two callers read-modify-write** (`datastore_manager.py`). Harmless while every document was a few thousand characters; a data-loss bug the moment the eTraining modules landed (Purchasing is **98,784**). `main.py`'s research-suggestion push does `existing = get_document_content(doc_id)` then saves `existing + new` — a truncated read would have **permanently destroyed everything past 50k on the next push**, silently. `kb_scrape_service.approve_change` stores `previous_content` the same way, so an Undo would have restored a truncated document. The default is now **no truncation**; callers wanting a preview pass `max_chars` explicitly. Regression tests in `tests/test_datastore_metadata.py`. Note the failure mode: the write was always fine — only the read lied, so "wrote 98,784 / reads back 50,000" looks like a write cap and is not.
- **`update_document` used to erase metadata on any read failure** (`datastore_manager.py`): the read-modify-write swallowed **every** exception into `data = {}` and then patched that over `struct_data`, silently wiping `title`, `category`, `kb_path` and `file_path` on one transient blip. The document still saved, still answered questions, and just lost everything that placed it. Now **`NotFound` alone** is a legitimate create (`allow_missing=True`); any other read failure **refuses to write** — a failed save is recoverable, a wiped document is not. Regression tests in `tests/test_datastore_metadata.py`. This was latent for a long time and became a real hazard only when bulk placement writes arrived.
- **`upload_document` invented categories and crashed on PDFs** (`datastore_manager.py`): it guessed `category` from the same dead `academic|career|financial` prefix list, so **every** upload landed as `"general"` with the doc_id repeated as its subcategory; and it eagerly `utf-8`-decoded the bytes into an **unused** variable, raising `UnicodeDecodeError` on a `.pdf` — a file type the endpoint explicitly accepts — before the handler's error path could catch it. It now writes no placement at all, so an upload surfaces under **Unfiled** where it is visible and one click from filed. A wrong label is worse than none.
- **Write paths didn't invalidate the listing cache** (`main.py`): upload and delete cleared `query_cache` but not `_cloud_kb_cache`, so a newly uploaded document was invisible to both the flat list and the tree's Unfiled bucket for up to 60s — exactly the "where did my document go" the tree exists to prevent.
- **Citations to anything newer than the snapshot had no link** (`vertex_agent._get_kb_url_map`): the title→URL map was built **only** from the committed `kb_structured/` files, so a document created in the admin dashboard — or otherwise added to the datastore since 2026-05-18 — was cited with **no URL at all**, silently. It now overlays the live datastore's `source_url` values on top, lazily and once per process, never overriding a snapshot entry. Related and worth knowing: **the seeded documents carry no `source_url` in `struct_data`** (`setup_kb_datastore_v8.py` writes `doc_id/title/category/subcategory/source_file/file_path/playwright_verified` and stops), which is why the scraper's URL→document index reads the snapshot and uses `struct_data` only as an overlay. A datastore-only index maps **zero** pages and reports all 382 as new.
- **Draft Review crashed the WHOLE APP on every successful review, from 2026-08-11 to 2026-08-13.** `DraftReviewModal`'s footer rendered `result.solicitation.cycle.loi_deadline`, but `cycle` went out with the NSF 23-598 hardcoding — `draft_review._solicitation_meta` returns `{id, title, url}` and nothing else. So `.cycle` was `undefined`, the deref threw mid-render, React unmounted the tree and `App.jsx`'s boundary painted "Something went wrong". **The backend was fine and returned 200** (confirmed in Cloud Run logs: `POST …/draft-review/upload → 200 OK`, then *zero* further requests — the browser died holding a good answer). Two lessons worth more than the fix: **a de-hardcoding is not finished when the engine stops branching** — the same commit left `NSF 23-598` in the score caption, under every quoted requirement (`<cite>`), in the merit-notes blurb, the intro and the placeholder, so a DOE proposal was told its requirements came from NSF; all now read `result.solicitation.id`. And **grep the CONSUMERS when a response field is removed** — the backend test suite was green throughout, because nothing on the Python side references `cycle`.
- **A render crash is not a failed download, and the error boundary said it was** (`App.jsx` `ChunkErrorBoundary`). It reported *"The app couldn't finish loading. A refresh usually clears it."* for **any** error, including a component that threw — advice that is false for a render crash (the same TypeError fires on every reload) and is why the above read as a loading glitch for two days rather than a bug. It now picks the message from which failure happened, shows the error text on a render crash, and `componentDidCatch` logs the component stack in production — previously the only record of a render crash was a user's screenshot.
- **The requirement list could be dropped by clicking Create at the moment the read landed** (`SolicitationUploadModal`). `handleConfirm` gated on `reqState` — the value captured by the render that painted the button — so a read arriving between that paint and the click was discarded while sitting in memory. Observed in production: the read returned 200 at 14:49:23.9, Create hit the server at **14:49:24.1**, the proposal was written with no requirements, and the PI paid a second 28s read to recover a list we already had. Fixed by mirroring the read into a **ref** (read at click time, not render time) plus a **2s grace** on an in-flight read. **The "never blocked on the requirement read" rule is unchanged and still load-bearing** — verified in a browser: a 30s read releases Create at exactly 2.0s with no list and the `source_id` still bound, while a read landing 900ms after the click is now saved. Waiting out a 60–150s read is the one thing that could lose the PI their work; 2s is not that.
- **`gemini_client.DEFAULT_MODEL` is `gemini-2.5-flash`, so a caller that FORGETS `model=` silently downgrades.** Nothing goes red; the answer just comes from a different model. Verified 2026-08-13 that every call on the draft-review path passes `model=MODEL, location=MODEL_LOCATION` explicitly — `draft_review` (locate, coverage, reviewer notes), `solicitation_requirements._ask`, and `solicitation_extractor`'s own client — and that **18 of 18 live API calls went to `gemini-3.6-flash` on `locations/global`**. None of the six model/location env vars are set on the live backend, so the code defaults hold in production; `AGENT_MODEL=gemini-2.5-flash` IS set and is the CHATBOT, not the reviewer. **This used to say "check this whenever a new Gemini call is added" and is now a TEST** — `test_every_call_on_both_review_paths_names_the_model_and_the_region` spies at the boundary where the model name reaches Vertex and drives BOTH entry points, because `review_draft` and `review_section` share an engine but not a call graph (only the first runs `locate_sections` and `_reviewer_notes`). Re-verified live 2026-08-17 after the PAPPG rules landed: **14 of 14 calls** (11 from Draft Review, 3 from Section Check) went to `gemini-3.6-flash` on `global`. The guard was mutation-tested — deleting `model=MODEL` from one call turns it red (`1 of 3 calls did not name gemini-3.6-flash@global`) — because a guard that cannot fail is not one. **Re-verified a THIRD time 2026-08-20** after the score and proofreading work: **9 of 9** calls in-process, then **9 of 9 again through the running HTTP server** with a passive spy at `client.models.generate_content` — the true wire boundary, one level below the `_generate` kwargs the test inspects. Confirmed independently by the backend's own httpx log, which prints the URL unprompted: `POST .../locations/global/publishers/google/models/gemini-3.6-flash:generateContent 200 OK`. **`services/proofread.py` names the pair too** and has its own guard (`test_the_model_and_region_are_named`) — a new module that forgets it downgrades silently, and the module-level test in `test_pappg_rules_wiring.py` only drives the two review paths.
- **Budget justification truncation** (`/api/budget/justification`): a non-empty-but-truncated AI fragment must fall back to the deterministic template (guard on the total figure), or the box shows a half-sentence.
- **Chat knows the profile** (`main.py` chat endpoint): inject the saved `User` profile every turn — see Memory section.
- **Cache poisoning** (`cache.py`): transient "busy / try again" messages must never be cached (they're not real answers).
- **Cached personalized greeting** (`cache.py`): the chat cache key has **no `user_id`** (answers are shared across users), so an answer that opens "Hello \<name>!" would replay one user's name to everyone who asks the same question. `_strip_personal_greeting` removes a leading salutation addressed to a proper name on **both write** (never persist a name) **and read** (sanitize entries poisoned before the fix). Kept narrow — a punctuation terminator is required, so generic openings ("Hello there, here's how…") and content ("Hello World Bank is a funder…") are left intact; the generic KB body stays cacheable.
- **Deploy:** `cloudbuild.yaml` must use the correct secret names (`ora-*`, plus `SMTP_USER`/`SMTP_PASS` — see Secrets) and set the full Vertex env on both ADK and backend; the Cloud Build service account needs Cloud Run Admin + Service Account User. An **optional** secret (e.g. `ora-firecrawl-api-key`) must never hard-fail the deploy — the backend step self-heals a placeholder (see Secrets). To change **env/secrets only**, `gcloud run services update <svc> --update-env-vars=… / --update-secrets=…` applies live in ~30s with no rebuild — use `--update-`/`--remove-env-vars`, **never `--set-env-vars`**, which replaces the whole env set and would drop the manually-set `RESEARCH_SECRET` the crons depend on.
- **Stale PWA bundle → chunk 404 after deploy** (`frontend/nginx.conf`): returning PWA users hit 404s on lazy-loaded page chunks (e.g. the solicitation UI in `MyProposals-<hash>.js`) because a blanket `\.(js|css)$ → expires 1y; immutable` rule also cached **`index.html` and `sw.js`** — pinning the old service worker + shell so it kept requesting last build's hashed filenames. Fix: serve `index.html`, `sw.js`, `registerSW.js`, `manifest.webmanifest` as **no-cache/must-revalidate**; apply immutable-1y **only** to content-hashed files under `/assets/`. (Existing stale clients still need a one-time cache clear.)
- **No KB Sources on personal answers** (`vertex_agent.py`): a personal-recall question ("what department am I in?") is answered from the profile/chat history, not the KB — so it must NOT get KB citations stapled on. **Two paths, both closed:** (a) the FALLBACK path is gated by `_wants_fallback_citations` (skips small talk AND `_is_personal_recall`); (b) the model can ALSO run a stray KB search on a personal question and return REAL `groundingMetadata` citations, which bypass the fallback gate. Path (b) is closed by `_is_personal_identity` — a **narrow** name/department/role/email/"who am I" check, deliberately TIGHTER than `_is_personal_recall` (which also matches institutional "my F&A rate / my UEI / my required attachments" — those DO come from the KB and KEEP their Sources) — which clears `result["citations"]` in BOTH the `_run_verified` and `_run_verified_stream` DELIVER steps. (Disabling agent "thinking" for latency made the model call the search tool more often on personal questions, surfacing path (b).) **Update:** the two-lane routing (`skip_kb_search`, above) now **strips the search tool** for `_is_personal_identity` turns, so path (b) is prevented at source for those — the citation-clear is now belt-and-suspenders, and still covers `_is_personal_recall`'s broader institutional cases (which keep the tool + their legitimate Sources).
- **No KB Sources on off-topic refusals / pleasantries** (`vertex_agent.py`): same class as above but for **non-KB replies** — the **off-topic refusal** ("I can only help with Morgan State University Office of Research Administration questions."), greetings, outages, and **"thanks" acknowledgments** ("Great! I'm glad I could help."). The model runs a **stray KB search even on an off-topic / small-talk turn** (e.g. "who is the president of US?" or "thanks!") and returns REAL `groundingMetadata` citations, so the reply was rendered WITH a bogus `SOURCES: Staff: Farin Kamangar`. Fix: in BOTH the `_run_verified` and `_run_verified_stream` DELIVER steps, a single `_non_kb = _is_non_kb_reply(message, text)` guard **clears `result["citations"]` AND skips the `_wants_fallback_citations` fallback** (`if not _non_kb and …`) — clearing alone isn't enough because the fallback would immediately re-add Sources. `_is_non_kb_reply` fires when **(a)** the user's *message* is small talk (`_is_smalltalk` — covers praise/affirmations "very good"/"perfect"/"excellent"/"makes sense"/"good job" so `VERY GOOD.` classifies as small talk, **and bot-directed social questions** "do you miss me"/"are you real?"/"who are you" via `_SOCIAL_QUESTION_RE`), **(b)** the *reply* starts with a canned non-KB line (`_SKIP_GROUNDING_RE`: greetings / `I can only help with Morgan State` / outages / `You're welcome`), or **(c)** the *reply* is a **short** (≤240 char) conversational reply (`_NON_KB_REPLY_RE`: the greeting-response opener `I'm doing well/great/good`, `thanks for asking`, `glad/happy to help`, `no problem`, `my pleasure`, `you're welcome`). `_NON_KB_REPLY_RE` is deliberately NOT folded into `_SKIP_GROUNDING_RE` (which also gates grounding *verification* — a real answer opening "Great, the F&A rate is…" must not be exempted), its markers essentially never open a genuine grounded answer, and the length bound keeps a long real answer that says "happy to help you draft…" from losing its legitimate Sources. The `_SKIP_GROUNDING_RE`-only prefix match was insufficient: a greeting reply like `I'm doing well, thanks! How can I help you with ORA…` (to `VERY GOOD.`) matched none of those prefixes, so the stray Sources survived until the message/reply gates above were added. Verified: full backend suite green (682 passed).
- **ADK generate-config: "thinking" OFF + tighter token cap** (`adk_agent/…/agent.py`): the agent runs `generate_content_config` with `thinking_config=ThinkingConfig(thinking_budget=0)` and `max_output_tokens=2048` (was 4096). Gemini 2.5 Flash "thinking" is ON by default and adds seconds to every answer for a grounded retrieve+cite bot at temp 0.05 — the biggest single-pass latency win. `gemini_client` grew an **opt-in `thinking_budget` param** (default `None` = unchanged for every existing caller; only `opportunity_finder`'s rank call passes `0`). Restore 4096 / drop `thinking_config` if answers truncate or reasoning quality regresses.
- **Cache-hit citations** (`main.py` `/chat`, `/chat/stream`, `/chat/guest`): citations ride a parallel `cit:` cache key so a HIT re-emits Sources. **Known gap (not yet coded):** answers cached *before* Sources were stored (older popular questions like "what's the F&A rate?") replay with **empty** citations — a cache flush (admin `POST /api/admin/knowledge-base/sync-all` → `query_cache.clear()`) re-answers them fresh WITH Sources (a fresh answer stores `_fallback_citations`, verified working). A durable code fix would back-fill Sources on any cache hit missing them for a citable question.
- **Research agent = only real KB gaps, real answers** (`research_agent.py`): (1) `detect_and_log_failed_query` skips personal questions (`_is_personal_question`: personal-recall regex + PII + personal phrases) — a personal miss is NOT a KB gap. (2) `research_topic` uses the real Vertex project from `GOOGLE_CLOUD_PROJECT` (was a hardcoded, nonexistent `oranavigator-vertex-ai` → every research call 403'd → empty suggestions). Admin flow stays two-step: review → **Approve** → **Push to KB**.
- **Opportunity Finder** (`opportunity_finder.py`): `_is_open()` drops opportunities whose close date has passed (Grants.gov "posted" still returns expired/recurring ones); `fetch_opportunity` calls run **concurrently** (ThreadPoolExecutor) — the sequential fetches were the latency bottleneck. **Upcoming-only + rolling (product decision):** the open-date filter reads BOTH the synopsis `responseDate` AND the search2 `closeDate` (often the only one populated for standing programs) — a PAST date in EITHER excludes the opp, so an expired program with a blank `responseDate` no longer slips through. Rolling/no-deadline programs are KEPT and flagged `rolling: true` (UI labels them "Rolling — no fixed deadline"); results are stably partitioned **dated-upcoming first, rolling last** (the AI fit ranking still orders within each group).
- **Outbound email — a working sender, and the silent failure that hid a broken one** (`email_service.py`, `cloudbuild.yaml`). Restored 2026-07-28 after months of `SKIP_EMAIL_VERIFICATION=true`. Four things to keep straight:
  - **The failure mode is silent by design.** `_send_email` returns **`True`** when `SMTP_USER`/`SMTP_PASS` are empty (`email_service.py:33-36`) so dev flows continue — which means a misconfigured prod looks *healthy*: `/api/forgot-password` reports success, the UI says "check your email", and nothing is sent. Proven in prod logs: `WARNING [EMAIL] SMTP not configured. Would send to milam5@morgan.edu`. **Never conclude email works from the UI or a 200** — grep the backend logs for `[EMAIL] Sent to` vs `[EMAIL] SMTP not configured`.
  - **Config:** `SMTP_HOST=smtp.gmail.com`, `SMTP_PORT=587`, `SMTP_USER`/`SMTP_PASS` from the same-named secrets (a Gmail **App Password** on a real mailbox — a Google **Group can never be `SMTP_USER`**, it has no password), `FROM_EMAIL=oranav@morgan.edu`, `REPLY_TO=oranav@morgan.edu`. `FROM_EMAIL` must never name an address unrelated to `SMTP_USER` — the old config claimed `noreplyinavigator@gmail.com` while authenticating as a `morgan.edu` account. Cloud Run **rejects a key set as both an env var and a secret**, so `SMTP_USER` must not appear in `--set-env-vars`.
  - **A successful SMTP handshake does NOT prove the `From` survived.** Gmail accepts the message and then **silently rewrites `From` to the authenticated account** when it disallows the address. `oranav@morgan.edu` works here (verified 2026-07-28 by sending a live message and **reading the delivered header**) *without* any send-as alias verification — the alias-verification path was a dead end, because the Google Group never receives Google's confirmation code. If the sender address ever changes, re-verify the same way: send, then read the header.
  - **`REPLY_TO` needs no verification anywhere**, unlike `From` — so replies can always be routed to a shared group even when the `From` question is unresolved.
  - Still open: `SMTP_USER` is an **individual's** mailbox + App Password, so email dies if that person leaves or rotates credentials. A service mailbox (not a group) is the durable fix. `SKIP_EMAIL_VERIFICATION` stays `true` until a live send to a **non-Morgan** address is confirmed — same-domain delivery doesn't prove external delivery.

## Known limitations / open work
- **OPEN, BY PRODUCT DECISION — a Section Check score can still move by one rule, and a CACHE is the only thing that closes it (2026-08-31).** Ten real uploads of one unchanged PDF: **93% in 8, 86% once, 100% once**; six of seven rules identical every run and the seventh (`pappg_ps_overview_methods`) carrying `borderline` in 9 of 10. Everything cheaper was tried and MEASURED not to work — a fixed seed, 3/5/7 votes, resolving splits downward, a vote threshold (all written up under Draft Review). **A result cache WAS built twice and removed twice at the PI's instruction**, and the design is worth not re-deriving: keyed on a one-way SHA-256 of the text so the manuscript is never stored, keyed ALSO on a fingerprint of the rules so editing one busts it immediately (which is what CLAUDE.md's "never cache the authoritative" rule actually exists to protect), model layer only so every deterministic check still runs, and never storing an outage or an empty proofreading pass. Measured with it on: **10/10 identical**. Two reasons it was refused, and the second is the one that matters for production: it **freezes whichever answer came first**, so a borderline rule locks at a coin-flip and the screen looks certain about something it is not; and the version built was **in-process**, while the backend runs up to 20 instances — two ORA reviewers on different instances would still disagree, so it would have to be Redis (already configured as `ora-redis-url`) to help the case it was for. **The honest third option is neither**: the rule is undecided because that Overview states what the work ADDRESSES and never names a method, so one sentence in the draft settles it permanently.
- **OPEN, AND TAKEN DELIBERATELY — the 142 extended PAPPG rules are live in NOTHING (2026-08-26).** Both review entry points now take the rulebook's 14 curated rows plus the solicitation's own (see "BASICS PLUS THE SOLICITATION"), and Draft Review was the last path that read the rest. They remain reviewed, in `kb_structured/_pappg_24_1_rules.json`, one line from being switched back on — but nothing checks them today, **including the 19 prohibitions**. Those are the rules a PI is least able to discover without reading the PAPPG themselves. The product decision was made with that cost stated; the middle option on the table and not taken was basics + solicitation + the 19 prohibitions (81 rules rather than 62).
  **WHAT THIS COSTS A PI, MEASURED FROM THEIR SIDE (2026-08-28).** Given the awarded NSF EiR package split into its 11 real section files, the picker offered **five** sections and **6 of the 11 files had nowhere to go**: Budget Justification, Biographical Sketch, Current and Pending Support, Synergistic Activities, Data Management and Mentoring Plan. For the first three the rules EXIST and are switched off — **Budget holds 45 PAPPG rules, Senior/Key Personnel 34 and Special Information 21, and every one of them is `extended`** — so those sections leave the picker entirely unless the solicitation happens to give them rows (which is the only reason Special Information appeared at all). The last three have no PAPPG rules in any tier. **It also silently voided the mitigation recorded under the unmarked-prohibition entry below** — that entry's safety net was the extended tier.
- **OPEN — `get_current_user` and `get_db` are each defined TWICE, in `main.py` and in `deps.py`, and the routes use `main.py`'s.** Found 2026-08-28 while driving the Section Check endpoints with a TestClient: overriding `deps.get_current_user` alone leaves every route 403, because `main.py` defines its own at line ~496 and `Depends()` binds by object identity. `tests/test_section_check_api.py` already overrides BOTH, which is why the suite never noticed. The hazard is not the tests — it is that a fix to one copy (a claim added to the token, a new ownership check) silently does not apply to whatever depends on the other. Not consolidated here because it is unrelated to the review engine and touches every authenticated route.

- **OPEN — the Letter of Intent is offered as a "section" and is not one.** NSF 23-598 states `Letter of Intent Due Date(s) (required)` MONTHS before the full proposal deadline: it is a separate submission, not a part of the proposal. Its 8 extracted rules are real and there is nowhere else in the app to check them, so removing it loses something — but listing it among proposal sections teaches a PI something false about what a proposal contains. Three options were put to the PI (label it as a separate submission / remove it from the picker / leave it); no decision yet.
- **OPEN — the proofreader breaks its own style ban intermittently, and the prompt cannot fix it.** `_SYSTEM` forbids rewrites and word-choice preference; the model offered *"use 'respectively' instead of 'for respective examples'"* in 3 of 4 runs and *"avoid starting the sentence with a coordinating conjunction"* in another. Tightening the prompt removed the false positive and cost two thirds of the recall on real errors (measured, 6 runs each, then reverted). The remaining levers are dropping `word_choice` from `_ALLOWED_KINDS` entirely — `language_slips` already catches `rather then` deterministically — or accepting it. Not decided.
- **OPEN — a solicitation-extracted PROHIBITION carries no `flag_if_present`, so a draft that violates it reports `not_found` instead of `flagged`.** The 19 curated PAPPG prohibitions were marked by hand at build time; rows read out of a solicitation are not marked at all. Seen live twice: NSF 23-598's *"Inclusion of voluntary committed cost sharing is prohibited"* came back **`partial`** on a draft that budgets $25,000 of it, and its *"Do not include letters of support from collaborators"* came back **`not_found`** on a draft carrying a gushing endorsement letter. **THAT MITIGATION IS NOW VOID, AND A DIFFERENT CHANGE VOIDED IT SILENTLY (verified 2026-08-28).** This entry used to read *"nothing slips through today — the PAPPG's own curated copy of each rule catches both correctly"*, so the visible symptom was only a duplicate row reading as partial compliance beside a correct flag. That safety net was the EXTENDED tier, and the 2026-08-26 basics-only decision retired it: **19 of the PAPPG's 22 prohibitions are `extended` and only 3 are live**, `Do Not Submit Unauthorized Letters of Support` among the retired, and both sections concerned — Special Information and Budget — hold **0 basic rows**. So the duplicate that was covering for this is gone and the solicitation's unmarked row is now the ONLY one checking either rule. Measured on the awarded package, 10 uploads: *"Do not include letters of support from collaborators"* came back **`partial` 5 runs and `addressed` 5 runs** on a draft carrying collaboration letters — and `addressed` is worse than the `partial` recorded above, because it awards the row FULL credit for containing the forbidden thing. **Two open items in this section interact, and neither entry said so** — worth re-reading both before changing either. The mirror risk is the dangerous one and is why this is not a quick regex: marking a CONTENT rule as a prohibition makes a draft that OMITS it come back `clear`, reporting a missing requirement as compliance.
- **OPEN — the two modals now present the same kind of number two different ways.** `SectionCheckModal` leads with the FRACTION (`7 of 7`, percentage demoted, no progress bar, length measurement in the same box) after a PI read a 2.5rem green **100%** on a 152-word section as a grade. `DraftReviewModal`'s `ScorePanel` still leads with a large percentage and a full bar. Left alone deliberately — its number spans ~200 rules across 14 sections, where `112.5 of 157` reads worse than `72%` — and it does carry the new presence caption. But CLAUDE.md's own rule for these two files is that they must not contradict each other on screen, and right now they do.
- **OPEN — `review_draft` does not proofread.** `services/proofread.py` is wired into `review_section` only. A whole package is many thousands of words and one model call over all of it is a different feature with a different cost and latency, not this one scaled up. So a PI who checks section by section gets their typos found and a PI who pastes the whole package does not, with nothing on screen saying so.
- **OPEN, UNDIAGNOSED — the ORA process tasks are duplicated on re-attach.** Observed 2026-08-13 on a dev proposal: **all ten `ora_process` tasks present THREE times** (20 of 46 tasks were duplicates), written as three contiguous id blocks, on a proposal created ONCE and then re-attached twice. The template is seeded by `create_submission` and `create_submission_from_solicitation`, both create-only; `attach_solicitation` (`PUT .../solicitation`) calls only `sync_solicitation_requirement_tasks` (idempotent, keyed by `source_ref`) and `sync_required_attachment_tasks` (dedupes on title). **The re-seed was not located** — start by instrumenting the attach path rather than re-reading it, since reading it is what already failed. A PI who re-reads their solicitation grows ten duplicate tasks each time.
- **RESOLVED 2026-08-17 — PAPPG support reaches a review for NINE sections, 156 rules** (a tenth slice, Table of Contents, correctly yields none — Research.gov generates it; `sections_offered` narrows the nine to **seven** for the Section Check picker). 14 curated by hand from Research.gov's login-gated "Content Instructions" pages, plus 142 extracted from the PAPPG itself and reviewed row by row (`kb_pappg/build_rules.py`; see Draft Review above). The original limitation, for context: `delegated_rules.rulebooks_in()` could identify a PAPPG citation but a solicitation almost never restates what the PAPPG itself requires, so four sections were all a curated table could cover.

- **THE UNTRIED LEVER WAS TRIED AND IT WORKS: slice the SOURCE, and the section stops being a guess (2026-08-17). 161 rules, 156s, 0 invented sections — against 598 rules / 97 invented sections / 245s.** `kb_pappg/slicer.py` → `backend/kb_structured/_pappg_24_1_sections.json` → `services/pappg_ingest.py` → `kb_pappg/ingest_all.py`. **WIRED IN 2026-08-17**, via a reviewed table — `kb_pappg/build_rules.py` turns this UNREVIEWED extraction into `_pappg_24_1_rules.json`, 161 rows -> 142, each carrying the decision that kept it. Golden rule 4 stood: the extraction never reached a PI unread. See "THE PAPPG RULES ARE IN" under Draft Review.
  - **WHY IT WORKS WHERE THREE ATTEMPTS FAILED, and it is not the prompt.** All three fed the model a chapter and asked which section each rule belonged to; that is where 97 invented sections came from. §II.D.2 is organised by the documents a PI *uploads* — Cover Sheet · Project Summary · Table of Contents · Project Description · References Cited · Budget and Budget Justification · Facilities · Senior/Key Personnel · Special Information — which maps 1:1 onto Research.gov's upload pages. Slice first and the section is an **input**: the model's own `section` field is discarded outright, and a test asserts the discarding happens end to end. Measured: Chapter II is 232,859 chars; the ten slices total **94,001**, and the largest (Budget, 34,600) is under `CHUNK_CHARS`, so **every section is ONE read** — 1 round, 1 chunk, no sweep, no caps.
  - **BOUNDARY DETECTION IS THE WHOLE RISK AND TWO OBVIOUS RULES ARE BOTH WRONG.** Each title appears twice — table of contents and body heading — and picking wrong yields a ~20-char slice that reads exactly like a section with few rules. *"Followed by prose"* breaks on Project Description and Senior/Key Personnel, whose body headings are legitimately followed by sub-headings. *"Not followed by a lettered heading"* breaks on Senior/Key Personnel, whose **TOC** entry is followed by `(i) Biographical Sketch(es)`. What holds: the TOC block is contiguous and comes first, so the body is the **LAST full-line match**. A third trap: the body heading carries a parenthetical the TOC lacks — `d. Project Description (including Results from Prior NSF Support)` — so exact-title matching finds only the TOC entry. Guarded three ways: strictly increasing offsets, a ±15% char band per slice, and an explicit §II.E end boundary without which the last slice swallows **126,792 chars** of §II.F proposal-type variants. Mutation-tested: switching to first-match yields a **14-char Cover Sheet** and the band guard refuses it.
  - **`canon_section` ≠ `section_key`, and they disagree on SIX of the nine titles.** The extractor canonicalises with `solicitation_requirements.canon_section` (strips filler, singularises); everything else files rules under `solicitation_profile.section_key` (neither). Worst case `facilities_equipment_and_other_resources` vs `facilitie_equipment_other_resource` — a different string entirely, and that exact mismatch silently disabled four rules earlier the same week. Ingested rows are keyed from the **slice**, never from the model. Also: the extractor emits **8** keys and a baseline row needs **13** (`section_label`, `rulebook`, `source_url`, `check`, `check_args`), and `section_label` must carry the real name **with punctuation** — title-casing a key is what broke Facilities.
  - **THE GATE: 12 of 12 curated rules recovered** (`tests/test_pappg_recall.py`, opt-in via `PAPPG_RECALL=1`, live model, never in CI). Project Summary 5/5, Project Description 3/3, References Cited 1/1, Facilities 3/3. The 14 curated rules are the only yardstick that exists for this document, because Research.gov is NSF's own distillation of these same sections. **If extraction stops recovering them, the method is wrong and nothing else it produced should be trusted** — that checkpoint is what the three previous attempts never had.
  - **DO NOT score this with the solicitation recall test's 5-gram shingles.** That method works because both sides quote ONE document. Here they quote TWO that paraphrase each other, and one Research.gov sentence maps to two PAPPG ones; overlap scored **0.31 against a 0.34 floor** — a failure saying nothing about the extraction. Two further automated heuristics were tried and both mis-scored rules the slice demonstrably states. The mapping is written out by hand instead. That is not weaker: cross-document equivalence *is* a judgement, and writing it down makes it reviewable rather than hidden in a threshold.
  - **RESEARCH.GOV AND THE PAPPG PLACE THE SAME RULES DIFFERENTLY, and this has a design consequence.** A first pass concluded two curated rules were "not in the PAPPG"; reading the full extraction disproved it. The unfunded-personnel rule is in §II.D.2.f **Budget** (*"their role on the project should be described in the Facilities, Equipment and Other Resources section"*), not Facilities. *"et al."* is in §II.D.2.h **Senior/Key Personnel** and says the **opposite** of Research.gov — biosketch authors *may* use it, while Research.gov says avoid it in References Cited. Both true, of different sections. **Research.gov states a rule where you NEED it; the PAPPG states it where the situation ARISES.** So "the section is an input" is right for EXTRACTION and wrong for PLACEMENT — a rule pulled from the Budget slice may need filing under Facilities, and that is a judgement for human review that cannot be inferred from the slice. Do not replace the curated table with an extraction.
  - **A LOW-YIELD SECTION IS A LOST SECTION, and the first full run had one.** Cover Sheet, 6,804 chars, returned **2 rows** after `[GEMINI] JSON parse failed: Extra data: line 1 column 5134` — the parse died and every requirement in that response was lost with only a log line, the same failure CLAUDE.md records from the 8192-token bug. Every other section yielded 1.8–6.2 rows/1k chars. Nothing went red. `read_section` now reports `suspicious_yield` and `ingest_all.py` exits non-zero. **The guard measures the RAW extraction, before `draft_scope`** — scoring the kept rows flagged Cover Sheet as broken on a run that had worked fine, because it is a Research.gov **form** and `draft_scope` correctly drops most of its rules. A guard that cries wolf on a form-shaped section gets turned off, and then it will not catch the parse failure it exists for. Table of Contents legitimately yields **0** (Research.gov generates it).
  - **Run-to-run variation is large — do not treat a count as a fact.** Two full runs: **175 then 161** rows; Budget 58 → 47; Senior/Key 48 → 35. Same input, temperature 0. Same class as the 43-vs-47 recorded for solicitations.
  - **ALL FOUR "still to do" ITEMS ARE DONE (2026-08-17), and three of them turned up bugs.** They were: human review of the 161 rows; placement for cross-section rules; `_SECTION_LABELS`/`_SECTION_ORDER` needing the new sections or `sections_offered`/`covered_sections` degrading silently; and the modals rendering one always-open flat list that 161 rules would bury. Each is closed and written up under "THE PAPPG RULES ARE IN" in the Draft Review section — including the three bugs the work surfaced that this list did not predict: 45 rules orphaned on a section key that lost a merge, prohibitions scored as content rules so a compliant draft was told to fix them, and unscored conditionals filling 71% of a fix-list.

- **`draft_review._review_section` is BATCHED at `REVIEW_BATCH = 15` (2026-08-17), and the batching is load-bearing.** Every requirement for a section used to go into one prompt capped at `max_output_tokens=8192`; when the reviewer cannot fit every row it **omits** rows rather than truncating visibly, an omitted row becomes `unclear`, and `unclear` is absent from `_CREDIT` — so it leaves the score's denominator with nothing on screen to say so. Four sections of curated rules never came close to the ceiling; the PAPPG's Project Description alone yields ~21 rows and Budget ~47. A batch whose model call fails falls back to `unclear` for **its** rows only — losing 40 assessments because one call timed out would be worse than the problem this solves. Three deterministic tests in `tests/test_rulebook_wiring.py`, all observed red first.
- **OPEN — `review_section` and `review_draft` (`services/draft_review.py`) handle `use_ai=False` through different MECHANISMS, and only one is a real gate.** `review_section` branches on the flag explicitly: when `False` it skips the model call outright and runs the same `_semantic_fallback` an AI outage uses, so a caller sees `unclear` rows with an explanatory message ("the requirements needing judgment are marked unclear ... only the rule-based checks are complete") rather than the rows silently vanishing — this was the state mid-review; the fallback branch was added to fix it. `review_draft`'s `use_ai=False` does **not** stop the per-section Gemini call: `pool.submit(_review_section, *job)` fires unconditionally whenever a section has semantic requirements, flag or no flag — the flag only forces `locate_sections` onto its non-AI path and skips `_reviewer_notes`. The deterministic-only test suite passes only because `conftest.py` pins `gemini_client.get_client()` to `None`, so the call fails on its own and `_semantic_fallback` runs anyway; in production, calling `review_draft(..., use_ai=False)` against a live, configured client would still make model calls for every section with semantic rules. The two entry points reach the same honest outcome (`unclear` rows, not silence, not a fabricated `not_found`), but a caller who wants to *guarantee* zero model calls can do it on `review_section` and cannot on `review_draft`. Not yet reconciled.
- **RESOLVED 2026-08-07 — the 14 staff PROFILE pages are now monitored.** All 14 `staff_*` documents point `source_url`/`procedure_url` at the person's profile page (the directory is kept as `directory_url`), and Moncrieffe's out-of-prefix profile is in `_ORA_ALIASES`. The original problem, for context: Every `staff_*` document carries `source_url`/`procedure_url` = the staff **directory** page, so the scraper watches names, titles, phones and emails but **not** the profile pages where the bios, degrees, certifications and publication lists actually live. A new credential or a rewritten bio would never be detected, and a citation cannot deep-link to a person's page. Two related quirks: `/staff-directory/farin-kamangar` and `/staff-directory/farin-kamangar-md-phd` are **byte-identical** (one document, two monitored URLs), and Keyshawn Moncrieffe's document carries detail (PhD, MBA, CCEP, the NSF-funded RISC initiative) that appeared on no page in the crawl — **that is now known to be a SCOPE artifact, not unsourced content**: their profile page lives outside the section prefix, and every claim in the document matches it word for word.
- **The live site contradicts itself on the current F&A rate, and that one is ORA's to fix.** `/pre-award/budget-development` says *"the current on-campus research F&A cost is 53% of the modified total direct cost"*; `/pre-award/fanda-cost-rates` gives **54.00% On-Campus / Organized Research** for 07/01/2025–06/30/2026 (the current period) and 53.00% for the prior year. `pre_award_budget_development` now records both **and points at the F&A Cost Rates page as authoritative**, which is the best the KB can do — it cannot resolve a contradiction that exists on the source. Raise it with ORA as a **website** correction.
- **A KB content fix is TWO writes, and neither implies the other.** `scripts/apply_kb_gap_fixes.py` writes the committed snapshot (`/forms`, the citation URL map, `kb_tree`, the scraper's URL index — all of which need a **backend redeploy** to take effect); `scripts/push_kb_gap_fixes.py` writes the live datastore (the chatbot, effective immediately). Fixing only the snapshot changes nothing a PI would notice in chat. **And after the datastore write, the chat answer cache must be cleared** (admin → Sync All / `POST /api/admin/knowledge-base/sync-all`): the cache is keyed on `md5(normalized_query : model)` with no content hash, so a cached answer keeps replaying precisely the fact the push just corrected, for up to 7 days.
- **RESOLVED 2026-08-13 — an abandoned read is now OFFERED BACK instead of being invisible.** The problem, for context: the text is stored the moment the contract read finishes but is **bound to a proposal at SAVE**, so abandoning the modal at the review step (or closing it during the 60–150s requirement read) left an orphan with `submission_id = NULL`. Observed in a dev DB: **three** rows holding the identical 50,508-char solicitation, two owned by one user, all unbound, while every one of that user's proposals still said *"This proposal needs its solicitation read in full"* — and the panel that would have helped (`canReread`, *"already on file … no upload needed"*) is gated on `has_solicitation_source`, which asks whether a row is bound to **that submission id**, so an orphan never qualified. Now: `list_unbound_solicitation_sources` + `GET /api/me/solicitation-sources/unbound` feed a picker in `SolicitationUploadModal`'s pick step, and Draft Review's panel offers the document directly. Four rules, each load-bearing: **(1) no auto-binding** — nothing in the data says which document belongs to which proposal, so the PI confirms; **(2) bound rows are excluded** — those are in USE, and offering one invites moving a solicitation off the proposal that owns it; **(3) the listing is filtered to the same `_UNBOUND_SOURCE_TTL` window the reaper uses**, because the reaper only runs when a NEW source is written, so a stale orphan can outlive it in the table and offering a three-day-old read resurrects a forgotten session; **(4) `text` is NEVER returned by the listing** (~300KB/row, and nothing renders it — the same reason it is not a column on `Submission`). `/from-solicitation` learned `source_id`, taken **in preference to a file**, mirroring `/solicitation-requirements`; it echoes the SAME id so reuse cannot store a second copy. Draft Review's "Use that document" routes through `onAttach(sourceId)` into the **one existing save path** rather than binding itself — a second attach path would drift. The picker is an affordance, never a gate: if the list call fails it simply does not render. Verified live: reuse in 4.4s with no upload, row count unchanged, another user's id 404s. Tests in `tests/test_solicitation_source_persistence.py`.
- **NSF (and similar Akamai sites)** block server-side URL fetches by IP. **Firecrawl** (set `FIRECRAWL_API_KEY`, free tier ~500 one-time credits) is the reliable path; the Jina/allorigins fallbacks are throttled from Cloud Run. Locally the direct fetch works (residential IP isn't blocked). Uploaded PDFs always work regardless.
- Drafting-coach AI tailoring/feedback quality depends on the deployed Gemini being healthy; it degrades to deterministic checks otherwise.
- Demo aids live in repo root: `ORA_Navigator_Demo_Script.pdf`, `Sample_Solicitation_for_Demo.pdf` (generated by `scripts/make_demo_pdfs.py`).
- **Vertex AI Search runs on pure defaults, and most knobs are unreachable from the chat path.** `agent.py` builds `VertexAiSearchTool(data_store_id=UNIFIED_KB_ID)` — datastore ID and nothing else — and the only `SearchRequest` in the repo (`vertex_agent.py:530`, the citation fallback) sets just `serving_config`/`query`/`page_size`. Verified live against `oranavigator-kb-v8`: **no Engine/app exists** (`engines` → `{}`), **no materialized ServingConfig** (`GET .../servingConfigs/default_search` → 404; search works only because `default_search` is *implicit*), no Controls, no custom models. Consequences: **(a)** synonym maps (the "subrecipient ≈ subaward" fix) require a Control attached to a serving config, so they are **unreachable without first creating a search app** — do not plan on them as a config tweak; **(b)** extractive answers/segments and `summary_spec` are **Enterprise/engine-level** and fail closed today (`FAILED_PRECONDITION`, `LLM_ADDON_NOT_ENABLED`); **(c)** `chunkingConfig` cannot be enabled after datastore creation and doesn't support `text/plain`, so `searchResultMode: CHUNKS` would need a **new datastore**. What DOES work now, unused: `filter` (bare field path — `category: ANY("...")`, **not** `structData.category`), `boost_spec` (±1, verified reordering), `content_search_spec.snippet_spec` (real snippets on Standard edition), `relevance_threshold` (10/10/5/4 results at LOWEST/LOW/MEDIUM/HIGH), `relevance_score_spec` (per-result 0–1 score — a real external signal, unlike Gemini's self-reported `coverage`). The live `default_schema` already marks every `struct_data` field indexable/searchable/filterable (except `title`, a `keyPropertyMapping`), so **no schema patch is needed** first. Two ceilings: the genai `VertexAISearch` type exposes only `datastore`/`engine`/`data_store_specs`/`filter`/`max_results` (so the **chat path can never use boost, snippets, thresholds, or scores** — only the direct `SearchServiceClient` path can), and `google-cloud-discoveryengine==0.13.5` is pinned while `relevanceThreshold`/`relevanceScoreSpec`/`rankingExpression` only exist in newer clients (current 0.20.2).
- **The KB is deliberately non-redundant — never gate an answer on "N sources".** Facts live on exactly one page, mirroring morgan.edu/ora's nav. Measured: the off-campus F&A rate (`26%`) and the instruction rate (`64%`) each appear in **exactly one** document; `54%`/`53%` in two. So a hard "must be backed by ≥2 sources" rule would **reject correct answers** about single-source facts. This is also why `_GROUNDING_MIN_CHUNKS = 2` was retired: when live it was `chunks >= 2` **OR** `coverage >= 0.3` (a *sufficient* pass route, never a requirement). If a source count is reintroduced, keep it an **OR**, and count distinct resolved citations — not Gemini's self-reported `result["chunks"]`, which is unreliable.
- **`BASE_INSTRUCTION` grounding rule 5 instructs a tool that does not exist.** `agent.py:378` tells the model to *"call `list_kb_topics` FIRST"* with detailed path examples, but `agent.py:476` registers `tools=[unified_kb]` only — `list_kb_topics` is imported and never wrapped in a `FunctionTool`, deliberately (the comment at `agent.py:473` records the Gemini API constraint: you cannot mix `VertexAiSearchTool` with `FunctionTool` in one agent). The functionality moved to the backend pre-processor (`kb_browser.try_browse`), but the instruction was never removed — so every turn spends prompt space telling the model to call an unsatisfiable tool, worst on the enumeration questions where it will try hardest. Safe to delete rule 5's tool references.
- **Two copies of the KB metadata, and nothing syncs them.** The live **Vertex AI Search datastore** (`oranavigator-kb-v8`) is what the chatbot searches and what the admin dashboard edits. `backend/kb_structured/` is a **static snapshot committed to the repo**, read at import and cached for the process lifetime — it powers the **Forms catalog** (`forms_catalog.py` reads `_all_documents.jsonl`, 383 rows), `_get_kb_url_map()` (citation title→URL, now with a live overlay — see Load-bearing fixes), `kb_tree.py` (tree shape + curated titles), `kb_browser.py`, the scraper's URL→document index, and `proposal_templates.py`. So **editing a form document in the admin dashboard fixes the chatbot's answer but NOT the `/forms` page**, and a doc added to the datastore never appears under `/forms` — with no error either way. Refreshing the snapshot requires regenerating the JSONL and redeploying the backend. Two things have narrowed this: `kb_path` moved **placement** onto the live documents (the manifest now only supplies shape and titles), and the citation map overlays live `source_url`s. **Approving a scrape change still writes to the datastore only** — `/forms` is unaffected.
- **The snapshot's 383rd row has no file.** `form_irb_informed_consent_template` is in `_all_documents.jsonl` with an **empty `file_path`** and no `.json` on disk — hand-added in `5dba735` without re-running the generator. Consequences: it has no derivable `kb_path` so it shows under **Unfiled** in the tree (correct, and the tree surfacing it is the feature working), and **`restructure_kb_v2.py --manifest-only` would silently drop it**, taking the JSONL from 383 → 382. Restore the file before regenerating.
- **There is no scraper for the original KB build, and never was.** The 382 documents were produced on 2026-05-14 by a **Claude Code session** — WebFetch for breadth, the Playwright MCP server for the JS accordions, then a verification sweep — not by any script in the repo. Residue is still at `.playwright-mcp/` (gitignored). `restructure_kb_v2.py` only *reorganizes* what already exists; it fetches nothing. `kb_scraper/` (2026-07-28) is the first real crawler, and it **re-checks** pages rather than rebuilding documents — the stored `content` is LLM-summarized prose plus hand-authored fields (`key_facts` in 51 docs, `leadership_history`, `irb_voting_members`, staff `phone`/`office`) that a scrape cannot regenerate.

## Docs
Human-readable docs in `docs/` (`docs/README.md`, `docs/sections/*.html`). Cross-tool entry point: `AGENTS.md`.
- **Building the PDFs:** `python3 docs/build_guide.py` assembles `docs/sections/*.html` (11 chapters, order asserted against the `TOC` list — adding a section without a TOC entry fails the assert) into `ORA_Navigator_Complete_Guide.html`; print it to PDF with headless Chrome (`--headless --no-pdf-header-footer --print-to-pdf=...`) → ~94 pages. `docs/ORA_Navigator_Technical_Brief.html` is a standalone 5-page summary using the same palette.
- **Two page-break gotchas** (both cost a rebuild cycle): use **`page-break-before`** on the per-page wrapper, not `page-break-after` — the latter silently doesn't split in Chrome's print path and sections run together. And **a callout closed with `</p>` instead of `</div>` swallows the rest of the chapter** into the orange box, inflating the layout (this was live in `08_kb.html`). Check with a tag-balance pass over `docs/sections/*.html` before building.
- **Tests must not hard-code future dates.** `test_opportunity_finder.py` pinned `closeDate: 07/21/2026`; from 07/22 on, `_is_open()` correctly filtered the fixture out and the suite went red with no code change. Compute fixture dates relative to `datetime.now()` (the sibling tests already do).
