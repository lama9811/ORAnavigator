# Kept deliberately small — this image only crawls, hashes, judges and writes.
playwright==1.49.0

# Shared with the backend. Versions MUST match backend/requirements.txt: this
# image reuses backend/db.py, models.py and datastore_manager.py directly, and a
# SQLAlchemy or client drift would mean the job and the API disagree about the
# same tables and the same datastore.
SQLAlchemy==2.0.41            # backend/requirements.txt:60
python-dotenv==1.1.0          # backend/requirements.txt:52
google-genai==1.14.0          # backend/requirements.txt:77
google-cloud-discoveryengine==0.13.5   # backend/requirements.txt:79
PyMySQL                       # backend/requirements.txt:80 (unpinned there too)

# PyMySQL needs this for MySQL 8's caching_sha2_password. The backend gets it
# transitively via python-jose[cryptography]; here it has to be explicit.
cryptography

# File readers for the document phase. All pure-Python — the image gains these
# wheels and no system libraries. pdfplumber's version MUST match
# backend/requirements.txt:88: the job and the solicitation extractor parse the
# same PDFs, and a drift would mean the two disagree about the same file.
pdfplumber==0.11.1            # backend/requirements.txt:88
python-docx==1.1.2
python-pptx==1.0.2
openpyxl==3.1.5
